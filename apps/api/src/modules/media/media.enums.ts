export enum MediaType {
  IMAGE = 'image',
  VIDEO = 'video',
}

export enum MediaProvider {
  UPLOAD = 'upload',
  YOUTUBE = 'youtube',
  VIMEO = 'vimeo',
}

export enum MediaStatus {
  PENDING = 'pending',
  PUBLISHED = 'published',
  HIDDEN = 'hidden',
  REJECTED = 'rejected',
}
