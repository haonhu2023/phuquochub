# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
out/
*.tsbuildinfo

# Turbo
.turbo/

# Testing
coverage/

# Env
.env
.env.local
.env.*.local
.env.bak-*
!.env.example

# Logs
logs/
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Docker volumes (local)
.data/

# PLACE-038: real database backups produced by scripts/backup.sh -- never committed
backups/

# Data Quality Audit (2026-08-20): generated JSON/Markdown reports change every run as real place
# data changes — a work-queue snapshot for a human to review locally, not a source-controlled artifact.
apps/api/reports/

# Claude Code per-machine local settings (not shared)
.claude/settings.local.json
# Deployment / release transfer artifacts
.release-transfer/
deploy-*.txt
deploy-*.sha256
release-*.txt
release-*.sha256
*.tar.gz
