schema_version: "1.0"

project:
  id: phuquochub
  name: PhuQuocHub

delivery:
  framework: enterprise-ai-delivery-framework
  mode: state-driven
  initialized: true

repository:
  # PLACE-020 (2026-07-24): the authoritative working copy is now D:\Projects\PhuQuocHub, a git
  # working tree on NTFS. The prior F:\PhuQuochub inspection (FAT32, vcs none) is SUPERSEDED and
  # treated as obsolete per owner instruction; it is recorded here as history, not deleted.
  inspected_at: "2026-07-24"
  path: "D:\\Projects\\PhuQuocHub"
  vcs: git
  branch: master
  commit: 0ca958f                 # 889d01f (baseline) -> c661912 (health e2e fix) -> 0ca958f (hygiene)
  working_tree: clean
  remote: none                    # local only; no remote configured, nothing pushed
  volume: "D: (NTFS) — real npm workspace symlinks available; FAT32 copy-staleness risk gone"
  superseded_inspection:
    path: "F:\\PhuQuochub"
    volume: "F: (FAT-family / non-NTFS)"
    vcs: none
    note: "Obsolete checkout; not a git working copy when inspected 2026-07-22."

current:
  phase: implementation
  workstream: place
  task: none                      # (2026-08-02, Media Orphan Cleanup -- RETURNING result-shape
                                  # corrective fix) Owner-directed focused corrective task,
                                  # immediately following Moderation Foundation M3 the same day.
                                  # M3's live validation found and fixed a defect in MediaRepository
                                  # .attachAndPublish() -- TypeORM's Postgres driver returns
                                  # UPDATE/DELETE ... RETURNING results from manager.query()/
                                  # repo.query() as a [rows, rowCount] TUPLE, not a plain rows array
                                  # (confirmed by reading PostgresQueryRunner.js) -- and flagged
                                  # MediaRepository.softDeleteOrphanCandidate() (a DIFFERENT,
                                  # already-shipped milestone) as suspected of the identical pattern,
                                  # explicitly out of M3's scope. This task re-opened that question
                                  # read-first, confirmed the suspicion live against REAL Postgres
                                  # (not a mock) before touching any code: a row with deleted_at
                                  # already set (UPDATE's WHERE matches 0 rows, genuinely ineligible)
                                  # still made the method return true. Root cause identical to
                                  # attachAndPublish's -- rows.length on an unde-structured 2-element
                                  # tuple is always 2, always >0, regardless of actual match count.
                                  # Blast radius scoped precisely: the eligibility predicate itself
                                  # was never bypassed (a row that was never eligible could never
                                  # actually be soft-deleted by this bug), and deleted_at was never
                                  # set incorrectly -- the defect was purely in the return value,
                                  # whose only real-world consequence is a phantom media.
                                  # orphan_cleaned audit row in the narrow race window where a row
                                  # becomes ineligible between findOrphanCleanupCandidates()'s SELECT
                                  # and softDeleteOrphanCandidate()'s conditional UPDATE (a
                                  # concurrent cleanup process winning the race, or an owner being
                                  # attached in that instant) -- narrow enough that neither the
                                  # original unit tests (wrong mock shape) nor e2e tests (never
                                  # manufactured the race) ever caught it. Fixed with the identical
                                  # destructuring pattern as attachAndPublish's fix
                                  # (`const [rows] = await this.repo.query(...)`), strictly scoped:
                                  # no change to the eligibility predicate, storage-before-DB
                                  # ordering, or dry-run behavior. Two corrected unit test mocks
                                  # (previously asserting the wrong plain-array shape) plus one new
                                  # unit test and one new live e2e test (simulating the exact race
                                  # window by setting deleted_at directly then calling
                                  # softDeleteOrphanCandidate() directly, bypassing the SELECT that
                                  # would otherwise mask the race) -- both new tests verified
                                  # MEANINGFUL by temporarily reverting the fix via git stash and
                                  # confirming they fail with the bug's exact signature (Received:
                                  # true) before passing again with the fix restored. Full
                                  # re-validation: backend unit 95 suites/1019 tests (up from 1018),
                                  # e2e 16 suites/143 tests (up from 142), zero regression, full
                                  # monorepo build/typecheck/lint green, git diff --check clean,
                                  # secret scan clean. Documented as an addendum (SS12) to the
                                  # existing MEDIA-ORPHAN-CLEANUP-POST-IMPLEMENTATION-REVIEW-
                                  # 2026-08-02.md report rather than a new report file, since it is a
                                  # follow-up to that same milestone's own review, not a new
                                  # milestone. ADR-018 not touched; M4 not started.
                                  # ---- prior state (2026-08-02, Moderation Foundation M3 -- Media
                                  # Decision Workflow + auto-publish-on-review-attach) ----
                                  # Owner-approved
                                  # implementation of ADR-018 M3, immediately following M2 the same
                                  # day. Scope was explicit and narrow: media moderation decisions
                                  # (POST /moderation/cases/{id}/decide, target_type=media only --
                                  # review decisions are M4), auto-publish-on-review-attach,
                                  # transactional review creation, legacy pending-review-media
                                  # backfill, audit, domain events, tests, docs -- NO review
                                  # moderation, NO report create/resolve, NO moderator frontend, NO
                                  # notifications, NO AI, NO sanctions, NO scheduler, NO new Owner
                                  # policy (all confirmed absent from the diff). Two new
                                  # transactions, both matching moderation-design.md SS7 exactly: T1
                                  # (ReviewsRepository.createWithMedia -- review insert +
                                  # MediaRepository.attachAndPublish + PlacesRepository.
                                  # recalculateRating, one dataSource.transaction, all-or-nothing --
                                  # if published-media count != requested media_ids count, throws
                                  # and the whole review is rolled back, INV-14) and T2
                                  # (ModerationService.decide -- pessimistic-lock read of case +
                                  # media, FSM validation via the EXISTING pure
                                  # assertValidMediaTransition (no duplicate transition logic in the
                                  # service), status + case update, commit; audit/event emitted ONLY
                                  # after commit in a separate try/catch that logs but never reverts
                                  # on failure, per ADR-018 INV-9). Auto-publish eligibility
                                  # (object_key IS NOT NULL, uploaded_by = caller, status=pending,
                                  # deleted_at IS NULL, orphan -- no place/post/business/event/
                                  # review_id set, D3's 6 conditions) is enforced in the UPDATE's SQL
                                  # WHERE clause itself, not just in TypeScript, so the affected-row
                                  # count is a true reflection of what the database actually allowed.
                                  # CRITICAL BUG found and fixed during Phase 8 live validation
                                  # (not caught by unit tests, whose mocks assumed the wrong shape):
                                  # TypeORM's Postgres driver returns manager.query()'s raw result as
                                  # a [rows, rowCount] TUPLE for UPDATE/DELETE statements with
                                  # RETURNING (confirmed by reading
                                  # node_modules/typeorm/driver/postgres/PostgresQueryRunner.js),
                                  # unlike INSERT/SELECT which return rows directly -- MediaRepository
                                  # .attachAndPublish() read the tuple as if it were the rows array,
                                  # so publishedMediaIds.length was always 2 regardless of actual
                                  # match count, inverting the all-or-nothing check (201/422 swapped
                                  # in e2e). Fixed by destructuring `const [rows] = await manager.
                                  # query(...)`; found live only because Phase 8 ran a REAL upload +
                                  # review round trip against real Postgres instead of trusting mocks.
                                  # A second, PRE-EXISTING instance of the same bug class was found in
                                  # MediaRepository.softDeleteOrphanCandidate() (a different,
                                  # already-shipped milestone) -- explicitly OUT OF SCOPE for M3,
                                  # flagged via a spawned follow-up task rather than fixed inline.
                                  # BackfillModerationCases migration (T4/D14/O7): counts eligible
                                  # legacy rows (status=pending AND review_id IS NOT NULL AND
                                  # deleted_at IS NULL) BEFORE writing, logs the count, INSERTs via
                                  # ON CONFLICT DO NOTHING (idempotent against the M1 partial unique
                                  # index), never touches media.status (O7 -- no bulk publish), and
                                  # writes one summary moderation.backfilled audit row per
                                  # invocation. LIVE-VERIFIED against real dev-DB legacy data (11
                                  # genuine pending+review_id media rows already present from
                                  # pre-M3 activity, not synthetic fixtures): first migration:run
                                  # created exactly 11 open cases (candidate_count=11,
                                  # created_count=11); a second direct invocation of the same up()
                                  # against the same live data produced candidate_count=11,
                                  # created_count=0 -- proving DB-level idempotency via the unique
                                  # index, not just a mocked assertion. down() refuses to revert if
                                  # any backfilled case has since been resolved/dismissed (MR-5),
                                  # verified both ways. Full apply/revert/reapply drill run for real
                                  # (not simulated) as part of Phase 9. Decision live-validated with
                                  # a real moderator token against real data: approve/reject
                                  # (reason required, INV-11)/hide/restore (target_status required,
                                  # no default, INV-10)/dismiss, invalid-transition 422 (INV-13),
                                  # self-moderation 403 (INV-12), already-resolved 409. Full
                                  # validation: backend 95 suites/1018 tests (up from 91/951 at end
                                  # of M2), e2e 16 suites/142 tests (up from 14/124), zero regression
                                  # in either; full monorepo build/typecheck/lint all green; git diff
                                  # --check clean (benign CRLF notices only); secret scan clean. Docs
                                  # updated: docs/api/openapi.yaml (new POST /moderation/cases/{id}/
                                  # decide path + request schema), docs/data/modules/
                                  # moderation-design.md (M3 marked done in banner + roadmap table
                                  # only, noting claim/release/reopen and POST /media/{id}/moderate
                                  # were NOT built -- decide() alone covers the M3 contract). ADR-018
                                  # was NOT modified -- implementation revealed no contradiction with
                                  # it. See docs/delivery/reports/
                                  # MODERATION-M3-MEDIA-WORKFLOW-2026-08-02.md. M4 (Review Decision
                                  # Workflow + rating recalculation in transaction) is the next
                                  # milestone -- explicitly NOT started per instruction.
                                  # ---- prior state (2026-08-02 Moderation Foundation M2 -- Queue
                                  # Read API) ----
                                  # Owner-approved implementation of ADR-018 M2, immediately
                                  # following M1 in the same day. Scope was explicit and narrow:
                                  # GET /moderation/cases + GET /moderation/cases/{id}, READ-ONLY --
                                  # NO decision endpoints, NO report create/resolve, NO
                                  # auto-publish-on-review-attach, NO M3 backfill, NO moderator
                                  # frontend, NO AI, NO notifications, NO new database schema (all
                                  # explicitly excluded by the Owner's task and confirmed absent
                                  # from the diff -- zero migration file, zero status-mutating
                                  # method call anywhere in the new code). Extended the EXISTING M1
                                  # repositories (ModerationCasesRepository.list()/
                                  # findTargetPreview(), ReportsRepository.findByCaseId()) rather
                                  # than creating a third persistence path -- list()/getCount() run
                                  # off the SAME QueryBuilder instance so filters can never drift
                                  # between the two (list/count parity, live-verified). Sort order
                                  # is FIXED (priority DESC, report_count DESC, created_at ASC, id
                                  # ASC tie-break) -- deliberately no sort_by/sort_dir query param,
                                  # since ADR-018/moderation-design.md document exactly one queue
                                  # order and no configurable one; adding one would have been
                                  # inventing a filter the ADR doesn't specify. Same reasoning ruled
                                  # out a created-date-range filter -- checked moderation-design.md
                                  # SS9's API table twice, confirmed only 5 filters
                                  # (status/target_type/source/severity/assigned_to) are actually
                                  # documented, not the date-range some earlier phrasing implied.
                                  # Target preview (GET .../{id} only, never on the list route --
                                  # avoids N+1) is a real but conservative decision: media preview
                                  # exposes type/status/uploaded_by/created_at and DELIBERATELY
                                  # OMITS object_key/bucket/checksum/url (storage internals, not
                                  # needed for a moderation decision, and no viewable image URL --
                                  # that needs StorageService signed-URL integration ADR-018 never
                                  # specified, disclosed as an honest limitation, not silently
                                  # skipped); review preview INCLUDES full content, since reading it
                                  # is literally the point of the endpoint, unlike media metadata.
                                  # report_id/reason/description/status ARE exposed on the detail
                                  # response (reporter_id too) -- confirmed correct against the
                                  # design's own SS12 ("hàng chờ chỉ hiện danh tính người báo cáo
                                  # cho người có Moderation.Queue.View", which this endpoint is
                                  # gated behind) -- but no join to `users` for reporter name/email,
                                  # since that field was never specified and would be excess
                                  # exposure beyond the documented report summary. ai_score/
                                  # ai_labels (existing ADR-009 columns) deliberately excluded from
                                  # BOTH responses -- M2 is explicitly out of AI scope, even though
                                  # the columns aren't literally an AI feature, to avoid opening API
                                  # surface for an M6/M7 capability that doesn't exist yet. No new
                                  # permission -- reused Moderation.Queue.View from M1's seed
                                  # exactly, scope Any, no role name special-cased anywhere in
                                  # service/controller code (PermissionsGuard/JwtAuthGuard already
                                  # global via APP_GUARD in AuthModule, confirmed by reading it
                                  # rather than assuming). LIVE VERIFIED end-to-end against the real
                                  # dev stack, not just unit tests: registered a real member and a
                                  # real moderator (role granted via a direct user_roles insert --
                                  # first e2e in this repo to do so, no prior precedent existed),
                                  # inserted 4 disposable moderation_cases rows (mixed statuses/
                                  # target_types/severities/sources/assigned) + 1 report row,
                                  # exercised anonymous 401, member 403 on both routes, moderator
                                  # 200 on both, unknown-id 404, invalid-UUID 400, invalid-enum 400,
                                  # single filter/combined filter/assigned_to filter, fixed sort
                                  # order, pagination envelope shape, reporter-privacy (no email/
                                  # name leak), ai_score/ai_labels absence, and a direct before/
                                  # after DB read proving zero mutation from any GET call -- then
                                  # deleted every fixture row (reports/moderation_cases/user_roles),
                                  # confirmed 0 residue via direct queries after the suite ran. Full
                                  # validation: backend 91 suites/951 tests (up from 88/911, +40 new
                                  # M2 unit tests across 4 new/2 extended suites), e2e 14 suites/124
                                  # tests (up from 13/104, +20 new), zero regression in either; full
                                  # monorepo build/typecheck/lint 4/4, 6/6, 6/6 green; git diff
                                  # --check clean; secret scan clean. ADR-018 was NOT modified --
                                  # implementation revealed no contradiction with it. See
                                  # docs/delivery/reports/MODERATION-M2-QUEUE-READ-API-2026-08-02.md.
                                  # M3 (Media Decision Workflow + auto-publish-on-review-attach) is
                                  # the next milestone -- explicitly NOT started per instruction.
                                  # ---- prior state (2026-08-02 Moderation Foundation M1 -- Schema Foundation) ----
                                  # Owner-approved implementation of ADR-018 (Accepted same day,
                                  # after O1-O7 all settled -- see decision-register.md), M1 of the
                                  # 7-milestone roadmap in moderation-design.md. Scope was explicit
                                  # and narrow: schema/enums/entities/migrations/FSM modules/
                                  # permission seed/foundational repositories/unit tests/docs ONLY
                                  # -- NO queue API, NO decision endpoints, NO report endpoints, NO
                                  # UI, NO AI, NO notifications, NO background jobs (all explicitly
                                  # excluded by the Owner's task and confirmed absent from the diff:
                                  # zero controller/service files, zero React files). Two new tables
                                  # (moderation_cases, reports, apps/api/src/modules/moderation/),
                                  # 7 new enums, 6 new permissions (Report.Create->member;
                                  # Moderation.Queue.View/Media.Moderate/Review.Moderate/
                                  # Report.Resolve->moderator; AI.ModerateMedia->ai_agent, NEVER
                                  # Media.Moderate -- "AI cannot self-approve" enforced by the
                                  # permission model itself, not a service-layer check). Two pure
                                  # FSM modules (media-moderation.transition.ts,
                                  # review-moderation.transition.ts) mirroring
                                  # booking-status.transition.ts exactly -- unit-tested against the
                                  # FULL status x action matrix for both (7/16 valid+invalid for
                                  # media, 3/9 for review), including the three ADR-018 INV-13 cases
                                  # (published->rejected/hidden->rejected/pending->hidden) and both
                                  # restore-without-target_status and restore-with-invalid-target
                                  # error paths (INV-10, "never guess the restore destination").
                                  # Migration InitModeration1720003200000's down() self-refuses
                                  # (throws, does not destroy) if any case is already 'resolved' --
                                  # same AddMediaUploadFoundation precedent as MR-5 requires -- LIVE
                                  # VERIFIED twice: inserted a resolved case, confirmed revert was
                                  # refused with the transaction rolled back and the DB untouched;
                                  # deleted the row, confirmed revert then succeeded cleanly. Full
                                  # revert->verify->reapply drill (PLACE-042 precedent) on the real
                                  # local dev Postgres: reverted, confirmed via \dt/pg_type that
                                  # both tables and all 7 enum types were fully gone and all 6
                                  # permissions were fully gone, reapplied, confirmed migration:show
                                  # [X] on both and \d/permission-grant output identical to the
                                  # pre-revert state. INV-3 (max one open/claimed case per target)
                                  # LIVE VERIFIED via a real duplicate-key rejection from the partial
                                  # unique index, not just asserted in a unit test. Docker Desktop
                                  # was not running at session start (found at a non-default install
                                  # path under %LOCALAPPDATA%, not C:\Program Files) -- launched
                                  # directly, same safe/reversible approach as PLACE-042; existing
                                  # dev stack auto-restarted healthy. docs/data/database.md SS11
                                  # updated: moderation_cases/reports added as Accepted, `reports`
                                  # struck from the Wave-1 "referenced but unapproved" list it had
                                  # carried since GAP-1. Full validation: backend 88 suites/911 tests
                                  # (up from 82/855), zero regression, 56 new tests across 6 new
                                  # suites; typecheck/lint/build all clean; full monorepo
                                  # build/typecheck/lint 4/4, 6/6, 6/6 green; git diff --check clean;
                                  # secret scan clean. ADR-018 itself was NOT modified (already
                                  # fully specifies M1; instruction was to implement exactly, not
                                  # redesign or annotate it). See
                                  # docs/delivery/reports/MODERATION-M1-SCHEMA-FOUNDATION-
                                  # 2026-08-02.md. M2 (Moderation Queue Read API) is the next
                                  # milestone -- explicitly NOT started per instruction.
                                  # ---- prior state (2026-08-02 Frontend Accessibility Baseline) ----
                                  # Owner-approved, per this same milestone's own
                                  # instruction citing roadmap item #8 ("Accessibility pass beyond
                                  # the current 8-file baseline"). Audit + trivial-remediation only,
                                  # explicitly not a redesign/new-flow milestone. Added
                                  # eslint-plugin-jsx-a11y@6.10.2 as an explicit apps/web
                                  # devDependency (was already an undeclared transitive dependency,
                                  # hoisted via eslint-config-next's own dependency on it -- confirmed
                                  # via package-lock.json/require.resolve; adding it explicitly
                                  # changed zero installed versions, 1-line lockfile diff). Extended
                                  # apps/web/eslint.config.mjs with the 28 jsx-a11y `recommended`
                                  # rules Next's own core-web-vitals preset does not already
                                  # configure (Next tunes 6 of the 34 rules itself, e.g. alt-text
                                  # scoped to img/Image only -- those were left untouched rather than
                                  # overwritten, to honor "preserve existing rules / do not weaken");
                                  # confirmed via eslint --print-config that all 34 resolve with no
                                  # plugin-redefinition conflict. Automated result: 0 findings across
                                  # 146 linted files -- verified genuine (not a silent misconfig) via
                                  # a throwaway file with 3 known violations that correctly triggered
                                  # click-events-have-key-events/no-static-element-interactions/
                                  # alt-text/anchor-is-valid, then deleted before commit. Manually
                                  # read every high-risk area the milestone named (nav/header,
                                  # login/register, all 4 browse filter components, review form +
                                  # image upload, pagination, map/explore, all 8 error/global-error
                                  # boundaries, dashboard, all 6 card components) -- found 3 trivial,
                                  # pattern-matched gaps (SearchMapExplorer's error text missing
                                  # role="alert", unlike every other error surface in the app;
                                  # RouteGuard's and login's Suspense-fallback loading states missing
                                  # aria-busy, unlike every route's own loading.tsx convention), fixed
                                  # all 3 with 4 new regression tests (2 new spec files,
                                  # SearchMapExplorer.spec.tsx + RouteGuard.spec.tsx). Found and
                                  # explicitly DEFERRED (not fixed, per this milestone's own
                                  # exclusion list) one real, confirmed gap: MapView's custom HTML
                                  # place markers (raw DOM div + onclick, not JSX -- jsx-a11y cannot
                                  # see it) have no keyboard access; verified via the installed
                                  # maplibre-gl bundle's own setAttribute calls that the library DOES
                                  # give its canvas built-in role="region"/aria-label="Map"/
                                  # tabindex="0"/arrow-key pan by default -- the gap is specific to
                                  # consumer-supplied custom marker elements, a known library
                                  # limitation, not a bug this codebase introduced. A second pattern
                                  # (async submit-button label changes with no live-region
                                  # announcement, recurring across login/register/dashboard/reviews)
                                  # was also deferred as a broader, judgment-laden change beyond a
                                  # trivial-fix bar. Explicitly NOT claimed as a WCAG conformance
                                  # audit -- no axe-core/Lighthouse/Pa11y, no color-contrast check, no
                                  # keyboard-only or screen-reader walkthrough was performed (all
                                  # recorded as future work, not done). Full validation: frontend 32
                                  # suites/193 tests (up from 30/189), run twice consecutively
                                  # identical, zero regression; typecheck/lint/build all clean; full
                                  # monorepo build/typecheck/lint 4/4, 6/6, 6/6 green; git diff
                                  # --check clean; secret scan clean. Roadmap item #8 updated from
                                  # open gap to "PARTIALLY ADDRESSED" (explicitly not marked DONE --
                                  # 2 real gaps remain open by design). See
                                  # docs/delivery/reports/ACCESSIBILITY-BASELINE-2026-08-02.md.
                                  # ---- prior state (2026-08-02 Frontend Bundle Size Baseline & Regression Reference) ----
                                  # (2026-08-02, Frontend Bundle Size Baseline & Regression
                                  # Reference -- ad-hoc, NOT PLACE-numbered) Owner-approved,
                                  # selected via the governance assessment appended to the Frontend
                                  # Component Coverage Extension milestone (ranked #1 -- the only
                                  # remaining candidate not entangled with the still-unresolved
                                  # Moderation Foundation / Owner-decision blockers). Measurement
                                  # and documentation ONLY, per explicit instruction -- no
                                  # production code changed, no dependency added/removed, no CI
                                  # gate, no enforced budget. Clean `next build` (rm -rf .next first)
                                  # measured directly via a new dependency-free Node script
                                  # (apps/web/scripts/measure-bundle-size.mjs, fs/path/zlib built-
                                  # ins only) reading the actual .next/static/chunks output --
                                  # Turbopack's Next 16 build no longer prints the old webpack-era
                                  # per-route "First Load JS" table, so route-level estimates are
                                  # instead derived from each route's own
                                  # page_client-reference-manifest.js (Next's own RSC module->chunk
                                  # map). Baseline: total client JS 1468.1 KB raw / 420.7 KB gzip /
                                  # 356.7 KB Brotli across 39 chunks; largest single chunk 768.8 KB
                                  # raw / 203.7 KB gzip, confirmed as MapLibre GL JS via THREE
                                  # independent checks (content strings incl. a real WebGL shader
                                  # attribute name and the RTL text plugin marker; cross-referenced
                                  # against every route's client-reference-manifest -- present only
                                  # on /map and /explore, the only 2 routes that render MapView;
                                  # and dependency inspection, exactly one client mapping library in
                                  # package.json, imported by exactly one component) -- not claimed
                                  # from filename/size alone. /map and /explore's route estimate
                                  # (~223-224 KB gzip) is ~12x every other sampled route (~16-20 KB
                                  # gzip each), entirely attributable to that one chunk. Total
                                  # matches PLACE-041's earlier informal "~1.5M" finding --
                                  # essentially unchanged since that audit. Zero client-shipped
                                  # source maps (192 found total, 169 unique + 23 standalone-output
                                  # duplicates, all server-side only). Two mid-size chunks (138.4 KB,
                                  # 110.0 KB raw) explicitly disclosed as unattributed rather than
                                  # guessed -- most likely Next.js/RSC runtime internals (present in
                                  # every route's rootMainFiles) but not confirmed via a content
                                  # marker. Recorded 4 ADVISORY-ONLY regression thresholds (not a
                                  # CI gate, not enforced, explicitly subject to Owner approval
                                  # before any enforcement is ever added) chosen from the measured
                                  # baseline: total JS gzip +10%, any route's JS gzip +15%, largest
                                  # chunk +10%, investigate any new chunk >=50 KB raw. Script wired
                                  # as `npm run measure-bundle` (plus `-- --json`) for
                                  # reproducibility -- deterministic, zero new dependency, passes
                                  # the same eslint/tsc scope as the rest of apps/web. Confirmed
                                  # zero dependency change (package-lock.json has no diff at all)
                                  # and next-env.d.ts unaffected (only `next build` was run, never
                                  # `next dev`). Full validation: monorepo build/typecheck/lint all
                                  # 4/4, 6/6, 6/6 green. Roadmap item #12 marked DONE
                                  # (measurement-only, explicitly not budget-enforced). See
                                  # docs/delivery/reports/FRONTEND-BUNDLE-SIZE-BASELINE-2026-08-02.md.
                                  # ---- prior state (2026-08-02 Frontend Component Coverage Extension) ----
                                  # Owner-approved, selected via the governance
                                  # assessment appended to the Error/Loading Boundary Completion
                                  # milestone (ranked #1 -- zero Owner decisions, zero risk, proven
                                  # pattern/tooling already installed). Extends the Frontend
                                  # Component Test Coverage Foundation (2026-08-01) to the 9
                                  # remaining browse-page card/filter components that milestone's
                                  # own report explicitly deferred: HotelCard/HotelFilters,
                                  # RestaurantCard/RestaurantFilters, TourCard/TourFilters,
                                  # BeachCard/BeachFilters, PlaceCard. Read every target component
                                  # in full before writing its test -- did not assume identical
                                  # props/behavior across cards (found real differences: HotelCard's
                                  # hotel_type label-map has a raw-string fallback for unknown
                                  # values, same pattern independently verified on TourCard's
                                  # tour_type via a different formatter function; BeachCard links to
                                  # /places/{slug} not /beaches/{slug} since beaches have no
                                  # satellite detail route; PlaceCard's distance_m is optional and
                                  # only present on /geo/nearby results; TourFilters has 6 filter
                                  # fields vs. 2-3 on the others). Confirmed by reading all 4 filter
                                  # components' source that their updateParam contract really is
                                  # byte-for-byte identical (preserves unrelated params, clears via
                                  # "Tất cả", always drops page on any change) before testing that
                                  # behavior per-component rather than assuming it. Shared
                                  # next/navigation mock deliberately NOT extracted into a helper
                                  # despite 4 files using the identical shape (met the "≥3 files"
                                  # threshold) -- Jest's jest.mock() hoisting only allows
                                  # same-file module-level bindings in the factory, so a real shared
                                  # helper would need a __mocks__/next/navigation.ts +
                                  # moduleNameMapper wiring, which is MORE indirection for 4 call
                                  # sites, not less; kept inline. 65 new tests. Full validation:
                                  # frontend 30 suites/189 tests (up from 21/124), run twice
                                  # consecutively with identical results, zero regression, zero
                                  # console/act warnings; typecheck/lint/build all clean; full
                                  # monorepo build/typecheck/lint 4/4, 6/6, 6/6 green. Roadmap items
                                  # #4 (Image Upload UI) and #6 (frontend test coverage) in
                                  # MVP-GAP-ANALYSIS-AND-ROADMAP-2026-07-25.md reconciled from stale
                                  # "Still open" to DONE (both had shipped, in intervening
                                  # sessions/this one, without ever being annotated). See
                                  # docs/delivery/reports/FRONTEND-COMPONENT-COVERAGE-EXTENSION-
                                  # 2026-08-02.md.
                                  # ---- prior state (2026-08-02 Error/Loading Boundary Completion) ----
                                  # Owner-approved, selected via the governance
                                  # assessment appended to the Media Orphan Cleanup post-
                                  # implementation review (ranked #1 -- zero Owner decisions, zero
                                  # risk, direct PLACE-041 precedent to copy). Added error.tsx +
                                  # loading.tsx to the 4 routes PLACE-041's own audit had left open
                                  # (/explore, /map, /search, /dashboard) -- same
                                  # placesStyles.state/.stateTitle/.btn pattern already shipped on
                                  # hotels/restaurants/tours/events, with route-specific adaptations
                                  # where the target page's real shape differed (explore/map
                                  # loading.tsx mirrors MapView's real height:70vh container to
                                  # minimize layout shift; search/loading.tsx mirrors its actual
                                  # resultList/resultItem shape, not a card grid; dashboard's
                                  # boundaries reflect its heading+greeting+button shape, no list).
                                  # No backend/schema/route-behavior change. Live-verified in a real
                                  # browser against the real dev stack: normal success path
                                  # unchanged on all 4 routes; error boundary confirmed on all 4 via
                                  # dev-only induced failure (stopped the live api server for
                                  # /search's real fetch-failure path; temporarily forced a render
                                  # throw in explore/map/dashboard's page.tsx, since those 3 have no
                                  # page-level network call to fail against -- every temporary throw
                                  # reverted immediately after, confirmed via a clean git diff
                                  # before staging); retry (reset()) confirmed both by a unit test
                                  # and by a live post-recovery reload; zero console errors from the
                                  # boundaries themselves on a fresh tab. One incidental environment
                                  # note disclosed: no nvm/fnm available this session, so validation
                                  # ran on the system Node v24.18.0/npm 11.16.0 rather than the
                                  # .nvmrc-pinned 20 (still satisfies package.json's >=20.0.0
                                  # engines range). Testing: thoroughly unit-tested ONE representative
                                  # pair (search/error.tsx + search/loading.tsx, 7 new tests -- /search
                                  # is the only one of the 4 with a real server-side data fetch, so
                                  # the widest real trigger surface); the other 3 pairs are
                                  # structurally byte-for-byte the same pattern, verified instead via
                                  # typecheck (Next's {error,reset} contract)/build/live verification,
                                  # not re-tested to avoid repetitive zero-value duplication. Full
                                  # validation: frontend 21 suites/124 tests (up from 19/117, zero
                                  # regression), typecheck/lint/build all clean, full monorepo build
                                  # 4/4 green. Roadmap item #10 (MVP-GAP-ANALYSIS-AND-ROADMAP-
                                  # 2026-07-25.md) marked DONE -- this was the last remaining item in
                                  # that list's original error/loading-boundary gap. See
                                  # docs/delivery/reports/ERROR-LOADING-BOUNDARIES-2026-08-02.md.
                                  # ---- prior state (2026-08-02 Media Orphan Cleanup post-implementation review) ----
                                  # Owner-requested focused review of
                                  # every file changed by the just-completed Media Orphan Cleanup
                                  # milestone, plus a fresh governance assessment -- no new feature.
                                  # Found and fixed ONE genuine defect: MediaRepository
                                  # .findOrphanCleanupCandidates() paginated with no cursor, so
                                  # progress depended entirely on rows being mutated between
                                  # batches -- broken for dry-run (nothing mutates, so every batch
                                  # re-fetched the same page) and for a real run hitting a non-fatal
                                  # storage error on one row (by design left untouched, but then
                                  # re-fetched every subsequent batch of the SAME run, potentially
                                  # starving every other candidate for the whole invocation). Fixed
                                  # with keyset pagination, cursor advanced after every batch
                                  # regardless of mutation. A SECOND bug surfaced while building
                                  # that fix, confirmed live against real Postgres: a naive
                                  # Date-based cursor silently truncates timestamptz's microsecond
                                  # precision to JS Date's millisecond precision, letting a row
                                  # satisfy created_at>cursor against its own real stored value and
                                  # get re-fetched as a duplicate instead of skipped -- reproduced
                                  # directly (two rows 100 microseconds apart, second one never
                                  # surfaced) before fixing by carrying Postgres's own exact
                                  # created_at::text rendering for the cursor, bound back as
                                  # ::timestamptz, never round-tripped through a Date. Also found
                                  # and fixed an incidental e2e test-hygiene leak (the original
                                  # dry-run test deleted a real MinIO object directly to verify it,
                                  # but never cleaned up the DB row -- leaking one orphan row per
                                  # e2e run); cleared the several rows this had accumulated in the
                                  # shared dev DB via a real npm run media:cleanup invocation
                                  # (confirmed 0 eligible rows remain). Every other reviewed
                                  # property (storage-before-DB ordering, non-not-found errors
                                  # leaving the row untouched, dry-run's structural inability to
                                  # write, audit context containing no secrets/signed URLs, Date
                                  # serialization, migration reversibility, the two pre-existing
                                  # BodyInit type-only casts) was confirmed already correct, no
                                  # change needed. New regression tests at both unit and live-e2e
                                  # level reproduce the original failures and pass after the fix.
                                  # Full re-validation: backend unit 82 suites/855 tests (up from
                                  # 850), e2e 13 suites/104 tests (up from 102), full monorepo
                                  # typecheck/lint/build all green, fresh dry-run against the live
                                  # local stack confirms 0 eligible rows remain. See
                                  # docs/delivery/reports/MEDIA-ORPHAN-CLEANUP-POST-IMPLEMENTATION-
                                  # REVIEW-2026-08-02.md.
                                  # ---- prior state (2026-08-02 Media Orphan Cleanup, initial implementation) ----
                                  # Owner
                                  # explicitly selected this candidate from a read-only governance
                                  # assessment (ranked against gallery/business-claim/moderation/
                                  # Transport-browse-page) and approved a full execution plan +
                                  # retention policy BEFORE any implementation. Backend-only job:
                                  # deletes orphaned `media` rows (status=pending, all 5 owner
                                  # columns NULL, created_at < now()-24h) -- storage object deleted
                                  # FIRST (new StorageService.deleteObjectForCleanup(), HEAD-then-
                                  # DELETE), THEN a conditional soft-delete UPDATE (full eligibility
                                  # predicate repeated in WHERE -- this IS the idempotency/
                                  # concurrency mechanism, no locking), THEN an audit_logs row
                                  # (isServiceAccount=true). No scheduler/queue added (repo has none
                                  # -- same "add later" precedent as InventoryHoldsRepository
                                  # .expireOverdueHolds()); manual runner only:
                                  # `npm run media:cleanup [-- --dry-run]`
                                  # (apps/api/src/scripts/clean-orphan-media.ts, first
                                  # NestFactory.createApplicationContext() use in this repo). Dry-
                                  # run reuses the exact same MediaCleanupService.run() loop, just
                                  # skips the storage/DB/audit writes. Bounded by batchSize=100/
                                  # maxBatches=50/maxExecutionMs=300000 (5 min) -- on time-budget
                                  # exceeded, finishes the row already in flight, never leaves one
                                  # half-processed. Optional partial index
                                  # (AddMediaOrphanCleanupIndex1720003100000, applied live).
                                  # TWO REAL DEFECTS found and fixed during required live
                                  # verification (not assumed correct from the plan alone): (1)
                                  # S3/MinIO's DeleteObject is itself idempotent and never errors on
                                  # a missing key (unlike HeadObject/GetObject) -- a naive "try
                                  # delete, catch NotFound" could never actually observe
                                  # `not_found`; fixed by making deleteObjectForCleanup() HEAD-first
                                  # THEN delete, confirmed live against real MinIO. (2)
                                  # AuditService.redact() walks Object.entries() on any object
                                  # value -- a raw JS Date has no own enumerable properties, so
                                  # passing candidate.createdAt straight into audit `context`
                                  # silently persisted as "{}"; caught by inspecting a real
                                  # audit_logs row after a live cleanup run, fixed via
                                  # `.toISOString()`, re-verified live. Also fixed one PRE-EXISTING,
                                  # unrelated defect blocking ALL e2e tests: apps/api's currently-
                                  # resolved @types/node no longer accepts a raw Buffer as fetch's
                                  # BodyInit (a real environment-drift regression, not caused by
                                  # this milestone) -- `media.e2e-spec.ts` was failing to even
                                  # compile before this session; fixed with a type-only `as BodyInit`
                                  # cast (zero runtime change) in both media.e2e-spec.ts and the new
                                  # media-orphan-cleanup.e2e-spec.ts. Full live verification against
                                  # real Postgres/MinIO via the actual `npm run media:cleanup`
                                  # command (not just e2e mocks): dry-run made zero DB/storage/audit
                                  # changes; real run deleted the live MinIO object, soft-deleted the
                                  # row, wrote exactly one correct audit row; a second consecutive
                                  # run was a clean no-op (0 scanned, audit count still 1); dry-run
                                  # after cleanup reported 0 eligible. Full regression: backend unit
                                  # 82 suites/850 tests (up from 80/824), e2e 13 suites/102 tests (up
                                  # from 12/95), full monorepo typecheck/lint/build all 6/6 green.
                                  # See docs/delivery/reports/MEDIA-ORPHAN-CLEANUP-2026-08-02.md.
                                  # ---- prior state (2026-08-01 Image Upload UI) ----
                                  # Previously
                                  # blocked (repeatedly, across 2 prior governance assessments) on
                                  # unmade Owner UX decisions. Re-evaluated after the Owner fixed
                                  # 11 scope decisions (single-file only; no drag-drop/crop/
                                  # gallery/progress-bar/delete/reorder/bulk/editing; exactly ONE
                                  # existing page with a clear backend owner; reuse existing UI
                                  # components). Investigation found the backend already had a
                                  # ready-made "clear backend owner" path never used by any
                                  # frontend: CreateReviewDto.media_ids + MediaRepository
                                  # .attachToReview() (Reviews milestone, 2026-07-26), accepting
                                  # orphan media the calling user uploaded. Integrated a single-
                                  # image upload into ReviewsSection.tsx (the ONLY existing
                                  # authenticated write-UI in the frontend) using this exact path
                                  # -- not the place_id-owned presign path. New: lib/sha256.ts
                                  # (client-side checksum, Web Crypto), modules/media/{types,
                                  # api/media.api,useSingleImageUpload}.ts. Mandatory Phase 0
                                  # before any UI code: live browser CORS verification (real
                                  # browser via the Browser pane's javascript_tool, not Node
                                  # fetch) of presign->PUT(MinIO)->register -- passed with zero
                                  # CORS block (OPTIONS preflight 204, PUT 200, register's server-
                                  # side checksum re-verification confirmed the object genuinely
                                  # persisted) -- no MinIO bucket CORS config was needed. Full live
                                  # verification via the actual rendered UI (not just the Phase 0
                                  # script): submitted a real review with an attached photo against
                                  # a live place, then confirmed via a direct DB query that
                                  # reviews.id JOINs media.review_id -- proof attachToReview() ran
                                  # for real, not just that the API returned 201. 14 new tests
                                  # (7 hook + 7 component, first test file for the reviews module),
                                  # zero regression: frontend 19 suites/117 tests (up from 17/103),
                                  # run twice consecutively with identical results and no hidden
                                  # console/act warnings; backend 80 suites/824 tests and e2e 12
                                  # suites/95 tests both unchanged (no backend file touched); full
                                  # monorepo build clean. Scope discipline confirmed: no drag-drop/
                                  # gallery/delete/reorder/real-progress-bar/crop/multi-file/
                                  # moderation/image-display-outside-reviews was added. See
                                  # docs/delivery/reports/IMAGE-UPLOAD-UI-2026-08-01.md and
                                  # docs/data/modules/media.md §11.
                                  # ---- prior state (2026-08-01 Frontend Component Test Coverage Foundation) ----
                                  # ad-hoc, NOT PLACE-numbered, per the same standing 2026-07-26
                                  # Owner instruction) Selected via a read-only governance
                                  # assessment following the Transport doc reconciliation --
                                  # compared against Image Upload UI again and picked this because
                                  # it needed zero product/design decision (no Owner UX call to
                                  # make) while Image Upload UI still does. Established the FIRST
                                  # jsdom/React Testing Library component-test pattern in
                                  # apps/web: added @testing-library/react, @testing-library/
                                  # jest-dom, jest-environment-jsdom as devDependencies (no
                                  # @testing-library/user-event -- every target component only
                                  # uses plain <select onChange>, so RTL's own fireEvent.change is
                                  # a fully faithful simulation; adding it would be unused
                                  # tooling). apps/web/jest.config.js: testMatch extended to also
                                  # match *.spec.tsx; existing 13 *.spec.ts suites deliberately
                                  # left running under the global testEnvironment: 'node'
                                  # unchanged -- new component tests opt into jsdom per-file via
                                  # Jest's own `/** @jest-environment jsdom */` docblock pragma,
                                  # so nothing about the 13 pre-existing suites changed; ts-jest's
                                  # globals.tsconfig gained jsx: 'react-jsx' (base tsconfig.json
                                  # has jsx: 'preserve', which does not work for ts-jest, which
                                  # needs an actual transform); new jest.cssModuleMock.js (a bare
                                  # Proxy returning the accessed property name) wired via
                                  # moduleNameMapper for '*.module.css' imports -- avoids adding
                                  # the identity-obj-proxy package for a one-line need; new
                                  # jest.setup.ts imports '@testing-library/jest-dom' via
                                  # setupFilesAfterEnv. Confirmed empirically (not assumed) that
                                  # next/link's <Link> renders under jsdom+RTL with no router
                                  # context and needs no mock; next/navigation's useRouter/
                                  # useSearchParams mocked per-test-file via jest.mock (no shared
                                  # reusable mock module needed for only 2 consumers so far).
                                  # 4 new test files, 26 new tests: AttractionCard.spec.tsx (9,
                                  # the representative presentational card -- plain <img>, next/
                                  # link, conditional metadata rendering), components/ui/
                                  # Pagination.spec.tsx (7, the shared cross-module pagination
                                  # component), modules/search/SearchFilters.spec.tsx (5) and
                                  # modules/attractions/AttractionFilters.spec.tsx (5, the
                                  # representative browse filter component -- chosen over
                                  # HotelFilters because it has 3 filter fields/ward+price_range
                                  # vs HotelFilters' 2, confirmed by reading both source files).
                                  # Full validation: apps/web 17 suites/103 tests (up from 13/77),
                                  # zero regression in the 13 pre-existing suites; full monorepo
                                  # typecheck/lint/build clean across all 5 packages; backend 80
                                  # suites/824 tests unaffected (no backend file touched). 4
                                  # pre-existing high-severity npm audit findings (brace-expansion,
                                  # postcss, sharp -- all transitive via next/eslint/jest, not via
                                  # the 3 new packages) noted, not fixed -- out of scope, and the
                                  # only available fix downgrades next to a breaking major version.
                                  # See docs/delivery/reports/FRONTEND-COMPONENT-TEST-COVERAGE-
                                  # 2026-08-01.md. No product behavior/backend/schema change; no
                                  # Image Upload UI started, per instruction.
                                  # ---- prior state (2026-08-01 Transport Browse Filters) ----
                                  # Selected via a read-only governance assessment
                                  # that deliberately did NOT default to the frontend Media Upload
                                  # UI -- compared it against Transport Browse Filters and frontend
                                  # component tests, and picked this one because every design
                                  # decision was already written down in
                                  # docs/data/modules/transport.md Section 8 (authored when
                                  # Transport shipped, self-flagged in ListTransportsQueryDto's own
                                  # code comment as "the next Transport Browse task"), needed zero
                                  # migration (every backing column/index already existed from
                                  # InitTransport), and stayed inside ADR-017's own Accepted scope
                                  # (schema + minimal read -- explicitly NOT the public browse
                                  # page, which remains a separate, larger, not-yet-authorized
                                  # decision). Backend only: GET /transports gained 5 optional
                                  # filters (transport_type/ward/pricing_model/booking_required/
                                  # airport_transfer), mirroring the Search Filters milestone's
                                  # exact parameterized-WHERE pattern. No schema/migration/new
                                  # endpoint. Two pre-existing tests had encoded the OLD "these 5
                                  # fields are rejected" behavior (transports.dto.spec.ts,
                                  # wave2.e2e-spec.ts) and were updated to match the new, intended
                                  # behavior -- not silently left failing. Full validation: 80
                                  # suites/824 unit tests (up from 800), 12 e2e suites/95 tests (up
                                  # from 91, all live against real Postgres/PostGIS/Redis),
                                  # monorepo build clean. See
                                  # docs/delivery/reports/TRANSPORT-BROWSE-FILTERS-2026-08-01.md.
                                  # ---- prior state (2026-07-30 Media Upload Foundation) ----
                                  # Design-review-approved backend milestone: new
                                  # apps/api/src/core/storage/ (S3-compatible StorageService --
                                  # MinIO dev/AWS S3/Cloudflare R2, all one client) +
                                  # apps/api/src/modules/media/ write path (POST /media/presign,
                                  # POST /media). Three approved design changes: (1) persist ONLY
                                  # object metadata (object_key/bucket/content_type/size_bytes/
                                  # checksum_sha256) -- media.url made nullable, NEVER a stored
                                  # absolute/signed URL, generated dynamically at read time (not
                                  # yet needed in practice since every new upload stays `pending`
                                  # and the existing published-only gallery read path never
                                  # touches these rows); (2) bucket isolation --
                                  # phuquochub-dev/phuquochub-test defaults, S3_BUCKET the only
                                  # production override, Jest's own default NODE_ENV=test already
                                  # isolates e2e for free; (3) verifyUploadedObject()
                                  # HEAD-first/GET-stream-fallback SHA-256 verification, provider-
                                  # agnostic, never buffers the full object, deletes the object on
                                  # any mismatch. Presign-session integrity via Redis (existing
                                  # infra, TokenService's refresh-token-record pattern reused, no
                                  # new DB table) closes the gap the original plan had flagged
                                  # unresolved (server could not previously prove the confirming
                                  # user was the same one who requested the presign). Migration
                                  # AddMediaUploadFoundation relaxed media's exclusive-owner CHECK
                                  # from exactly-one to at-most-one -- MediaRepository
                                  # .attachToReview() had ALREADY assumed a fully-orphaned (0-owner)
                                  # row was reachable, but the original CHECK made that impossible
                                  # to ever INSERT; fixed at the root, not worked around. down()
                                  # refuses (throws, does not destroy data) if any object_key/
                                  # null-url/orphan row exists that would make reverting unsafe --
                                  # verified via a real revert->reapply drill on the live dev DB
                                  # while the table was empty. Full validation: 80 suites/800 unit
                                  # tests (up from 74/724), 12 e2e suites/91 tests (up from 11/81,
                                  # includes a new 10-test live-MinIO-round-trip spec proving real
                                  # presign->PUT->register->verify->duplicate-detect), full
                                  # monorepo build clean (build-determinism fix from the Search
                                  # Filters review re-confirmed still holding). See
                                  # docs/delivery/reports/MEDIA-UPLOAD-FOUNDATION-2026-07-30.md.
                                  # ---- prior state (2026-07-30 governance reconciliation) ----
                                  # (2026-07-30, GOVERNANCE RECONCILIATION -- this comment block
                                  # and next_action.objective below had fallen behind 4 completed
                                  # ad-hoc milestones; this entry catches both up. All ad-hoc, NOT
                                  # PLACE-numbered, per the same standing 2026-07-26 Owner
                                  # instruction cited throughout this history.)
                                  # 1. Booking Application Layer (Phase 2), 2026-07-30: query
                                  # (GET /bookings admin/staff), status transitions (confirm/
                                  # cancel/markExpired via a pure booking-status.transition.ts FSM),
                                  # domain-event abstraction (BOOKING_EVENT_PUBLISHER DI token +
                                  # logging-only default impl), audit (AuditService, ADR-016), new
                                  # Booking.{List,Confirm,Cancel,MarkExpired} permissions
                                  # (AddBookingManagePermissions migration). Zero change to the
                                  # Booking Foundation schema/entities. See
                                  # docs/delivery/reports/MVP-BOOKING-APPLICATION-LAYER-2026-07-30.md.
                                  # 2. Availability & Inventory Foundation, 2026-07-30: new
                                  # apps/api/src/modules/availability/ (polymorphic
                                  # availability_slots + inventory_holds, ADR-003 pattern, reused
                                  # -- see ADR-003 update below), InitAvailability +
                                  # SeedAvailabilityPermissions migrations, GET/POST
                                  # /availability-slots. Additive-only Booking integration
                                  # (optional availability_slot_id/hold_ttl_minutes on
                                  # CreateBookingRequestDto; hold placed atomically inside the
                                  # existing booking-creation transaction). Over-allocation
                                  # prevented via SELECT...FOR UPDATE + SUM(quantity); expired
                                  # holds rejected lazily at confirm time (no scheduler exists in
                                  # this repo). All migrations verified applied live
                                  # (migration:show, all [X]). See
                                  # docs/delivery/reports/AVAILABILITY-AND-INVENTORY-FOUNDATION-2026-07-30.md.
                                  # 3. Search Filters, 2026-07-30: GET /search gained optional
                                  # category/ward/price_range (same columns/parameterized pattern
                                  # as ListPlacesQueryDto), /search rewritten to a URL-driven
                                  # Server Component matching hotels/restaurants/tours/attractions/
                                  # beaches, new categories.api.ts frontend module (GET /categories
                                  # had zero frontend consumers before this). No schema change. A
                                  # dedicated post-implementation review then found and fixed 3
                                  # real defects (a wasted API call, an OpenAPI schema-ref
                                  # inconsistency, an under-strict e2e assertion) plus a genuine,
                                  # deterministic, pre-existing build defect (nest-cli.json's
                                  # deleteOutDir conflicting with tsc's incremental cache, causing
                                  # any SECOND consecutive `nest build` to silently produce an
                                  # empty dist/ -- fixed via tsconfig.build.json's
                                  # incremental:false, reproduced and re-verified 3x). See
                                  # docs/delivery/reports/SEARCH-FILTERS-2026-07-30.md and
                                  # docs/delivery/reports/SEARCH-FILTERS-POST-IMPLEMENTATION-REVIEW-2026-07-30.md.
                                  # 4. Release Readiness Audit, 2026-07-30: full re-validation
                                  # (lint/typecheck/unit/e2e/build, both workspaces) plus targeted
                                  # re-verification that the build-determinism fix holds (3
                                  # consecutive nest builds, forced + cache-hit turbo runs). Zero
                                  # release blockers found; zero source touched. Flagged this
                                  # state.yaml file and the 2026-07-25 roadmap report as stale --
                                  # resolved by this same governance-reconciliation entry. See
                                  # docs/delivery/reports/RELEASE-READINESS-AUDIT-2026-07-30.md.
                                  # ---- prior state (2026-07-29 Booking Foundation) ----
                                  # (2026-07-29, ad-hoc, NOT PLACE-numbered -- Owner instruction
                                  # on record since 2026-07-26 to prioritize product completion
                                  # over new governance tasks) Audit-before-implement session for
                                  # the Booking domain (Owner supplied a Bookings.xlsx data-design
                                  # reference in 02_Business_Data, external to this repo). Phase 1
                                  # read-only assessment found ZERO pre-existing booking/
                                  # reservation/payment/refund/invoice/voucher/inventory-hold/
                                  # commission/settlement code anywhere, and no Owner decision
                                  # blocking a minimal slice -- every non-trivial design choice
                                  # (polymorphic entity_type/entity_id, place_id as the real FK
                                  # anchor, no dependency on unmigrated business_claims/members)
                                  # directly copies an existing Accepted pattern (ADR-003's
                                  # price_history precedent; ADR-017's treatment of Business
                                  # ownership as non-blocking). Implemented "Booking Request
                                  # Foundation": Booking + BookingItem entities, 3 separate status
                                  # enums (booking/payment/fulfillment), InitBooking migration,
                                  # SeedBookingPermissions, POST /bookings (auth + throttled) and
                                  # GET /bookings/:code (auth + ownership-checked, not public --
                                  # deliberate judgment call, no direct precedent existed). No
                                  # payment/inventory/discount/commission/refund/invoice/
                                  # settlement engine built, per the mission's own exclusion list.
                                  # Full validation: apps/api tsc/eslint/jest all exit 0 (62 suites/
                                  # 558 tests, zero regression); root turbo typecheck/lint/build all
                                  # green (web still 17/17 routes, untouched). Migration written and
                                  # structurally tested (mocked QueryRunner) but NOT run against a
                                  # live database -- Docker daemon unreachable this session, same
                                  # constraint as PLACE-039/MVP-REVIEWS-FEATURE-2026-07-26. No e2e
                                  # test added (needs live DB). See
                                  # docs/delivery/reports/MVP-BOOKING-FOUNDATION-2026-07-29.md.
                                  # LIVE VERIFICATION (2026-07-29, later same day, Docker Desktop
                                  # restarted by Owner): both migrations applied to the live local
                                  # dev Postgres, schema verified column-by-column, permissions
                                  # confirmed granted to `member`, full revert->verify->reapply
                                  # drill completed (booking_items/bookings/3 enum types cleanly
                                  # removed then restored, places=49/users=98 unaffected). New
                                  # apps/api/test/bookings.e2e-spec.ts (14 tests) -- found and fixed
                                  # 2 real bugs in the NEW TEST (not the endpoint): beforeAll needed
                                  # a longer Jest hook timeout (heavier real setup than other e2e
                                  # files), and one test tripped the endpoint's own 10/min throttle
                                  # by making an 11th POST in-suite (fixed by reusing one shared
                                  # booking instead of creating a fresh one). Full e2e suite: 11
                                  # suites/77 tests green (zero regression in the 10 pre-existing
                                  # files). Regression under Node v20.20.2: unit 63 suites/581
                                  # tests, typecheck/lint/build all green. Both previously-open
                                  # verification gaps (migration-vs-live-DB, e2e) are now closed.
                                  # See report Sec 6 (current).
                                  # ---- prior state (same-day refinement pass) ----
                                  # Same-day refinement pass (report Sec 5b): full domain-review
                                  # checklist run against the implementation; fixed 6 real gaps --
                                  # missing booking_status/service_start_at indexes (amended
                                  # InitBooking directly, never executed against a live DB so safe
                                  # to amend pre-first-run), no cross-field service_end_at-after-
                                  # service_start_at validation (added, first custom class-validator
                                  # decorator in this repo), no bookingCode format validation on the
                                  # read endpoint (added, BadRequest before any DB call), no
                                  # rate-limit on GET /bookings/:bookingCode (added, 30/min -- only
                                  # POST had one before), no input trim on guest_note/item.label
                                  # (added, first trim convention in this repo), and an undocumented
                                  # pricing trust boundary (unit_price is client-submitted/
                                  # unconfirmed -- documented at DTO/entity/mapper/domain-doc level,
                                  # no new pricing engine built). Added docs/data/modules/booking.md
                                  # and Bookings paths/schemas in docs/api/openapi.yaml (previously
                                  # undocumented). Re-certified full validation under Node v20.20.2
                                  # (repo .nvmrc pin, not just the environment's default v24.18.0) --
                                  # 63 suites/581 tests, typecheck/lint/build all green both times.
                                  # Docker still unreachable -- migration-vs-live-DB and e2e remain
                                  # not run, same disclosed limitation.
                                  # ---- prior state (2026-07-26 Reviews feature) ----
                                  # (2026-07-26, ad-hoc, NOT PLACE-numbered per explicit Owner
                                  # instruction -- "stop creating new governance tasks, priority
                                  # is PRODUCT COMPLETION, do not create PLACE-044") Re-verified
                                  # the 2026-07-25 gap analysis fresh against the filesystem (still
                                  # true: reviews/community/contributions/notifications were
                                  # .gitkeep-only on both BE and FE), then selected + fully
                                  # implemented the Reviews feature (roadmap Critical #2): backend
                                  # migration+module (reviews table, Review.Create permission,
                                  # rating aggregate recompute) and frontend (POST-capable http
                                  # helper, ReviewsSection wired into the place detail page). See
                                  # docs/delivery/reports/MVP-REVIEWS-FEATURE-2026-07-26.md.
                                  # ---- prior state (2026-07-25 SEO task) ----
                                  # (2026-07-25, ad-hoc, NOT a PLACE-numbered task per explicit
                                  # Owner instruction -- "khong tao PLACE-044, khong tiep tuc
                                  # chuoi infrastructure, chuyen trong tam tro lai hoan thien
                                  # PhuQuocHub MVP") MVP gap analysis and roadmap, plus one
                                  # selected+executed local-only task: SEO & Discoverability
                                  # completeness. See docs/delivery/reports/MVP-GAP-ANALYSIS-AND-
                                  # ROADMAP-2026-07-25.md (standalone report, same convention as
                                  # PRODUCTION-READINESS-ASSESSMENT-2026-07-24.md -- no new task
                                  # file/evidence-index scheme invented). Gap analysis found the
                                  # single biggest MVP gap: the web frontend has ZERO write
                                  # capability beyond auth (login/register/logout) -- grepped, no
                                  # POST/PATCH call exists outside auth.api.ts anywhere in
                                  # apps/web; reviews/community/contributions/notifications are
                                  # all empty .gitkeep-only stubs on the BACKEND too, despite
                                  # "Reddit" being one of this project's three named pillars. Also
                                  # found: no list/browse page exists for hotels/restaurants/tours
                                  # (detail-only); zero frontend component tests exist (only 3
                                  # unit files for lib/auth); /search's UI does not surface
                                  # category/price/ward filters despite backend support. Selected
                                  # and executed the highest-value fully-local task: SEO &
                                  # Discoverability. Found and fixed a real, previously-
                                  # undiscovered gap: no NEXT_PUBLIC_SITE_URL env var existed
                                  # anywhere, so Next.js's metadataBase/canonical-URL/OG-image
                                  # resolution silently fell back to http://localhost:3000 even
                                  # in a production build -- fixed via the exact same
                                  # ARG/ENV/build.args pattern PLACE-038 already established for
                                  # NEXT_PUBLIC_API_URL/MAP_TILE_URL. Added metadataBase to
                                  # layout.tsx; new apps/web/src/app/sitemap.ts (Next-native,
                                  # force-dynamic, fetches all 5 entity types' slugs, verified
                                  # NEVER attempted at next-build time -- classified ƒ dynamic in
                                  # the build output, not ○ static, so no live-API build
                                  # dependency was introduced); new apps/web/src/app/robots.ts
                                  # (allow-all, disallows login/register/dashboard, references
                                  # the sitemap); new apps/web/src/lib/structured-data.ts
                                  # (schema.org JSON-LD builders for TouristAttraction/
                                  # LodgingBusiness/Restaurant/TouristTrip/Event, wired into all 5
                                  # detail pages -- the FIRST dangerouslySetInnerHTML use anywhere
                                  # in this app, PLACE-041 had confirmed zero prior uses; a
                                  # serializeJsonLd helper escapes `<` in the JSON string first,
                                  # because these entities carry community-contributed name/
                                  # description text that could otherwise contain </script> and
                                  # break out of the inline script tag -- a real XSS-adjacent risk
                                  # in the standard inline-JSON-LD pattern, caught and closed, not
                                  # present before because no JSON-LD existed to carry it). Added
                                  # listHotelSlugs/listRestaurantSlugs/listTourSlugs (sitemap-only,
                                  # deliberately not wired into any UI -- no list page exists for
                                  # those types). Full validation: apps/web tsc/eslint both exit 0,
                                  # unit 17/17 unchanged, a full next build succeeded with ZERO
                                  # error (13/13 routes, /robots.txt correctly static, /sitemap.xml
                                  # correctly dynamic), docker compose config --quiet exit 0. Zero
                                  # backend code touched. Does NOT claim the MVP is feature-
                                  # complete -- the Critical/High roadmap items (write capability,
                                  # reviews, hotel/restaurant/tour list pages, image upload,
                                  # search filters, frontend test coverage) remain fully open. No
                                  # PLACE-044 created.
                                  # ---- prior state (PLACE-043, completed 2026-07-25) ----
                                  # PLACE-043 (2026-07-25, Hostinger VPS provisioning
                                  # verification and production access readiness) is now
                                  # COMPLETED with outcome "COMPLETED -- VPS NOT PURCHASED;
                                  # PRODUCTION DEPLOYMENT DEFERRED" -- reopened same-day, same
                                  # task (NOT PLACE-044), per explicit Owner authorization, to
                                  # record facts the Owner supplied directly: Hostinger Web
                                  # Hosting CONFIRMED active (50GB NVMe/daily backup/CDN/Node.js);
                                  # Hostinger KVM VPS CONFIRMED NOT PURCHASED; DNS CONFIRMED
                                  # managed at Hostinger (nameservers atlas.dns-parking.com/
                                  # hyperion.dns-parking.com; apex A -> 2.57.91.91, EXPLICITLY NOT
                                  # a VPS IP -- it is Hostinger's current Web-Hosting/DNS-parking
                                  # target; www CNAME -> phuquochub.com). Added an evidence-based
                                  # Web Hosting Suitability Assessment (report Sec 5): grounded in
                                  # this repository's actual docker-compose.prod.yml/data-source.ts/
                                  # redis.service.ts/Caddyfile requirements (6-service Docker
                                  # Compose topology, direct Postgres+PostGIS/Redis connectivity,
                                  # Caddy needing to own ports 80/443) -- concludes NOT confirmed
                                  # suitable for the current multi-service topology without
                                  # material redesign; explicitly did NOT recommend or attempt any
                                  # such redesign. Production deployment using the current
                                  # Docker/PostgreSQL/PostGIS/Redis topology remains BLOCKED UNTIL
                                  # VPS OR EQUIVALENT INFRASTRUCTURE IS PURCHASED -- a real,
                                  # unavoidable prerequisite, not a verification gap. All prior
                                  # repository-side deliverables (existing-assumption inventory,
                                  # SSH-readiness baseline, 3-tier resource-suitability assessment,
                                  # 24-stage HOSTINGER-VPS-BOOTSTRAP-RUNBOOK.md, PRODUCTION-ACCESS-
                                  # AND-SECRET-BOUNDARIES.md) remain unchanged and ready for use
                                  # once a VPS is purchased. Recorded Owner action: do NOT purchase
                                  # a VPS immediately; continue local development and Docker
                                  # production-like validation; purchase when go-live readiness is
                                  # reached; re-check real Hostinger KVM pricing against the
                                  # Recommended tier and budget before purchase; reopen the
                                  # bootstrap runbook after purchase. No urgent purchase
                                  # requirement created. Never claims "VPS ready"/"SSH ready"/
                                  # "production ready"/"deployment complete"; never classifies
                                  # 2.57.91.91 as a VPS address. Zero application code, DNS
                                  # change, or real secret touched. See
                                  # docs/delivery/tasks/PLACE-043.yaml.
                                  # ---- prior state (PLACE-042, completed 2026-07-25) ----
                                  # PLACE-042 completed 2026-07-25 (database migration rollback
                                  # rehearsal and recovery verification). Owner explicit written
                                  # authorization. Re-verified repository truth first (HEAD
                                  # 8d6c5c7, clean tree, PLACE-037..041 all completed,
                                  # current.task: none). Docker engine -- unreachable across the
                                  # PLACE-039/040/041 sessions -- was diagnosed (docker context ls
                                  # confirmed the correct "desktop-linux" context was already
                                  # active, ruling out a misconfiguration) and RESTORED by
                                  # launching the already-installed Docker Desktop application
                                  # directly (safe, reversible, no reinstall, no OS config change)
                                  # -- daemon reachable within 5s, pre-existing dev stack
                                  # (phuquoc-postgres/-redis/-minio) auto-restarted healthy. This
                                  # unblocked a REAL, live rehearsal. Selected
                                  # AddPlacesStatusPartialIndex1720001900000 (current head,
                                  # PLACE-003) -- its down() only drops the one index its own up()
                                  # creates, zero dependency on any other migration, zero data
                                  # mutation -- the safest available candidate since all 20
                                  # migrations in this repository are additive. Inserted 2 marked
                                  # test rows (place-042-rehearsal-active/-deleted) into the LOCAL
                                  # DEV database (not production, not a disposable stack --
                                  # justified because this migration's up()/down() never touch row
                                  # data). Ran the OFFICIAL migration:revert command (exit 0):
                                  # verified via direct SQL that idx_places_status_active was
                                  # dropped, migrations table 20->19, head moved back one step,
                                  # BOTH marker rows unchanged. Ran the OFFICIAL migration:run
                                  # command (exit 0): verified the index was recreated
                                  # byte-identically, migrations table back to 20, head restored
                                  # to AddPlacesStatusPartialIndex1720001900000, zero duplicate
                                  # migration name or index, migration:show reporting all 20 [X]
                                  # with zero pending. A safe failure-mode test (an intentionally
                                  # nonexistent local DB_NAME) confirmed migration:revert fails
                                  # loudly (exit 1) at the connection stage -- the real database
                                  # was independently confirmed completely unaffected afterward.
                                  # New scripts/migration-rollback-rehearsal.sh (safety-guarded:
                                  # refuses under NODE_ENV=production or a non-localhost DB_HOST,
                                  # both guards actually tested and confirmed refusing) wraps this
                                  # exact sequence and was itself run live end-to-end, exit 0. New
                                  # docs/delivery/DATABASE-ROLLBACK-RECOVERY-RUNBOOK.md answers
                                  # every required recovery question (revert fails / re-apply
                                  # fails / app-schema incompatible / when to stop / when to
                                  # restore backup / how to confirm head / evidence retention /
                                  # decision authority). Backup/restore procedure reviewed but NOT
                                  # operationally re-rehearsed in this task (already actually
                                  # executed for real in PLACE-038; not repeated). Both marker rows
                                  # deleted afterward; dev database confirmed back at its exact
                                  # original 49-place baseline. Does NOT prove a real production
                                  # rollback, nor a destructive (non-additive) migration's down()
                                  # -- none exists in this repository yet; the runbook requires
                                  # this exact procedure be repeated against the first one
                                  # authored, before it is ever deployed for real. Zero production
                                  # deployment, zero Hostinger/DNS/production-credential action,
                                  # zero new product feature. See docs/delivery/tasks/PLACE-042.yaml.
                                  # ---- prior state (PLACE-041, completed 2026-07-25) ----
                                  # PLACE-041 completed 2026-07-25 (enterprise production audit:
                                  # repository health, backend, frontend, infra, ops, security,
                                  # performance). Re-verified active authority first (HEAD
                                  # 5ca81cd, clean tree, PLACE-037/038/039/040 all completed). Full
                                  # 10-section audit per Owner instruction, explicitly no new
                                  # features/refactors/tech debt. Found and FIXED a real
                                  # correctness bug: hotels/restaurants/tours/events [slug] pages
                                  # blanket-caught EVERY error (incl. 5xx/network) into a 404
                                  # response, unlike places/[slug] (PLACE-035/036), which correctly
                                  # distinguishes 404 from other errors -- masking real outages as
                                  # "not found" (misleading + wrong for SEO de-indexing risk).
                                  # Fixed identically in all 4 files using the exact
                                  # already-proven places pattern. Also fixed 3 further gaps, each
                                  # mirroring an existing proven pattern: added error.tsx/
                                  # loading.tsx to those same 4 routes (previously had ZERO
                                  # boundary, confirmed via repo-wide find) + a new root
                                  # global-error.tsx; added apps/api security response headers
                                  # (X-Content-Type-Options/X-Frame-Options/Referrer-Policy) via a
                                  # new security-headers.middleware.ts mirroring correlation-id.
                                  # middleware.ts's exact file+spec structure (NOT the helmet
                                  # package -- avoided a new dependency for a 3-header need);
                                  # wrote docs/delivery/INCIDENT-RESPONSE-RUNBOOK.md (was
                                  # completely absent). apps/api unit suite 256/256 (was 254/34
                                  # suites -> 256/35), apps/web tsc/eslint/jest(17/17)/next-build
                                  # (11/11 routes) all clean. Full 8-dimension score recorded
                                  # (Architecture 88, Backend 90, Frontend 74, Infrastructure 93,
                                  # Operations 87, Security 86, Performance 83, Documentation 95,
                                  # Testing 79); repository-controlled production readiness held
                                  # at ~92% (PLACE-040 methodology unchanged). NOT concluded
                                  # "production ready" -- Critical/High items remain: Hostinger
                                  # VPS/DNS/R2/uptime-account (Owner-side) + migration:revert
                                  # rehearsal (Docker engine unreachable this session, same as
                                  # PLACE-039/040). Prioritized action list recorded for
                                  # everything found but NOT executed (frontend component tests,
                                  # remaining segments' error/loading boundaries, live Swagger UI,
                                  # sitemap/robots.txt, accessibility audit, HSTS-at-Caddy, MinIO
                                  # tag pinning, hotels/restaurants/tours list pages -- the last
                                  # explicitly a new feature, not built). See
                                  # docs/delivery/tasks/PLACE-041.yaml.
                                  # ---- prior state (PLACE-040, completed 2026-07-25) ----
                                  # PLACE-040 completed 2026-07-25 (production configuration
                                  # inventory, gap analysis, and local-only readiness closure).
                                  # Re-verified active authority first (HEAD 15dac59, clean tree,
                                  # current.task: none, PLACE-037/038/039 all completed). Full
                                  # 20-category inventory (env vars, Docker images/Compose,
                                  # reverse proxy, ports, health endpoints, Redis, PostgreSQL,
                                  # MinIO, upload storage, JWT, cookies, CORS, rate limiting,
                                  # logging, backup, restore, email, monitoring, build artifacts,
                                  # static assets) each given a READY/PARTIAL/MISSING/BLOCKED
                                  # verdict with cited evidence. Found and FIXED two real,
                                  # previously-undiscovered gaps: (1) REDIS_URL had no
                                  # production-required Joi rule (unlike DB_HOST/
                                  # CORS_ALLOWED_ORIGINS) -- RedisService reads only redis.url, so
                                  # a misconfigured deploy would have silently connected to the
                                  # unauthenticated redis://localhost:6379 dev default instead of
                                  # failing fast; fixed to mirror the DB_HOST pattern exactly, 3
                                  # new tests added, full apps/api suite 254/254 (was 251/251),
                                  # tsc/eslint/nest-build all clean. (2) scripts/deploy.sh Step 11
                                  # pointed at a "scripts/smoke-routes.sh" that was never actually
                                  # committed (an inert no-op); fixed by writing the real
                                  # scripts/smoke-test.sh (health/route/optional-real-data checks
                                  # against Caddy's local :8080 address) and wiring it in with a
                                  # rollback instruction on failure. New
                                  # docs/delivery/RELEASE-AND-ROLLBACK-CHECKLIST.md (operator
                                  # checkbox checklist: pre-release/deploy/smoke-test/rollback-
                                  # triggers/rollback/restore/backup/startup/shutdown, each step
                                  # naming the exact script). Repository-controlled production
                                  # readiness scored ~92% (16 READY + 1 PARTIAL(monitoring) out of
                                  # 18 scored items; migration:revert still BLOCKED this session --
                                  # Docker engine unreachable, same as PLACE-039's session; real
                                  # VPS/DNS/R2/uptime-account items are pure Owner-side and
                                  # excluded from scoring, not fabricated). Zero
                                  # docker-compose.prod.yml/Dockerfile/Caddyfile change --
                                  # `docker compose config --quiet` confirms these were already
                                  # correct (PLACE-038). See docs/delivery/tasks/PLACE-040.yaml.
                                  # ---- prior state (PLACE-039, completed 2026-07-25) ----
                                  # PLACE-039 completed 2026-07-25 (pre-deployment checklist:
                                  # Hostinger VPS preflight + secrets/environment go-live
                                  # inventory). Re-verified repository truth first: PLACE-037 and
                                  # PLACE-038 were ALREADY completed at task start (HEAD 13539a1);
                                  # the Owner's 5 restated decisions this session (Hostinger KVM
                                  # VPS pending real-account verification, ~1-2M VND/month budget,
                                  # phuquochub.com/https://phuquochub.com/api, email notifications,
                                  # no staging for release 1 as an explicit MVP decision) were
                                  # found BYTE-FOR-BYTE IDENTICAL to what PLACE-037/038 already
                                  # recorded -- nothing to reopen or re-close. New deliverable:
                                  # docs/delivery/PRE-DEPLOYMENT-CHECKLIST.md -- an actionable
                                  # Hostinger hPanel preflight (8 non-sensitive facts, explicit
                                  # no-password/no-SSH-key/no-token instruction) plus a production
                                  # secrets/environment inventory (every DB_PASSWORD/
                                  # REDIS_PASSWORD/JWT_*/R2_* placeholder cross-referenced to
                                  # .env.example/docker-compose.prod.yml). This session's Docker
                                  # engine was found UNREACHABLE (docker ps/version failed to
                                  # connect to npipe:////./pipe/dockerDesktopLinuxEngine; no
                                  # Docker Desktop process running; no install found at the
                                  # standard path) -- a materially different local capability than
                                  # this file's own verification_environment section below (which
                                  # reflects a prior session). The still-open migration:revert
                                  # rehearsal (flagged since PLACE-037 §12 / PLACE-038 evidence
                                  # NX-5) was therefore NOT attempted here either, to avoid a false
                                  # claim -- explicitly carried forward in the new checklist §3.
                                  # Zero application code/Dockerfile/Compose/script touched. See
                                  # docs/delivery/tasks/PLACE-039.yaml.
                                  # ---- prior state (PLACE-038, completed 2026-07-25) ----
                                  # PLACE-038 completed 2026-07-25 (production readiness
                                  # implementation, repository-controlled infrastructure prep).
                                  # Implemented PLACE-037's Topology A (single VPS + Docker
                                  # Compose + Caddy) under Owner authorization. TWO REAL DEFECTS
                                  # found and fixed via required local build+boot verification:
                                  # (1) NEXT_PUBLIC_API_URL/MAP_TILE_URL were build-time-baked by
                                  # Next.js but only ever set at container runtime -- had ZERO
                                  # effect; fixed via Dockerfile ARG + compose build.args. (2)
                                  # postgres's WAL-archiving command crashed on boot (shell
                                  # word-splitting of a multi-word -c value, pre-existing since
                                  # PLACE-026, never previously triggered); fixed via YAML list
                                  # form. Full local stack (postgres/redis/minio/api/web/caddy)
                                  # built, booted, health-verified; migrations (20) ran via a new
                                  # `migrate` compose service; Redis auth genuinely enforced;
                                  # Caddy routing verified live + via `caddy validate`; web->api
                                  # data-fetching verified end-to-end with real seeded data;
                                  # backup.sh/restore.sh/rollback.sh actually executed (not just
                                  # syntax-checked) against disposable local data. Zero real
                                  # infrastructure touched (no DNS/VPS/Hostinger/Cloudflare/R2
                                  # connection). Full regression unaffected. See
                                  # docs/delivery/tasks/PLACE-038.yaml.
                                  # ---- prior state (PLACE-037, completed 2026-07-25) ----
                                  # PLACE-037 completed 2026-07-25 (production deployment target +
                                  # monitoring provider decision gate). Gate result: READY WITH
                                  # CONDITIONS. Recommended topology: single VPS running the
                                  # existing docker-compose.prod.yml stack + Caddy for automatic
                                  # TLS. Recommended minimum monitoring: infrastructure-native
                                  # (Docker healthchecks + existing structured/correlated logs) +
                                  # one external uptime check; Sentry recommended as the first
                                  # upgrade once real users exist. Hostinger compatibility
                                  # classified UNKNOWN (not assumed compatible) pending Owner plan
                                  # verification. Decision-gate/planning only -- zero application
                                  # code, Dockerfile, Compose, CI, or env file touched. See
                                  # docs/delivery/tasks/PLACE-037.yaml.
                                  # ---- prior state (PLACE-036, completed 2026-07-25) ----
                                  # PLACE-036 completed 2026-07-25 (Next.js 16 post-migration
                                  # stabilization). Decision: STABILIZED WITH BOUNDED FOLLOW-UPS.
                                  # Next.js 16.2.11 is now the trusted apps/web baseline -- see
                                  # docs/delivery/reports/PLACE-036-NEXTJS-16-BASELINE-DECLARATION-
                                  # 2026-07-25.md. One real, migration-caused defect found and
                                  # fixed: apps/web/Dockerfile's runtime COPY commands lacked
                                  # --chown=node:node, causing EACCES on .next/cache writes under
                                  # Next 16's runtime (Next 14 never exercised this write path) --
                                  # proven via direct A/B test against the retained N14 baseline
                                  # image. Fixed, re-verified. See docs/delivery/tasks/PLACE-036.yaml.
                                  # ---- prior state (PLACE-035, completed 2026-07-25) ----
                                  # PLACE-035 completed 2026-07-25 (Next.js 14->16 migration). 3
                                  # approved packages bumped (next/eslint-config-next -> 16.2.11,
                                  # eslint -> 9.39.5); ESLint flat-config migration + 5 mandatory
                                  # async-params conversions completed; zero apps/api or packages/*
                                  # file touched. See docs/delivery/tasks/PLACE-035.yaml.
                                  # ---- prior state (PLACE-032, completed 2026-07-24) ----
                                  # PLACE-032 completed 2026-07-24 (NestJS 10->11 migration). 10
                                  # approved packages bumped to their NestJS-11-line versions
                                  # (@nestjs/common/core/platform-express/testing/cli/schematics ->
                                  # 11.1.28-family, @nestjs/config -> 4.0.4, @nestjs/jwt -> 11.0.2,
                                  # @nestjs/typeorm -> 11.0.3, @nestjs/terminus -> 11.1.1);
                                  # @nestjs/throttler settled at in-range 6.5.0. apps/api/package.json
                                  # diff is exactly those 10 lines; zero apps/web change. A real
                                  # installation defect was found and fixed: incremental npm install
                                  # left @nestjs/config/jwt/throttler pointing at a stale nested
                                  # @nestjs/common@10.4.22 copy alongside the new top-level 11.1.28
                                  # (confirmed via require.resolve(), not just npm ls); npm dedupe
                                  # failed (ERESOLVE); fixed via a full clean reinstall
                                  # (node_modules+lockfile deleted, npm install regenerated both) --
                                  # verified single 11.1.28 copy throughout afterward.
                                  # redis.health.ts (deprecated HealthIndicator/HealthCheckError
                                  # pattern) confirmed compiling+working correctly unchanged --
                                  # left as-is per the task's decision rule, deprecation recorded as
                                  # a follow-up. npm audit --omit=dev: 15 -> 2 (13 @nestjs/*-rooted
                                  # findings closed; 2 remaining are next/postcss, Next.js-rooted,
                                  # out of scope). Unit 251/251 and e2e 59/59 both IDENTICAL to the
                                  # PLACE-031 baseline -- zero regression. Docker-verified live on
                                  # the NestJS 11 image: health/auth/rate-limiting/correlation-ID
                                  # (explicit header-vs-body match)/structured logs/quiet
                                  # 4xx/zero secret leakage/DB+CORS fail-fast all confirmed
                                  # unaffected; web image rebuilt (no dependency changed), unaffected.
                                  # Full N11->N10->N11 rollback rehearsal succeeded, including
                                  # explicit data-continuity proof (a user created under N11 logged
                                  # in successfully against N10, and again against N11). Dev stack
                                  # confirmed unchanged (migrations=20, places=49) throughout; all
                                  # verification artifacts cleaned up. See
                                  # docs/delivery/tasks/PLACE-032.yaml.
  status: awaiting_task_authorization

legacy_build_chain:
  execution_control: deprecated
  known_reports:
    - BUILD_002   # delivery/sprint-01-core-data/BUILD_002_PLACE_PRIORITY_REMEDIATION.md
    - BUILD_001   # delivery/sprint-01-core-data/BUILD_001_PLACE_INSPECTION_GAP_ANALYSIS.md (authored 2026-07-22)
  missing_reports:
    - BUILD_003
    - BUILD_004
    - BUILD_005
    - BUILD_006
    - BUILD_007
    - BUILD_008
    - BUILD_009
    - BUILD_010
    - BUILD_011
    - BUILD_012
    - BUILD_013
    - BUILD_014
  synthetic_reconstruction_allowed: false
  note: >-
    BUILD_003..BUILD_014 were never produced. Prompts numbered BUILD_003 and higher
    repeatedly returned BLOCKED because they presupposed prior reports and
    implementation/deployment states that never existed. This state file — not the
    BUILD numbering — is now the execution-control source of truth.

gates:
  # G-3 (PLACE-019, 2026-07-23): three comments below were refreshed because their TEXT was
  # contradicted by later verified evidence. Every gate VALUE is unchanged and deliberately
  # conservative — no gate was advanced, and no transition condition was treated as met.
  repository_inspection: passed
  architecture_baseline: partial   # docs/ SSOT + ADRs exist; typecheck/build run. PLACE-020 refresh: DB + HTTP evidence now EXISTS (e2e 22/22 on real Postgres/PostGIS/Redis; /api/health 200 db=up/redis=up). Value stays `partial` — deploy/canary evidence still absent.
  place_domain_analysis: passed    # PLACE-001 completed 2026-07-22 (evidence/PLACE-001-baseline.md)
  implementation: in_progress      # PLACE-020 refresh: the old rationale ("nothing is exercised against a database or over HTTP") is now FALSE — the API booted from its production build and the DB-backed e2e paths run green. Value held at `in_progress` conservatively pending EXPLAIN evidence (F-15) and the open owner decisions; no gate advanced by a governance task.
  testing: in_progress             # PLACE-020 refresh: the old rationale ("DB mocked or absent; the 5 e2e specs have never run") is now FALSE — the e2e suite ran 5 suites / 22 tests green against real Postgres/PostGIS + Redis under Node v20.20.2. Value held at `in_progress` pending EXPLAIN/index-usage proof (GAP-06/F-15) and broader DB-backed coverage judgement.
  deployment: not_started
  canary: not_started
  hypercare: not_started
  stabilization: not_started

verification_environment:
  # PLACE-020 (2026-07-24): reconciled to the certified D: (NTFS) checkout. The pinned repository
  # runtime Node v20.20.2 / npm 10.8.2 (repo .nvmrc=20, CI node-version:20) was used for all
  # certification. The portable v24.18.0 extract still exists but was NOT used for certification.
  node_runtime: available              # PINNED v20.20.2 / npm 10.8.2 at %LOCALAPPDATA%\node-portable\node-v20.20.2-win-x64 (used); v24.18.0 also present, not used
  node_on_system_path: false           # portable; prepended per shell
  workspace_packages_linked: true      # D: is NTFS — node_modules/@phuquochub/{shared-types,utils} are REAL workspace symlinks
  workspace_packages_materialized: >-
    Superseded on D:. On the NTFS checkout the @phuquochub/* packages resolve via real npm
    workspace symlinks, so the FAT32 copy-staleness workaround no longer applies.
  can_run_lint: true                   # verified: eslint src/**/*.ts --max-warnings=0 → exit 0
  can_run_typecheck: true              # verified: tsc -p tsconfig.json --noEmit → exit 0
  can_run_build: verified              # full `turbo run build --force` → 4/4, 0 cached; api dist/main.js+app.module.js+core/ present, 153 src==153 js; web .next present; production startup VERIFIED (see below)
  can_run_unit_tests: true             # verified: jest → 210/210, 29 suites
  can_run_e2e: true                    # verified: jest --config test/jest-e2e.json → 5 suites / 22 tests green against real Postgres/PostGIS + Redis
  production_startup: verified         # node dist/main.js booted; GET /api/health → 200 (data.status=ok, database=up, redis=up); web `next start` GET / → 200; processes terminated, ports freed
  docker: installed_and_running        # phuquoc-postgres (postgis:16-3.4), phuquoc-redis (redis:7-alpine), phuquoc-minio all health=healthy on EXISTING volumes (not recreated); migrations table = 20 rows
  volume: "D: NTFS (D:\\Projects\\PhuQuocHub). Prior F: FAT32 checkout is obsolete."
  highest_leverage_fix: "Obtain OWNER adjudication on GAP-05/10 (openapi list params) — the sole outstanding release blocker; gather EXPLAIN evidence for idx_places_status_active (GAP-06/F-15)."
  runbook: docs/delivery/ENVIRONMENT-SETUP-RUNBOOK.md

owner_decisions:
  # Recorded 2026-07-23 by the owner-decision approval gate. Approval source: owner instruction
  # supplied in-session. NO individual approver name, job title, ticket or meeting exists in
  # repository context and none is asserted. Records live in docs/delivery/decisions/ and the
  # four-part finding state in docs/delivery/findings/ (a directory created by that gate).
  recorded_at: "2026-07-23"
  convention_note: >-
    No owner-decision naming convention pre-existed. docs/delivery/decisions/ held only
    ADR-DELIVERY-001, whose own scope line reads "Delivery execution control only", so extending
    that series to product/contract decisions would have mislabelled them. The OWNER-DECISION-<F-id>
    convention was established by this gate and should be ratified or renamed by the owner.
  decisions:
    - id: OD-F-1
      finding: F-1
      option: F1-C
      status: APPROVED
      record: docs/delivery/decisions/OWNER-DECISION-F-1.md
      remediation_task: PLACE-016
      summary: "Out-of-box coordinates must warn, not reject. Globally invalid coordinates still rejected. No replacement coordinates supplied or invented."
    - id: OD-F-6
      finding: F-6
      option: F6-A
      status: APPROVED
      record: docs/delivery/decisions/OWNER-DECISION-F-6.md
      remediation_task: PLACE-017
      summary: "OpenAPI follows validated runtime. status/sort/cursor get explicit deprecation notes, not silent deletion, because external consumer usage cannot be ruled out."
    - id: OD-F-17
      finding: F-17
      option: F17-D
      status: APPROVED
      record: docs/delivery/decisions/OWNER-DECISION-F-17.md
      remediation_task: PLACE-018
      summary: "status documented on PlaceCard using the EXISTING PlaceStatus enum (payload unchanged); score removed as dead surface — no endpoint ever emitted it via PlaceCard."
  approval_does_not_imply: >-
    A recorded decision is NOT an implementation and NOT a resolved finding. F-1, F-6 and F-17 all
    remain release blockers with implementation_status PENDING and validation_status PENDING.

owner_decisions_2026_07_24:
  # Release-blocker adjudication from the Owner Decision Package. Approval: owner instruction
  # in-session 2026-07-24. Consolidated record: docs/delivery/decisions/OWNER-DECISIONS-2026-07-24.md
  # Evidence base preserved: docs/delivery/reports/OWNER-DECISION-PACKAGE-2026-07-24.md
  recorded_at: "2026-07-24"
  record: docs/delivery/decisions/OWNER-DECISIONS-2026-07-24.md
  decisions:
    - { id: OD-B1, item: "GAP-05/GAP-10", option: B1-A, class: implementation, status: APPROVED,
        also: "ADR-010 Proposed -> Accepted", remediation_task: PLACE-021,
        summary: "Ratify offset page/limit; status/sort/cursor stay deprecated (400), not deleted; NO cursor pagination; runtime preserved, OpenAPI reconciled to runtime." }
    - { id: OD-B2, item: F-24, option: B2-A, class: implementation, status: "APPROVED (impl PENDING)",
        summary: "Rename getCardById to privileged name + architecture test barring @Public reach. Not executed here." }
    - { id: OD-B3, item: F-34, option: B3-A, class: implementation, status: "APPROVED (impl PENDING)",
        summary: "bboxClusters deterministic truncation ORDER BY cnt DESC, cell ASC before LIMIT 500. Not executed here." }
    - { id: OD-B4, item: SearchResult.score, option: B4-A, class: implementation, status: "APPROVED (impl PENDING)",
        summary: "Remove score from public /search payload; retain ts_rank for ordering. Not executed here." }
    - { id: OD-B5, item: "F-1/PROVISIONAL-bbox", option: B5-B, class: accepted_temporary_risk, status: ACCEPTED,
        summary: "Provisional bbox accepted as documented non-authoritative boundary for initial release; softened behaviour (PLACE-016) retained; NO new code. Authoritative boundary remains future work." }
    - { id: OD-B6, item: "GAP-06/F-15", option: B6-A, class: deferred_optimization, status: DEFERRED,
        summary: "Defer EXPLAIN/index-planner proof to a future performance task at scale. Do NOT add or alter indexes now." }
    - { id: OD-B7, item: GAP-15, option: B7-A, class: implementation, status: "APPROVED (impl PENDING)",
        summary: "Record TypeORM migrations/entities as authoritative runtime persistence; Prisma reference-only. Authority note is a separate PENDING task; not executed here." }
  approval_does_not_imply: >-
    Only OD-B1 is authorized for implementation (PLACE-021). OD-B2/B3/B4/B7 are APPROVED but
    implementation PENDING; OD-B5 is an accepted documented risk (no new code); OD-B6 is deferred.
    None of B2..B7 may be represented as implemented.

proposed_tasks:
  # Derived from approved decisions, deliberately NOT activated except PLACE-021 (OD-B1, this turn).
  # Each other requires explicit owner authorization to become current.task.
  - { id: PLACE-016, status: COMPLETED, decision: OD-F-1, file: docs/delivery/tasks/PLACE-016.yaml,
      completed: "2026-07-23" }
  - { id: PLACE-017, status: COMPLETED, decision: OD-F-6, file: docs/delivery/tasks/PLACE-017.yaml,
      completed: "2026-07-23" }
  - { id: PLACE-018, status: COMPLETED, decision: OD-F-17, file: docs/delivery/tasks/PLACE-018.yaml,
      completed: "2026-07-23" }
  - { id: PLACE-021, status: COMPLETED, decision: OD-B1, file: docs/delivery/tasks/PLACE-021.yaml,
      completed: "2026-07-24", note: "B1 — GAP-05/10 RESOLVED; offset ratified; docs reconciled to runtime; runtime unchanged." }
  - { id: PLACE-022, status: COMPLETED, decision: OD-B2, file: docs/delivery/tasks/PLACE-022.yaml,
      completed: "2026-07-24", note: "F-24 RESOLVED — getCardById -> getCardByIdIncludingInactive + mutation-checked architecture test; runtime unchanged." }
  - { id: PLACE-023, status: COMPLETED, decision: OD-B3, file: docs/delivery/tasks/PLACE-023.yaml,
      completed: "2026-07-24", note: "F-34 RESOLVED — ORDER BY cnt DESC, sample_id ASC before LIMIT; ordering-only; determinism proven 5x." }
  - { id: PLACE-024, status: COMPLETED, decision: OD-B4, file: docs/delivery/tasks/PLACE-024.yaml,
      completed: "2026-07-24", note: "F-35 RESOLVED — score removed from public /search payload; ts_rank internal; ordering empirically unchanged." }
  - { id: PLACE-025, status: COMPLETED, decision: OD-B7, file: docs/delivery/tasks/PLACE-025.yaml,
      completed: "2026-07-24", note: "GAP-15 RESOLVED — TypeORM confirmed sole runtime persistence authority; Prisma reference-only; documentation/governance only, zero runtime impact." }

next_action:
  task_id: none
  status: awaiting_task_authorization
  objective: >-
    (2026-08-01, Image Upload UI) High #4 on the roadmap is no longer a bare gap -- a strictly
    bounded single-image upload now exists, integrated into the review submission form via the
    pre-existing orphan-media + attachToReview() path. Live-verified end-to-end including a real
    browser CORS check (no config change needed) and a direct DB join proving the attachment
    genuinely occurred. Full detail in current.task's comment block above; report at
    docs/delivery/reports/IMAGE-UPLOAD-UI-2026-08-01.md. No backend/schema change. Deliberately NOT
    done (per the fixed Owner scope): drag-drop, crop, gallery, delete, reorder, bulk upload, image
    editing, a real progress bar, moderation, or any place_id-owned upload flow -- these remain
    open if a future milestone wants them. Remaining genuinely-open roadmap items: Critical #1
    (general write capability beyond reviews), High #6 (frontend component test coverage --
    a first reusable pattern now exists, though coverage
    is still far from comprehensive across all browse pages/cards).
    ---- prior state (2026-08-01 Transport Browse Filters) ----
    GET /transports gained the 5 filters
    docs/data/modules/transport.md had already fully designed -- zero new Owner decision, zero
    migration. Full detail in current.task's comment block above; report at
    docs/delivery/reports/TRANSPORT-BROWSE-FILTERS-2026-08-01.md. No frontend Transport browse
    page started (that remains outside ADR-017's current Accepted scope). Remaining genuinely-open
    roadmap items, unchanged by this milestone: Critical #1 (general write capability), High #4
    (image upload UI -- backend ready since Media Upload Foundation, still needs a frontend and
    real UX decisions nobody has made yet), High #6 (frontend component test coverage).
    ---- prior state (2026-07-30 Media Upload Foundation) ----
    Design-review-approved backend milestone shipped -- new
    S3-compatible StorageService (core/storage/) + media presign/register write path
    (modules/media/), with the 3 approved design changes (metadata-only persistence, dev/test
    bucket isolation, HEAD-first/GET-stream-fallback checksum verification) plus a presign-session
    integrity mechanism (Redis) that closes a gap the original plan had left unresolved. Full
    detail in current.task's comment block above; report at
    docs/delivery/reports/MEDIA-UPLOAD-FOUNDATION-2026-07-30.md. Frontend upload UI explicitly not
    started, per instruction. Remaining genuinely-open roadmap items unchanged by this
    infrastructure-layer milestone: Critical #1 (general write capability), High #4 (image upload
    UI -- now unblocked at the backend, still needs a frontend), High #6 (frontend component test
    coverage).
    ---- prior state (2026-07-30 governance reconciliation) ----
    This field had fallen behind the ad-hoc product-
    completion stream by several milestones -- it still ended at the 2026-07-25 SEO task even
    though Reviews (07-26), Booking Foundation (07-29), Booking Application Layer/Phase 2 (07-30),
    Availability & Inventory Foundation (07-30), and Search Filters (07-30, + a post-implementation
    review) had all shipped since. Rather than re-transcribing all of that narrative a second time
    in this field, the authoritative detailed record is current.task's comment block above (the
    same convention this field and that one have always mirrored) -- see it for full detail on
    each milestone, and see docs/delivery/reports/MVP-GAP-ANALYSIS-AND-ROADMAP-2026-07-25.md
    (updated same day) for the roadmap's own now-corrected status: item #2 (Reviews) and item #5
    (Search filters) are DONE; item #3 (hotel/restaurant/tour list pages) was found already-built,
    undocumented, during an earlier audit this same day, and is now also marked DONE. Remaining
    genuinely-open roadmap items, in priority order: Critical #1 (general write capability beyond
    reviews -- still the single biggest gap), High #4 (image upload UI), High #6 (frontend
    component test coverage). No PLACE-044 created; this reconciliation is itself ad-hoc,
    documentation-only, per the same standing 2026-07-26 Owner instruction.
    ---- prior state (2026-07-25 SEO task) ----
    (2026-07-25, ad-hoc, not PLACE-numbered per explicit Owner instruction) MVP gap analysis and
    roadmap completed -- see docs/delivery/reports/MVP-GAP-ANALYSIS-AND-ROADMAP-2026-07-25.md.
    Biggest finding: the web frontend has ZERO write capability beyond auth; reviews/community/
    contributions/notifications are unimplemented backend stubs despite "Reddit" being a named
    pillar. Selected and executed the highest-value fully-local task: SEO & Discoverability
    completeness (NEXT_PUBLIC_SITE_URL + metadataBase fix, new sitemap.ts/robots.ts, JSON-LD
    structured data on all 5 detail page types with an escaped-serialization helper closing a
    real XSS-adjacent risk in the standard inline-JSON-LD pattern). Full validation clean
    (tsc/eslint/unit/build all pass, zero backend touched). Critical/High roadmap items (write
    capability, reviews, hotel/restaurant/tour list pages, image upload, search filters, frontend
    test coverage) remain fully open for a future task. No PLACE-044 created.
    ---- prior state (PLACE-043, completed 2026-07-25) ----
    PLACE-043 (2026-07-25, Hostinger VPS provisioning verification) is now COMPLETED --
    "COMPLETED -- VPS NOT PURCHASED; PRODUCTION DEPLOYMENT DEFERRED" -- reopened same-day per
    explicit Owner authorization to record Owner-supplied facts: Hostinger Web Hosting CONFIRMED
    active (50GB NVMe/daily backup/CDN/Node.js); Hostinger KVM VPS CONFIRMED NOT PURCHASED; DNS
    CONFIRMED managed at Hostinger (nameservers atlas.dns-parking.com/hyperion.dns-parking.com,
    apex A -> 2.57.91.91 explicitly NOT a VPS IP, www CNAME -> phuquochub.com). New evidence-based
    Web Hosting Suitability Assessment: not confirmed suitable for the current multi-service
    Docker topology without material redesign (no redesign attempted). Production deployment
    remains BLOCKED UNTIL VPS OR EQUIVALENT INFRASTRUCTURE IS PURCHASED -- a real prerequisite,
    not an open task item. All repository-side deliverables from the original completion
    (existing-assumption inventory, a 3-tier resource-suitability assessment grounded in the
    actual docker-compose.prod.yml topology, the 24-stage HOSTINGER-VPS-BOOTSTRAP-RUNBOOK.md (zero
    destructive command, correct SSH-hardening sequencing), and PRODUCTION-ACCESS-AND-SECRET-
    BOUNDARIES.md) remain valid and ready for when a VPS is purchased. Live SSH verification not
    attempted -- no VPS exists. The original OWNER INPUT REQUIRED request for 11 non-sensitive
    Hostinger facts has now been fully answered by the Owner directly -- no password/key/token
    was ever requested or supplied. See
    docs/delivery/tasks/PLACE-043.yaml.
    ---- prior state (PLACE-042, completed 2026-07-25) ----
    PLACE-042 (2026-07-25) proved, via a real live rehearsal (not simulated -- Docker was
    successfully restored this session by launching the already-installed Docker Desktop app),
    that the current-head migration (AddPlacesStatusPartialIndex1720001900000) can be reverted
    via the official migration:revert command, leaves 2 designed marker rows fully intact, and can
    be re-applied via migration:run to restore the exact original migration head -- with zero
    duplicate/inconsistent migration-table state. A safe failure-mode test (invalid local DB_NAME)
    confirmed migration:revert fails safely without touching the real database. New
    scripts/migration-rollback-rehearsal.sh (safety-guarded, both guards tested) wraps this cycle
    and ran live end-to-end. New DATABASE-ROLLBACK-RECOVERY-RUNBOOK.md answers every required
    recovery question. Backup/restore procedure reviewed but NOT operationally re-rehearsed here
    (already actually executed for real in PLACE-038). This proves the LOCAL rollback mechanism
    only -- it does NOT prove a real production rollback, nor cover a destructive (non-additive)
    migration, none of which exists in this repository yet. See docs/delivery/tasks/PLACE-042.yaml.
    ---- prior state (PLACE-041, completed 2026-07-25) ----
    PLACE-041 (2026-07-25) ran a full 10-section enterprise production audit (repository health,
    backend, frontend, infra, ops, security, performance) and fixed the one real correctness bug
    found: hotels/restaurants/tours/events [slug] pages masked every error (incl. 5xx/network) as
    404, unlike places/[slug]'s already-correct pattern -- fixed identically in all 4 files, plus
    error/loading boundaries for those routes + a root global-error.tsx, new security-response-
    headers middleware, and a new INCIDENT-RESPONSE-RUNBOOK.md. 256/256 api unit tests (was 254),
    apps/web tsc/eslint/17-unit/11-route-build all clean. 8-dimension score recorded; repository-
    controlled readiness held at ~92%. NOT concluded production-ready -- see the report's
    Critical/High action list (Owner-side VPS/DNS/R2/monitoring + the still-Docker-blocked
    migration:revert rehearsal). See docs/delivery/tasks/PLACE-041.yaml.
    ---- prior state (PLACE-040, completed 2026-07-25) ----
    PLACE-040 (2026-07-25) ran a full 20-category production configuration inventory (evidence-
    backed READY/PARTIAL/MISSING/BLOCKED verdicts, see its report) and fixed two real gaps found:
    REDIS_URL now production-required (mirrors DB_HOST, closes a silent-misconfiguration risk);
    scripts/deploy.sh's dangling smoke-test reference replaced with a real, working
    scripts/smoke-test.sh. New docs/delivery/RELEASE-AND-ROLLBACK-CHECKLIST.md (operator checkbox
    checklist). Repository-controlled readiness scored ~92%; migration:revert remains BLOCKED
    (Docker engine unreachable this session, not Owner input). Zero
    docker-compose.prod.yml/Dockerfile/Caddyfile change -- audit found these already correct. See
    docs/delivery/tasks/PLACE-040.yaml.
    ---- prior state (PLACE-039, completed 2026-07-25) ----
    PLACE-039 (2026-07-25) produced docs/delivery/PRE-DEPLOYMENT-CHECKLIST.md: a Hostinger VPS
    hPanel preflight (8 non-sensitive facts, explicit no-credential instruction) plus a production
    secrets/environment go-live inventory (every DB_PASSWORD/REDIS_PASSWORD/JWT_*/R2_* placeholder
    named against .env.example/docker-compose.prod.yml). Confirmed PLACE-037/038's 5 Owner
    decisions unchanged and already correctly closed -- no reopening was needed. Docker engine was
    unreachable this session; the migration:revert rehearsal (open since PLACE-037 §12) remains
    NOT performed, carried forward. Zero code/Dockerfile/Compose/script touched. See
    docs/delivery/tasks/PLACE-039.yaml.
    ---- prior state (PLACE-038, completed 2026-07-25) ----
    PLACE-038 (2026-07-25) implemented PLACE-037's Topology A production readiness (single VPS +
    Docker Compose + Caddy) under direct Owner authorization of all 5 PLACE-037 conditions --
    repository-controlled infrastructure preparation ONLY, external deployment explicitly NOT
    authorized. TWO REAL, PREVIOUSLY-UNDISCOVERED DEFECTS found and fixed, both surfaced only by
    this task's own required local build+boot verification (not by review alone): (1)
    NEXT_PUBLIC_API_URL/NEXT_PUBLIC_MAP_TILE_URL are baked in by Next.js at BUILD time, but
    docker-compose.prod.yml only ever set them as a container-runtime environment: value, which had
    ZERO effect (proven via direct compiled-bundle inspection, both client and server chunks) --
    fixed via apps/web/Dockerfile ARG/ENV + compose build.args, re-verified with an explicit
    --build-arg producing the correct URL throughout the bundle. (2) postgres's WAL-archiving
    command crashed the container on every boot (invalid argument: /opt/wal-archive.sh) -- a
    pre-existing defect dating to PLACE-026, caused by YAML folded-scalar command: shell-word-
    splitting a multi-word -c value, NEVER previously triggered because no prior task had actually
    booted this exact service through this exact compose path end-to-end; fixed by converting to
    YAML list form, re-verified via a clean stack boot with postgres reporting healthy. Implemented:
    Caddy reverse proxy (new infrastructure/caddy/Caddyfile, phuquochub.com + a documented local
    :8080 test address sharing the identical routing logic, automatic HTTPS, verified via both
    `caddy validate` and live local requests); removed direct host-port publishing for
    postgres/redis/minio/api/web (Topology A's own private-networking requirement, only Caddy
    publishes 80/443); added a Redis password (verified genuinely enforced -- unauthenticated ping
    fails NOAUTH, authenticated succeeds, API's RedisHealthIndicator confirmed healthy); added
    native Docker HEALTHCHECK to both apps/api and apps/web Dockerfiles (verified reporting healthy
    live); added --chown=node:node to apps/api/Dockerfile defensively (PLACE-036 precedent, no
    direct evidence of a runtime issue but no evidence of safety either); added a new `migrate`
    compose service (reuses the Dockerfile's intermediate build stage with full TS source +
    ts-node, since postgres's port is no longer host-published so migrations can't run from a bare
    host process) -- verified live: all 20 migrations applied cleanly to a fresh empty database.
    Wrote scripts/deploy.sh, backup.sh, restore.sh, rollback.sh, sync-offsite.sh -- backup.sh and
    restore.sh actually EXECUTED (not just syntax-checked) against disposable local data (produced
    a real, valid gzip pg_dump; destructively restored it; row counts identical after);
    rollback.sh actually executed against two tagged images (swap + health re-verification
    succeeded); sync-offsite.sh's safe no-credential exit path exercised, never run against real
    R2 (no credentials exist in this session). Web-to-API data-fetching mechanism verified
    end-to-end with real seeded data through Caddy -- caught and corrected a genuine test-setup
    mistake along the way (an initial attempt pointed the web build at localhost:8080, which
    cannot work cross-container; re-diagnosed, fixed the test methodology, not the
    implementation). Full monorepo regression (lint/typecheck/unit web-17-api-251/e2e-59) run
    fresh post-change: zero application-source behavior change, identical totals to every prior
    PLACE baseline. docs/architecture/deployment.md reconciled: status header + the original
    9-item Section-15 decision list now marked resolved/deferred against PLACE-037/038's actual
    outcomes. ZERO real infrastructure touched anywhere (no DNS, no VPS, no Hostinger/Cloudflare/R2
    connection, no public exposure) -- entirely local Docker verification, fully torn down and
    cleaned up afterward; dev stack confirmed unaffected throughout. Deriving another task without
    explicit owner authorization is NOT permitted.
  eligible_candidate_disposition:
    - "Candidate 1 (deploy pipeline, OD2-2,3,4,5,7,8,9) — IMPLEMENTED (PLACE-026) + repository-controlled readiness IMPLEMENTED (PLACE-038, Topology A/Caddy/scripts). Real-infrastructure deployment now gated ONLY on Owner-side real-world steps (Hostinger plan verification/provisioning, real DNS, real R2 credentials) — no further engineering work required to reach a deployable state."
    - "Candidate 2 (dependency remediation, OD2-11) — IMPLEMENTED (PLACE-027 safe subset + PLACE-029 bcrypt/tar closure + PLACE-032 NestJS-11 closure of 13 findings + PLACE-035/036 Next.js-16 migration+stabilization, though the next/postcss/sharp findings themselves remain open pending an upstream fix — see place_036_status)."
    - "Candidate 3 (rate-limit + CORS, OD2-12/13) — IMPLEMENTED (PLACE-028)."
    - "PLACE-029 Candidate A (bcrypt + DB-credential hardening) — IMPLEMENTED (PLACE-029)."
    - "PLACE-029 Candidate B (NestJS 10->11) — IMPLEMENTED (PLACE-032). Candidate C (Next.js 14->16) — IMPLEMENTED and STABILIZED (PLACE-035/036). All three PLACE-029 candidate framework-migration items are now closed."
    - "PLACE-029 Candidate D, logging sub-scope (AppLoggerService wiring + correlation-ID propagation) — IMPLEMENTED (PLACE-030). The monitoring-provider sub-scope (OD2-6) minimum stack IMPLEMENTED (PLACE-038 — Docker healthchecks + existing structured logs + Caddy); the external uptime-monitor account itself and any enhanced-stack (Sentry) decision remain Owner-side real-world steps."
    - "PLACE-031 candidate assessment Candidate E (release & rollback readiness) — IMPLEMENTED (PLACE-031). Candidate F (product delivery acceleration) — DEFER, no evidence-backed bounded scope currently exists."
    - "OD2-1 (release scope), OD2-14 (Prisma stub) — APPROVED but require no PLACE candidate (OD2-1's action is the approval itself; OD2-14 is ordinary hygiene)."
    - "OD2-6 (monitoring) — APPROVED; minimum stack IMPLEMENTED (PLACE-038); enhanced-stack (Sentry) sequenced after real users exist, an Owner-side timing decision, not an engineering blocker."
    - "OD2-10 (AI budget) — DEFERRED, unchanged."
  pending_owner_decision: "(2026-07-26) Reviews (create+read) is now IMPLEMENTED -- see docs/delivery/reports/MVP-REVIEWS-FEATURE-2026-07-26.md. NOT yet run against a live database (no Docker/Postgres available in this environment) -- run `npm run migration:run --workspace=apps/api` before relying on it end-to-end. Remaining Critical/High from the 2026-07-25 roadmap, still fully open: general place write/edit UI (create/edit forms beyond auth), hotel/restaurant/tour list pages (detail-only today), image upload UI (no presign/upload endpoint or UI anywhere), search filters (category/price/ward) not surfaced in the /search UI, moderation queue UI, business-claim workflow, frontend component-test infrastructure (jest env=node, no jsdom/RTL -- structurally cannot render-test React components today). Reviews' own PATCH/DELETE/report/reply/vote endpoints (already documented in openapi.yaml) remain unimplemented too. Infrastructure track remains CLOSED/DEFERRED (PLACE-043: VPS not purchased, not urgent) and is not to be resumed without new explicit Owner authorization."
  release_blockers_remaining: "NONE from a code-correctness OR a repository-readiness standpoint. 0 critical + 8 high + 0 moderate (prod) dependency findings remain OPEN as of PLACE-036 (unchanged, blocked on an upstream fix outside this repository's control). The single-VPS Docker Compose + Caddy topology is now fully implemented and locally verified (PLACE-038); real production release requires only the Owner-side real-world steps listed above, which no engineering task can fabricate or substitute for."

completed_tasks:
  - id: PLACE-043
    completed: "2026-07-25"
    outcome: "COMPLETED — VPS NOT PURCHASED; PRODUCTION DEPLOYMENT DEFERRED"
    result: >-
      Hostinger VPS provisioning verification and production access readiness, under explicit
      Owner written authorization, reopened same-day (NOT PLACE-044) after its original
      completion at status `blocked` to record facts the Owner supplied directly. Original
      completion had produced everything repository-side possible: a full existing-assumption
      inventory (CONFIRMED/OWNER-STATED/INFERRED/PLACEHOLDER/UNVERIFIED classification of every
      prior Hostinger-related claim -- "Hostinger KVM VPS" was consistently an OWNER-STATED
      DECISION, never a confirmed provisioning fact), a 3-tier resource-suitability assessment
      grounded in docker-compose.prod.yml's actual 6-service topology (Minimum Viable/
      Recommended-already-Owner-approved/Upgrade-threshold, unchanged ~1-2M-VND/month budget), a
      new 24-stage docs/delivery/HOSTINGER-VPS-BOOTSTRAP-RUNBOOK.md (zero destructive command
      anywhere, SSH hardening correctly sequenced AFTER key-access verification to avoid a
      self-lockout), and a new docs/delivery/PRODUCTION-ACCESS-AND-SECRET-BOUNDARIES.md (what may
      be known vs. what must never be stored, naming only pre-existing storage locations). That
      completion stopped at an explicit "OWNER INPUT REQUIRED" table requesting exactly 11
      non-sensitive Hostinger facts. This reopening records the Owner's direct answers: Hostinger
      Web Hosting CONFIRMED active (50GB NVMe, daily backup, CDN, Node.js support); Hostinger KVM
      VPS CONFIRMED NOT PURCHASED; DNS CONFIRMED managed at Hostinger, nameservers
      atlas.dns-parking.com/hyperion.dns-parking.com, apex A record -> 2.57.91.91 (EXPLICITLY
      recorded as NOT a VPS IP -- it is Hostinger's current Web-Hosting/DNS-parking target, no
      VPS exists to have an IP at all), www CNAME -> phuquochub.com; Web Hosting backup CONFIRMED
      (daily, part of the plan); VPS backup/snapshot NOT APPLICABLE (no VPS purchased); VPS
      OS/region/vCPU/RAM/storage/snapshot spec recorded as NOT APPLICABLE rather than still-
      pending-unknown, since no such machine exists to have a spec. Added a new, evidence-based Web
      Hosting Suitability Assessment: reviewed the current multi-service Docker Compose production
      topology (postgres/redis/minio/api/web/caddy, docker-compose.prod.yml) against Hostinger Web
      Hosting's actual confirmed capabilities (Node.js support, not Docker support) and this
      repository's own connectivity assumptions (apps/api/src/core/database/data-source.ts expects
      a directly-reachable Postgres+PostGIS server; apps/api/src/core/redis/redis.service.ts
      expects a directly-reachable Redis server; infrastructure/caddy/Caddyfile needs to own ports
      80/443 for automatic TLS) -- concluded NOT confirmed suitable for the current topology
      without material redesign; explicitly did NOT recommend or attempt any such redesign; named
      what Web Hosting CAN still legitimately serve today (domain/DNS/email/a temporary landing or
      maintenance page/a static informational page) versus what genuinely requires a VPS or
      equivalent managed service (the NestJS API runtime, PostgreSQL/PostGIS, Redis, Docker Compose
      orchestration itself, reverse-proxy port ownership, system-level monitoring/backup controls).
      Recorded the governing conclusion: production deployment using the current Docker/
      PostgreSQL/PostGIS/Redis topology is BLOCKED UNTIL VPS OR EQUIVALENT INFRASTRUCTURE IS
      PURCHASED -- a real, unavoidable prerequisite this task did not fail to resolve, simply
      cannot resolve by itself. Recorded the exact future Owner action (not urgent): do not
      purchase a VPS immediately; continue local development and Docker production-like
      validation; purchase only once go-live readiness is reached; re-check real Hostinger KVM
      pricing against the Recommended tier and the existing budget before purchasing; reopen the
      bootstrap runbook after purchase. Explicitly never claims "VPS ready", "SSH ready",
      "production ready", or "deployment complete"; explicitly never classifies 2.57.91.91 as a
      VPS address; explicitly does not recommend rewriting the architecture to fit shared Web
      Hosting. Zero application code, Dockerfile, Compose file, or script touched; zero DNS
      modification (read-only recording of Owner-supplied facts only); zero real secret used or
      stored. No PLACE-044 created or started.
    report: docs/delivery/reports/PLACE-043-hostinger-vps-readiness-report.md
    evidence_index: docs/delivery/evidence/PLACE-043-hostinger-vps-readiness-evidence-index.md
  - id: PLACE-042
    completed: "2026-07-25"
    result: >-
      Database migration rollback rehearsal and recovery verification, under explicit Owner
      written authorization. Re-verified repository truth first (HEAD 8d6c5c7, clean tree,
      PLACE-037..041 all completed, current.task: none). Docker engine -- unreachable across the
      PLACE-039/040/041 sessions -- was diagnosed (docker context ls confirmed the correct
      "desktop-linux" context was already active, ruling out a misconfiguration as the cause) and
      RESTORED by launching the already-installed Docker Desktop application directly (a safe,
      fully reversible action -- no reinstall, no OS configuration change); the daemon became
      reachable within 5 seconds and the pre-existing dev stack (phuquoc-postgres/-redis/-minio)
      auto-restarted healthy via its own restart:unless-stopped policy. This unblocked a REAL,
      live rehearsal rather than a documentation-only exercise. Selected
      AddPlacesStatusPartialIndex1720001900000 (the current head, authored PLACE-003) as the
      rehearsal migration: its down() only drops the one index its own up() creates, has zero
      dependency on any other migration for its own revert, and is purely structural (zero data
      mutation) -- the safest and most representative migration available, since all 20
      migrations in this repository are additive (independently re-confirmed by reading every
      down() this task, not merely citing the prior PLACE-037/038 claim). Inserted 2 minimal,
      clearly-marked test rows (place-042-rehearsal-active with deleted_at NULL,
      place-042-rehearsal-deleted soft-deleted) directly into the LOCAL DEV database -- not a
      disposable throwaway stack, justified because this specific migration never touches row
      data, only an index. Ran the OFFICIAL migration:revert command (exit 0): independently
      verified via direct SQL that idx_places_status_active was dropped, the migrations table
      went from 20 to 19 rows with the head correctly moved back one step, and BOTH marker rows
      remained completely unchanged. Ran the OFFICIAL migration:run command (exit 0):
      independently verified the index was recreated with a byte-identical definition, the
      migrations table returned to exactly 20 rows with the head restored to
      AddPlacesStatusPartialIndex1720001900000, zero duplicate migration name or index object
      exists, and migration:show reports all 20 as [X] with zero pending. Noted and explicitly
      explained (not hidden) a cosmetic id-column gap in the migrations table after each
      revert+re-apply cycle -- normal Postgres SERIAL/IDENTITY behavior following DELETE+INSERT,
      carrying no significance since TypeORM tracks migration identity by (timestamp, name), not
      id, and migration:show's own name-based output was correct and consistent at every check.
      Performed a real (not merely reviewed) safe failure-mode test: migration:revert invoked with
      an intentionally nonexistent local DB_NAME, confirming a clean, loud failure (exit 1) at the
      connection stage before any transaction opens -- independently re-verified the real
      phuquochub database was completely unaffected afterward (unchanged migration count/head/
      marker data). Wrote a new, reusable, safety-guarded script,
      scripts/migration-rollback-rehearsal.sh, wrapping migration:show -> migration:revert ->
      migration:show -> migration:run -> migration:show with two guards (refuses under
      NODE_ENV=production; refuses unless DB_HOST resolves to localhost/127.0.0.1) -- BOTH guards
      were actually triggered and confirmed refusing (not merely reviewed), and the script was
      then run live, end-to-end, for real, exit code 0, with neither guard tripped. Wrote a new
      docs/delivery/DATABASE-ROLLBACK-RECOVERY-RUNBOOK.md answering every required recovery
      question with an explicit decision table: what to do if migration:revert fails; what to do
      if revert succeeds but re-apply fails; what to do if application code and schema become
      incompatible; when to stop a release; when a real backup restore is required instead of
      relying on down(); how to confirm migration head after any recovery action; how to retain
      logs/evidence; and who holds decision authority (the Owner, for anything beyond deploy.sh's
      own automatic halt). Cross-linked from RELEASE-AND-ROLLBACK-CHECKLIST.md section 6.
      EXPLICITLY: "Backup/restore procedure reviewed but not operationally rehearsed in
      PLACE-042" -- scripts/backup.sh/restore.sh were already actually executed for real in the
      separate, prior PLACE-038 task; this task did not repeat that cycle and does not claim to.
      Both test marker rows deleted after the rehearsal and its automation run completed; the dev
      database was independently re-verified back at its exact original 49-place baseline before
      this task was considered complete. Explicitly did NOT conclude a production rollback was
      proven -- only the LOCAL migration:revert/migration:run mechanism, the migration-tracking
      table, and the new safety-guarded script were proven, live, against the local dev database,
      for one representative additive migration. Does NOT cover a destructive (non-additive)
      migration's down() -- none exists in this repository yet; the new runbook requires this
      exact procedure be repeated against the first one ever authored, before it is deployed for
      real. Zero production deployment, zero Hostinger/DNS/production-credential action, zero new
      product feature, zero new npm dependency. No PLACE-043 created or started.
    report: docs/delivery/reports/PLACE-042-migration-rollback-rehearsal-report.md
    evidence_index: docs/delivery/evidence/PLACE-042-migration-rollback-rehearsal-evidence-index.md
  - id: PLACE-041
    completed: "2026-07-25"
    result: >-
      Enterprise production audit (repository health, backend, frontend, infrastructure,
      operations, security, performance, an 8-dimension final score, and a prioritized action
      list), authorized by an Owner instruction explicitly forbidding new features, unnecessary
      refactors, and new technical debt. Re-verified active authority first (HEAD 5ca81cd, clean
      tree, PLACE-037/038/039/040 all completed) before any file was touched. Found and FIXED one
      real, evidence-backed CORRECTNESS bug: hotels/restaurants/tours/events [slug] page
      components blanket-caught EVERY error from their data fetch -- including 5xx/network
      failures -- into a 404 Not Found response, unlike places/[slug]/page.tsx (the PLACE-035/036
      baseline), which correctly distinguishes a genuine 404 (via ApiError.isNotFound) from other
      errors and rethrows the latter to its own error.tsx. This masked real outages as "page
      doesn't exist," misleading to users and incorrect for search-engine indexing (a transient
      server error should never assert permanent non-existence to a crawler). Fixed identically
      in all 4 files using the exact already-proven places/[slug] pattern -- confirmed via direct
      comparison read of both the buggy and correct versions before editing. Three further real
      gaps were found and closed, each mirroring an existing, already-tested repository pattern
      rather than inventing a new one: (1) those same 4 routes (confirmed via a repository-wide
      find to have ZERO error/loading boundary before this task, unlike /places) now have
      error.tsx + loading.tsx, plus a new root-level global-error.tsx (also confirmed absent) --
      necessary since the fix above now lets real errors reach these routes instead of being
      silently masked; (2) apps/api had ZERO security response headers anywhere (confirmed via
      grep -- no helmet package, no manual equivalent) -- fixed via a new, small, dependency-free
      security-headers.middleware.ts (X-Content-Type-Options/X-Frame-Options/Referrer-Policy,
      deliberately NOT Strict-Transport-Security, which belongs at the Caddy layer once real HTTPS
      is live) structured identically to the existing, proven correlation-id.middleware.ts pattern
      (own file + own unit spec) rather than inline in main.ts or via the helmet npm package (
      avoided adding a new dependency for a 3-header need, per this task's own no-new-tech-debt
      instruction); (3) docs/delivery/INCIDENT-RESPONSE-RUNBOOK.md written (confirmed completely
      absent from docs/ beforehand) -- triage steps, a symptom-to-response table cross-referencing
      the existing release/rollback/backup scripts, and an explicit statement of what it does NOT
      cover (no on-call rotation or status page exist yet, honestly, not invented). Every other
      audited item across all 7 technical sections was found already correctly implemented by
      prior PLACE tasks (026/028/029/030/035/036/037/038/040) and left unchanged, OR is a
      deliberately-approved deferral (MinIO/object storage, EXPLAIN-at-scale evidence per OD-B6,
      no staging per the Owner's MVP decision) -- re-confirmed, not newly closed. Also newly
      discovered and explicitly NOT built (a new feature, forbidden by this task's own
      instruction): hotels/restaurants/tours have NO list/browse page at all (confirmed via find
      -- only their [slug] detail routes exist; only places and events have list pages). Full
      validation: apps/api unit suite 256/256 across 35 suites (was 254/34 -- +2 new tests from
      security-headers.middleware.spec.ts, zero regression); tsc/eslint both exit 0; nest build
      produced dist/common/middleware/security-headers.middleware.js cleanly. apps/web: tsc/eslint
      both exit 0; unit suite unchanged 17/17; a full `next build` (Turbopack) succeeded with
      ZERO error, all 11 routes generated and correctly classified static-vs-dynamic -- direct
      proof the 4 fixed pages + 9 new boundary files compile and build correctly. Full monorepo
      e2e was NOT re-run (Docker engine unreachable this session, same as PLACE-039/040's sessions
      -- confirmed via the identical docker ps/version npipe-connection-failure check); judged
      unaffected since zero backend business logic changed (only a new, independently-unit-tested
      header-setting middleware). An 8-dimension score was recorded with a cited evidentiary basis
      per dimension (Architecture 88, Backend 90, Frontend 74, Infrastructure 93, Operations 87,
      Security 86, Performance 83, Documentation 95, Testing 79) plus repository-controlled
      production readiness held at ~92% (PLACE-040's methodology, unchanged -- this task's fixes
      are correctness/resilience additions, not newly-scored readiness-category items). Explicitly
      did NOT conclude "production ready" -- Critical/High items remain genuinely open: the real
      Hostinger VPS/DNS/R2-credentials/uptime-monitor-account items are Owner-side and unchanged
      since PLACE-039, and the migration:revert rehearsal remains blocked on this session's
      unreachable Docker engine, not on Owner input. A full prioritized action list (Medium/Low)
      was recorded for every other real gap found but deliberately not executed this task
      (zero frontend component/integration tests exist; remaining route segments still lack
      error/loading boundaries; no live Swagger/OpenAPI UI exists, only the manually-maintained
      spec file; sitemap.xml/robots.txt remain absent (pre-existing, SEO-only); accessibility
      coverage is thin (8 files use aria-*/role repository-wide) and no dedicated audit tool was
      run; HSTS deliberately deferred to when a real domain/TLS is live; MinIO's compose image tag
      is unpinned (:latest) while the service itself remains unused) -- each with a one-line reason
      it was judged out of this task's own immediate-execution bar. Zero new npm dependency added.
      Zero real secret written anywhere. No PLACE-042 created or started.
    report: docs/delivery/reports/PLACE-041-enterprise-production-audit-report.md
    evidence_index: docs/delivery/evidence/PLACE-041-enterprise-production-audit-evidence-index.md
  - id: PLACE-040
    completed: "2026-07-25"
    result: >-
      Production configuration inventory, gap analysis, and local-only readiness closure,
      authorized by an Owner instruction requesting a full 20-category audit (environment
      variables, Docker images/Compose, reverse proxy, ports, health endpoints, Redis, PostgreSQL,
      MinIO, upload storage, JWT, cookies, CORS, rate limiting, logging, backup, restore, email,
      monitoring, build artifacts, static assets) with a READY/PARTIAL/MISSING/BLOCKED verdict and
      cited evidence per item, then execution of every locally-completable gap. Re-verified active
      authority first (HEAD 15dac59, clean tree, current.task: none, PLACE-037/038/039 all
      completed) before any file was touched. TWO REAL, PREVIOUSLY-UNDISCOVERED GAPS found and
      fixed: (1) REDIS_URL had no production-required Joi validation rule -- unlike
      DB_HOST/CORS_ALLOWED_ORIGINS, which already fail-fast in production, RedisService
      (apps/api/src/core/redis/redis.service.ts) reads only config.get('redis.url'), so a
      misconfigured production deploy missing REDIS_URL would have silently connected to the
      unauthenticated redis://localhost:6379 dev default instead of crashing at boot. Fixed in
      apps/api/src/core/config/env.validation.ts to mirror the exact DB_HOST
      required-in-production pattern; 3 new tests added to env.validation.spec.ts (pass-when-set,
      fail-fast-when-missing, dev-default-unaffected), and the pre-existing PLACE-029 DB-credential
      test suite's baseProdEnv fixture updated to include REDIS_URL (it would otherwise have newly
      failed under the stricter schema) -- full apps/api unit suite re-run clean afterward: 254/254
      across 34 suites (up from the 251/251 pre-PLACE-040 baseline by exactly the 3 new tests,
      zero regression). tsc --noEmit and eslint both exit 0; `nest build` produced dist/main.js
      cleanly. (2) scripts/deploy.sh's own Step 11 pointed at a "scripts/smoke-routes.sh" that was
      never actually committed anywhere in this repository -- a dangling reference, functionally
      an inert echo with no real check performed. Fixed by writing scripts/smoke-test.sh (new): a
      reusable, exit-code-driven script checking /api/health (200), the web home page (200), an
      unknown route (404 -- deliberately NOT /places/<bogus-slug>, which PLACE-035/036 already
      documented as having an unrelated, accepted 200-not-404 quirk), and an optional real-slug
      content check, run by default against Caddy's own documented local :8080 test address (
      identical routing to the real domain, no DNS/TLS required); scripts/deploy.sh Step 11 now
      calls it directly and halts with an explicit rollback instruction on failure. New
      docs/delivery/RELEASE-AND-ROLLBACK-CHECKLIST.md: an operator-facing, checkbox-style
      companion covering pre-release, deploy, smoke-test, rollback-trigger criteria, rollback,
      restore, routine backup, startup verification, and shutdown verification, each step naming
      the exact script/command -- distinct from and cross-linked to PLACE-039's
      PRE-DEPLOYMENT-CHECKLIST.md (which was updated with a REDIS_URL secrets-table row and a
      cross-link to this new document). Every OTHER audited category (Docker images, Docker
      Compose topology, reverse proxy, ports, health endpoints, PostgreSQL/migrations, JWT
      secrets, CORS, rate limiting, logging, backup/restore scripts, build artifacts) was found
      ALREADY correctly implemented by prior PLACE tasks (026/028/029/030/038) and left completely
      unchanged -- confirmed via `docker compose config --quiet` (exit 0, zero drift) rather than
      re-implemented. MinIO/object-storage integration, transactional email, and sitemap/robots.txt
      remain open, but as pre-existing, deliberately-approved deferrals (PLACE-036/037), not newly
      discovered defects -- re-confirmed still open, not newly closed. This session's Docker engine
      was again unreachable (same as PLACE-039's) -- the migration:revert rehearsal (PLACE-037 §12)
      remains carried forward, and scripts/smoke-test.sh/deploy.sh were syntax-checked (`sh -n`)
      only, never executed against a live stack. Repository-controlled production readiness scored
      approximately 92% (16 READY + 1 PARTIAL(monitoring, infra-native done/uptime-account pending)
      out of 18 scored items; the 19th, migration:revert, scored 0 as session-BLOCKED). Real
      go-live readiness explicitly NOT scored -- it depends entirely on 4 Owner-side real-world
      actions (Hostinger VPS, DNS, R2, uptime-monitor account) no repository evidence can resolve.
      Zero real secret value written anywhere. No PLACE-041 created or started.
    report: docs/delivery/reports/PLACE-040-production-configuration-inventory-report.md
    evidence_index: docs/delivery/evidence/PLACE-040-production-configuration-inventory-evidence-index.md
  - id: PLACE-039
    completed: "2026-07-25"
    result: >-
      Pre-deployment checklist (Hostinger VPS preflight + production secrets/environment go-live
      inventory), authorized by an Owner instruction that restated PLACE-037/038's 5 conditions
      (Hostinger KVM VPS pending real-account verification, ~1-2M VND/month budget,
      phuquochub.com/https://phuquochub.com/api same-domain path, email notification channel, no
      staging for release 1 as an explicit MVP decision) and asked for repository truth to be
      re-established before any closure or new work. Repository truth check found PLACE-037 and
      PLACE-038 ALREADY COMPLETED (HEAD 13539a1 at task start) with all 5 decisions already
      recorded and BYTE-FOR-BYTE IDENTICAL to the Owner's restatement -- no drift, nothing to
      reopen or re-close. New deliverable: docs/delivery/PRE-DEPLOYMENT-CHECKLIST.md, giving the
      Owner (a) exactly 8 non-sensitive Hostinger hPanel facts to confirm before any real deploy
      (plan name, status, OS, IPv4, region, CPU/RAM/storage, root-access flag, DNS-manager
      location), explicitly instructing never to share password/SSH key/API token, and (b) a
      production secrets/environment inventory naming every DB_PASSWORD/REDIS_PASSWORD/JWT_*/R2_*
      variable that currently holds a placeholder in .env.example/docker-compose.prod.yml and must
      be replaced with a real value before scripts/deploy.sh is ever run for real -- zero real
      secret value written anywhere. This session's Docker engine was found UNREACHABLE (`docker
      ps`/`docker version` failed to connect to npipe:////./pipe/dockerDesktopLinuxEngine; no
      Docker Desktop process running; no installation found at the standard path) -- a materially
      different local capability than this file's own verification_environment section (which
      reflects a prior session). The migration:revert rehearsal flagged open since PLACE-037 §12 /
      PLACE-038 evidence NX-5 was therefore NOT attempted here either, to avoid a false claim --
      explicitly carried forward in the new checklist §3 as still open, blocked on Docker
      availability, not on Owner input. Zero application code, Dockerfile, Compose file, or script
      touched -- PLACE-038 already implemented all of those correctly and nothing here required
      changing them. No PLACE-040 created or started.
    report: docs/delivery/reports/PLACE-039-pre-deployment-checklist-report.md
    evidence_index: docs/delivery/evidence/PLACE-039-pre-deployment-checklist-evidence-index.md
  - id: PLACE-038
    completed: "2026-07-25"
    result: >-
      Production readiness implementation (repository-controlled infrastructure preparation),
      implementing PLACE-037's Topology A (single VPS + Docker Compose + Caddy) under direct Owner
      authorization of all 5 PLACE-037 conditions -- external deployment explicitly NOT authorized.
      TWO REAL, PREVIOUSLY-UNDISCOVERED DEFECTS found and fixed, both surfaced only by this task's
      own required local build+boot verification: (1) NEXT_PUBLIC_API_URL/NEXT_PUBLIC_MAP_TILE_URL
      are baked in by Next.js at BUILD time; docker-compose.prod.yml previously set them only as a
      container-runtime environment: value, which had ZERO effect -- proven via direct compiled-
      bundle inspection (both client static chunks and server SSR chunks showed the old hardcoded
      default regardless of the runtime override). Fixed: apps/web/Dockerfile now accepts
      ARG/ENV NEXT_PUBLIC_API_URL/NEXT_PUBLIC_MAP_TILE_URL before next build; docker-compose.prod.yml
      passes them via build.args. Re-verified with an explicit --build-arg: phuquochub.com/api
      correctly present throughout both bundles, zero localhost:4000/api occurrences. (2)
      postgres's WAL-archiving `command:` crashed the container on every boot -- postgres: invalid
      argument: "/opt/wal-archive.sh" -- a pre-existing defect dating to PLACE-026 (YAML
      folded-scalar command: shell-word-splitting the multi-word -c archive_command value into
      invalid separate argv entries), never previously triggered because no prior task had
      actually booted this exact service through this exact compose file's code path end-to-end.
      Fixed by converting command: to YAML list form (no shell splitting involved at all);
      re-verified via a clean full-stack boot with postgres reporting healthy, zero crash-loop.
      Implemented: infrastructure/caddy/Caddyfile (new) -- routes phuquochub.com/api-> api,
      else-> web, automatic HTTPS, plus a documented local :8080 test address sharing the
      IDENTICAL routing logic (a supported Caddy multi-address pattern) for verification without
      real DNS/TLS -- verified via both `caddy validate` (Valid configuration) and live local
      requests through Caddy (both / and /api/health returned 200 with correct content). Removed
      direct host-port publishing for postgres/redis/minio/api/web (Topology A's own private-
      networking requirement -- confirmed via `docker compose config`: only caddy has a ports:
      block). Added a Redis password -- verified genuinely enforced live: unauthenticated
      redis-cli ping fails NOAUTH, authenticated succeeds PONG, API's RedisHealthIndicator
      confirmed reporting healthy through the authenticated connection. Added native Docker
      HEALTHCHECK instructions to both apps/api/Dockerfile and apps/web/Dockerfile -- verified
      both reporting `healthy` live after boot. Added --chown=node:node to apps/api/Dockerfile's
      runtime COPY lines defensively, matching PLACE-036's proven web fix, with no direct evidence
      of a runtime issue but none of safety either -- removes the open question at zero cost.
      Added a new `migrate` compose service (reuses the Dockerfile's intermediate build stage,
      which has the full TypeScript source + ts-node, unlike the lean runtime stage the api
      service uses) since postgres's port is no longer host-published, so migrations can no
      longer run via a bare host-side npm process -- verified live: all 20 migrations applied
      cleanly to a fresh, empty database (places=49, migrations=20 confirmed after). Wrote
      scripts/deploy.sh, backup.sh, restore.sh, rollback.sh, sync-offsite.sh. backup.sh and
      restore.sh were ACTUALLY EXECUTED (not merely syntax-checked) against disposable local test
      data: backup.sh produced a real, valid, non-empty gzip pg_dump; restore.sh (destructive,
      confirmation-gated) dropped and restored the database from it, with places=49/migrations=20
      confirmed identical afterward. rollback.sh was actually executed against two tagged images
      (swap + health re-verification both succeeded). sync-offsite.sh's safe no-credential exit
      path was exercised; it was never run against a real R2 endpoint (no credentials exist in
      this session -- exactly the documented, correct behavior). Web-to-API end-to-end data-
      fetching was verified with real seeded data flowing through Caddy -- and along the way, a
      genuine test-setup mistake was caught and corrected rather than silently producing a false-
      positive result: an initial attempt pointed the web image's build-time API URL at
      localhost:8080 assuming it would reach Caddy, but localhost inside a container is that
      container's own loopback, not another container -- this produced ECONNREFUSED in web's logs
      and a fallback (not real) page title; re-diagnosed, rebuilt web pointed at the api service
      by its correct internal Docker DNS name, and re-verified: the real seeded title
      ("Dinh Cậu · PhuQuocHub") rendered correctly, zero fetch errors. Full monorepo regression
      (lint/typecheck/unit web-17/api-251, e2e 59) run fresh after every change: zero application-
      source behavior change, identical totals to every prior PLACE task's baseline -- confirming
      the two defect fixes and all compose/Dockerfile/script changes touched only infrastructure
      configuration, never application logic. docs/architecture/deployment.md reconciled: the
      status header and the original 9-item Section-15 pre-production decision list are now marked
      resolved/deferred against what PLACE-037 (decisions) and PLACE-038 (implementation) actually
      closed, following this repository's own established discipline of keeping documented status
      honest against verified reality rather than aspirational. ZERO real infrastructure touched
      anywhere in this task -- no DNS record, no VPS, no Hostinger/Cloudflare/R2 connection, no
      public exposure of any kind; the entire local test stack (6 containers, 6 named volumes, 1
      network) was fully torn down and all disposable test images/tags/backup files removed after
      verification; the actual dev stack (phuquoc-postgres/-redis/-minio) was confirmed unaffected
      and healthy throughout. Not claimed: no real Hostinger/VPS provisioning; no real DNS for
      phuquochub.com; no real R2 connection or upload; scripts/deploy.sh not run end-to-end against
      a real environment (its constituent mechanisms were each verified individually instead,
      since deploy.sh assumes a real VPS checkout this repository cannot provide); no
      migration:revert rehearsal (carried forward as an open item); zero application
      feature/UI/business-logic/schema change. No PLACE-039 created or started.
    report: docs/delivery/reports/PLACE-038-production-readiness-implementation-report.md
    evidence_index: docs/delivery/evidence/PLACE-038-production-readiness-implementation-evidence-index.md
  - id: PLACE-037
    completed: "2026-07-25"
    result: >-
      Production deployment-target and monitoring-provider decision gate. GATE RESULT: READY WITH
      CONDITIONS. Decision/planning task only -- zero application code, package manifest, lockfile,
      Dockerfile, CI file, or environment file touched; no deployment, no cloud provisioning, no
      purchase, no DNS change, no monitoring integration, no SDK installed. Preflight: all 22
      required facts verified (repo state, PLACE-032..036 completion, current Next.js
      16.2.11/NestJS 11.1.28 baselines, toolchain versions, Docker/Compose versions, service health,
      existing Dockerfiles/Compose/CI/deployment-docs/monitoring-docs/env-templates/health-endpoint/
      logging-correlation-ID/rollback-docs). Architecture inventory grounded in direct evidence, not
      deployment.md's target design alone: independently confirmed MinIO/object storage exists in
      docker-compose.prod.yml but is NEVER called by application code (zero S3_*/@aws-sdk/minio
      reference anywhere in apps/api/src, env.validation.ts doesn't even validate S3 vars) --
      correctly classified deferrable, not mandatory, for initial launch; confirmed no BullMQ
      worker, no scheduled jobs, no email provider exist despite appearing in deployment.md's target
      diagram -- correctly excluded from required-services. Deployment readiness assessed area-by-
      area: Dockerfiles/Compose/CI/env-validation/migrations all production-ready or ready-with-
      configuration; two real gaps found and FLAGGED, not fixed (Dockerfile/Compose changes
      prohibited by this task's own constraints): Redis has no password in docker-compose.prod.yml;
      neither apps/api/Dockerfile nor apps/web/Dockerfile declares a native HEALTHCHECK instruction.
      apps/api/Dockerfile shares apps/web/Dockerfile's PLACE-036 COPY-without---chown pattern, but
      with NO evidence the API ever writes to its own filesystem at runtime (unlike Next.js's
      proven .next/cache write) -- explicitly flagged as an UNVERIFIED pre-launch check, not
      claimed as a defect absent evidence, honoring the discipline of not speculating without proof.
      5 deployment models compared against actual repository constraints (single VPS Compose / VPS+
      managed data / provider-native Node hosting / split-provider / current Hostinger account).
      HOSTINGER: classified UNKNOWN, explicitly NOT assumed compatible merely because an account
      exists (per this task's own instruction) -- the specific plan tier (shared hosting vs VPS/
      Docker/root access) is not established anywhere in repository evidence; this is the first of
      5 genuine Owner-side facts this repository cannot resolve. TOPOLOGY: two realistic options
      presented (single-VPS-full-stack vs VPS-apps+managed-data-services); recommended single VPS
      running the existing, already-verified docker-compose.prod.yml stack fronted by Caddy
      (automatic TLS) -- reuses infrastructure already built and repeatedly verified locally
      (PLACE-026 through PLACE-036), matches the project's actual zero-real-user current scale,
      keeps the exact rollback mechanism already rehearsed three times this session (PLACE-031,
      PLACE-032, PLACE-036), matching this task's own required priority order (reliability,
      rollback simplicity, operational burden, cost, scalability) over deployment.md's own more
      elaborate GD2/GD3 target design (correctly deferred, not skipped -- already the document's own
      stated roadmap). Resource sizing, backup policy (daily-7/weekly-4/monthly-6 retention,
      matching deployment.md's own pre-existing figures), migration strategy (grounded in the REAL
      current TypeORM setup: migrationsRun:false confirmed in database.module.ts:25, migrations run
      as a separate CI step already, not invented), and secrets strategy all designed as policy,
      none executed. OBSERVABILITY: existing capabilities inventoried (structured logs/correlation-
      ID/quiet-4xx/redaction/health-checks all IMPLEMENTED per PLACE-030; metrics/traces/dashboards/
      alerting/uptime-checks all ABSENT, unchanged from the pre-existing PRODUCTION-READINESS-
      ASSESSMENT-2026-07-24.md's own finding). 5-option MONITORING comparison (infra-native/Sentry/
      Grafana-stack/OpenTelemetry/lightweight-uptime) with a scored decision matrix (12 criteria,
      weighting explained: implementation/operational-effort/cost weighted highest given zero real
      traffic today) recommends infra-native (Docker healthchecks + existing structured/correlated
      logs) + one external uptime check as the launch-minimum stack -- zero new dependency, zero new
      infrastructure, directly leverages the already-built correlation-ID work; Sentry named as the
      correct FIRST upgrade once real users exist (best-scoped gap-filler for near-zero frontend
      error visibility, no new infra to operate); Grafana/Prometheus and OpenTelemetry explicitly
      deferred as disproportionate to current (zero) traffic, matching this task's own instruction
      not to assume a paid platform is required. Minimum-launch-observability classified item by
      item (uptime/API-health/web-health/disk-space/TLS-expiry = launch-blocking; DB/Redis/CPU/RAM/
      restart/5xx alerts = strongly recommended; frontend-error-reporting/log-aggregation/metrics-
      dashboard = post-launch follow-up; tracing = unnecessary). Full alert policy designed (10
      signals) with notification channel explicitly marked an OWNER DECISION, not invented, per
      instruction. Logging/retention policy designed (14-30 day recommendation; a REAL gap found and
      flagged: docker-compose.prod.yml currently has no logging: size/rotation block configured on
      any service). Health-check design: /api/health assessed sufficient for initial launch as a
      combined liveness+readiness signal; the gap versus deployment.md's target /health+/health/
      ready split explicitly NOT recommended to close preemptively (matches Docker-Compose's actual
      operational model, not Kubernetes's). Reverse-proxy/TLS: Caddy recommended over nginx
      specifically for automatic-TLS's lower single-operator operational risk. DNS: no production
      domain represented anywhere in repository evidence; domain naming explicitly marked an OWNER
      DECISION, not assumed. 15-step rollout strategy designed; simple replace-and-rollback
      explicitly selected over canary/blue-green as correctly matched to actual project stage (zero
      users), not the theoretical enterprise complexity this task's own instruction warns against.
      Rollback requirements enumerated element-by-element; independently-rollback-able elements
      (web/API images, proven separable in every PLACE-036 rehearsal) distinguished from the one
      genuine dependency-on-database-restore case (a hypothetical FUTURE destructive migration --
      none exist today, all 20 current migrations confirmed additive). 5 genuine OWNER DECISIONS
      inventoried (Hostinger plan, budget ceiling, domain, notification channel, offsite-backup
      destination) -- none reflect missing engineering work, none are normal engineering detail
      escalated unnecessarily. Recommended first-launch architecture stated with explicit separation
      of repository-evidence-based recommendation vs Owner-confirmation-required assumption. A
      future, separately-authorized implementation-task scope proposed (nginx/Caddy config, Compose
      logging/Redis-password/WAL-offsite-destination adjustments, Dockerfile HEALTHCHECK addition +
      api file-ownership verification following PLACE-036's own proven method, deploy/backup
      scripts, doc reconciliation, a REAL-environment rollback rehearsal, a migration:revert
      rehearsal) -- explicitly excluding product features/UI/business-logic/schema
      redesign/unrelated dependency upgrades/content population/broad performance work/production
      deployment itself -- but NOT created, NOT activated. Final scored decision matrix (11
      criteria, rollback-confidence and security-readiness weighted highest given this is a
      first-ever real launch handling real user data) -> gate result READY WITH CONDITIONS. Cross-
      referenced against the pre-existing PRODUCTION-READINESS-ASSESSMENT-2026-07-24.md ('E. NOT
      READY'): confirmed PLACE-026 through PLACE-036 have since resolved most of its findings
      (deployment artifacts now exist and are verified, rate-limiting/CORS added, DB-credential
      fail-fast closes its exact flagged permissive-default risk, AppLoggerService now wired) --
      monitoring and deployment-target selection remain exactly as open as that assessment found
      them, which is precisely what this gate now resolves with a concrete, evidence-based
      recommendation. Not claimed: no Hostinger plan verified; no monitoring vendor account created;
      no SDK installed; no infrastructure provisioned; no PLACE-038 created or started.
    report: docs/delivery/reports/PLACE-037-production-deployment-monitoring-decision-gate-2026-07-25.md
  - id: PLACE-036
    completed: "2026-07-25"
    result: >-
      Post-migration stabilization and independent review of PLACE-035's Next.js 16 migration.
      PLACE-035 evidence review: read all 4 documents in full (not summarized), independently
      re-verified every major claim fresh (versions via require.resolve(), diff scope via git,
      audit numbers via a fresh npm audit run, route behavior via live Docker checks) -- zero claim
      found contradictory, stale, or unsupported. Scope-compliance review of PLACE-035's 4 commits
      (17 files): every file classified as mandatory-migration-change, migration-evidence, or the
      one justified conditional change (a 1-line lint-forced comment) -- zero unauthorized,
      unrelated, or suspicious file. Dependency integrity: next/react/react-dom confirmed single
      physical copy fresh; the dual eslint copy (root 9.39.5 + nested 8.57.1 for 3 sub-plugins)
      reconfirmed benign; npm install --dry-run's 61 "would-add" packages traced to other-platform
      optional binaries (a known dry-run display quirk, not a real inconsistency); ONE genuine
      extraneous package found and explained -- @emnapi/runtime@1.10.0, orphaned from two
      wasm32-target optional packages correctly skipped on this win32-x64 machine, zero functional
      impact, deliberately NOT remediated (fixing it would require an npm install/prune action this
      task's own governance discourages running merely to tidy the tree). Configuration integrity:
      no legacy .eslintrc remains, turbo.json/CI need no update, Node/Docker engine versions fully
      coherent. Source compatibility: zero usage of cookies()/headers()/draftMode() (category
      inapplicable -- localStorage-only sessions by design), no middleware/route handlers, next/image
      confirmed genuinely UNUSED anywhere (both prior grep hits were inside eslint-disable comment
      text, not real imports). Fresh verification (forced, 0 cached): lint 6/6, typecheck 6/6, unit
      web 17/17 + api 251/251 + utils 4/4, e2e 59/59 -- all match or exceed PLACE-035's claims, zero
      decrease. Repeated builds: attempt 1 succeeded but took 26.1min because THIS TASK ran a
      concurrent npm audit by mistake (the same class of error PLACE-035 itself documented);
      caught, re-ran isolated -- 27s clean, identical 15-route set both times, zero nondeterministic
      failure. Fresh Docker rebuild (phuquochub-web:place036-stabilization): pre-fix image was
      BYTE-IDENTICAL (same digest) to PLACE-035's own place035-next16 image, proving true
      reproducibility. ONE REAL MIGRATION-CAUSED DEFECT FOUND during runtime verification, NOT
      caught by PLACE-035: EACCES: permission denied on .next/cache mkdir + an unhandled promise
      rejection on the very first request, because apps/web/Dockerfile's runtime COPY commands left
      .next root-owned while the container runs as non-root `node`. PROVEN migration-caused (not a
      pre-existing latent bug) via a direct A/B test: booted the retained
      phuquochub-web:place035-next14-baseline image (identical root-owned .next) under the exact
      same request sequence -- zero EACCES, zero unhandled rejection, proving Next.js 16's runtime
      newly attempts this cache write while Next 14 never did under these conditions. FIXED: added
      --chown=node:node to the 3 runtime-stage COPY --from=build lines (Next.js's own documented
      Docker pattern) -- the only application/infra file this task modified. Rebuilt; re-verified
      .next now owned by node:node and writable; zero EACCES; full route inventory (all
      static/index routes, all 5 dynamic [slug] routes including a byte-identical reproduction of
      the pre-existing /places/[slug] 200-not-404 quirk, SEO metadata, static assets, the
      image-optimization endpoint's correct 400 for an unconfigured/nonexistent source) re-verified
      unchanged otherwise. Regression coverage for this Docker-level fix: not Jest-testable (non-root
      container filesystem ownership is outside Jest's execution model); the before/after/control
      Docker-level A/B demonstration itself IS the regression evidence. Security stabilization: fresh
      npm audit --omit=dev = 8 total, identical 8 packages to PLACE-035, zero drift, zero new
      finding; zero secret/credential/env leakage found in the built image or logs (only the
      by-design public NEXT_PUBLIC_API_URL default is baked into the client bundle). Rollback
      viability recheck: full destructive N16->N14->N16 rehearsal NOT repeated (not required --
      PLACE-035's evidence found complete, fresh, reproducible); instead independently re-booted the
      retained Next-14 baseline image as this task's own A/B control, confirming it still serves
      real data correctly; git can cleanly reach the pre-migration commit; zero DB schema change.
      DECISION: STABILIZED WITH BOUNDED FOLLOW-UPS. Remaining open items (next/postcss/sharp audit
      findings pending upstream fix, the @emnapi/runtime extraneous leftover, the pre-existing
      /places/[slug] quirk, missing hotels/restaurants/tours/events seed data, redis.health.ts's
      deprecated pattern, missing sitemap/robots) are all low-risk, none block treating Next.js 16
      as the trusted baseline. Baseline declaration written. Verification containers/disposable API
      image cleaned up; dev stack confirmed healthy throughout. Not claimed: no real deployment; the
      next/postcss/sharp findings not closed; the pre-existing quirk and missing seed data not
      fixed; zero apps/api or packages/* file touched. No PLACE-037 created or started.
    report: docs/delivery/reports/PLACE-036-nextjs-16-stabilization-report.md
    evidence_index: docs/delivery/evidence/PLACE-036-nextjs-16-stabilization-evidence-index.md
    baseline_declaration: docs/delivery/reports/PLACE-036-NEXTJS-16-BASELINE-DECLARATION-2026-07-25.md
  - id: PLACE-035
    completed: "2026-07-25"
    result: >-
      Next.js 14.2.35 -> 16.2.11 controlled migration execution, resolving the three open Owner
      decisions from the PLACE-034 decision gate (timing = now; ESLint flat-config migration
      bundled into this task; fetch-caching behavior = accept Next 16 defaults as-is). Bumped
      exactly 3 approved packages (next, eslint-config-next -> 16.2.11; eslint -> 9.39.5);
      apps/web/package.json diff is exactly those 3 lines plus the lint script (next lint -> eslint
      . --max-warnings=0, forced by next lint's complete removal in Next 16, not mere deprecation).
      Zero apps/api or packages/* file touched; react/react-dom/typescript confirmed unchanged;
      next/react single-copy dependency tree confirmed; eslint resolves to two physical copies
      (root 9.39.5 which actually runs, plus a nested 8.57.1 used only by 3 of eslint-config-next's
      own sub-plugins whose peer ranges don't yet include eslint 9) -- normal npm resolution for the
      eslint 8->9 ecosystem transition, not an ERESOLVE error, verified functionally clean. Legacy
      .eslintrc.json replaced with a native flat-config eslint.config.mjs (eslint-config-next@16
      ships eslint-config-next/core-web-vitals as a flat array natively, no FlatCompat wrapper
      needed) -- one real lint delta surfaced (react-hooks/set-state-in-effect, a new rule from the
      bundled eslint-plugin-react-hooks@7.1.1), fixed with a single targeted, documented
      eslint-disable-next-line comment in AuthProvider.tsx rather than an out-of-scope
      rearchitecture. All 5 dynamic-route files (events/hotels/places/restaurants/tours
      [slug]/page.tsx) converted params from sync to Promise<{slug:string}> + await, in both
      generateMetadata and the page component -- purely mechanical, zero logic change, confirmed
      via grep (zero stale params.slug references) and tsc. TURBOPACK: an initial build showed
      alarming ~25-minute static-generation timing with repeated 60s-timeout retries; investigated
      rather than accepted -- reproduced as self-inflicted resource contention from this task's own
      concurrent Docker/audit work, NOT a Turbopack defect; an isolated re-run completed in 31s
      (1.7s static generation), zero retries, confirming full compatibility, no --webpack fallback
      needed. SECURITY AUDIT: a CORRECTED EXPECTATION from PLACE-032/034 -- the assumption that this
      migration would close the next/postcss findings was FALSE, caught by re-tracing each finding's
      via/range/nodes fields rather than trusting the prior claim: next's own advisory range already
      extended through a near-current unreleased canary/preview boundary at the Next-14 baseline too
      (no stable release, 14.x or 16.x, is marked fixed); postcss's finding (not a direct dependency
      anywhere in this repo, purely next's own transitive choice) carried the identical 3 CVEs/range
      before and after. One genuinely NEW finding, sharp, is fully explained -- next@16.2.11 itself
      declares sharp@^0.34.5 as a new optionalDependency absent from next@14.2.35. Net: npm audit
      --omit=dev 7 -> 8, +1, fully explained, zero unapproved package installed to chase a cleaner
      number. Full regression: web unit 17/17 (3 suites) + api unit 251/251 (34 suites) + api e2e
      59/59 (10 suites), ALL IDENTICAL to baseline -- zero test added, skipped, or weakened; web
      build 15 routes both before/after; api build (nest build) unaffected. Docker: baseline
      (phuquochub-web:place035-next14-baseline) and migrated (phuquochub-web:place035-next16) images
      both built, tagged, retained (the migrated image's first build attempt hit a transient npm ci
      EIDLETIMEOUT registry timeout, non-reproducible on retry). Live-verified via a support API
      container (unmodified NestJS 11 source, real dev Postgres/Redis) sharing the web container's
      network namespace: all static routes 200 on both images; unknown route 404; /places/dinh-cau
      (the only seeded entity type -- hotels/restaurants/tours/events have zero dev-DB rows, a
      pre-existing gap) renders real data byte-identically including SEO metadata (og:*/twitter:*/
      canonical); the other 4 dynamic routes correctly 404 on a nonexistent slug on both images; one
      PRE-EXISTING baseline quirk (/places/nonexistent-slug returns 200 not 404, traced to a custom
      not-found.tsx + conditional notFound() call, correct UI/fallback-title still render) reproduced
      BYTE-IDENTICALLY post-migration -- confirmed unrelated to this migration, deliberately left
      unfixed as out of this task's authorized scope. Full N16->N14-baseline->N16 rollback rehearsal
      executed: rollback verified, forward recovery verified identically; ZERO DATABASE INVOLVEMENT
      proven -- places=49, migrations=20 unchanged at every checkpoint. Verification
      containers/disposable API image removed afterward; both web images retained per the rollback
      plan; dev stack confirmed healthy throughout. Not claimed: no real deployment; no React 19,
      Node.js, or TypeScript upgrade; the /places/[slug] not-found quirk not fixed (pre-existing,
      out of scope); hotels/restaurants/tours/events dynamic routes not verified with real data (no
      seed data exists); next/postcss/sharp audit findings not closed (blocked on upstream). No
      PLACE-036 created or started.
    report: docs/delivery/reports/PLACE-035-nextjs-16-migration-report.md
    evidence_index: docs/delivery/evidence/PLACE-035-nextjs-16-migration-evidence-index.md
    migration_assessment: docs/delivery/reports/PLACE-035-NEXTJS-16-MIGRATION-ASSESSMENT-2026-07-25.md
  - id: PLACE-032
    completed: "2026-07-24"
    result: >-
      NestJS 10 -> 11 migration, executing the PLACE-032 read-only migration-readiness assessment's
      APPROVE PLACE-032 EXECUTION recommendation (overall risk 2/10, zero peer conflicts confirmed
      via npm install --dry-run). Owner timing decision: execute now, before staging. Bumped exactly
      the 10 approved packages (@nestjs/common/core/platform-express/config/jwt/typeorm/terminus,
      plus dev @nestjs/testing/cli/schematics) to their exact approved NestJS-11-line versions;
      @nestjs/throttler naturally settled at an in-range 6.5.0, explicitly permitted. Every "must
      remain unchanged" package (typeorm, reflect-metadata, rxjs, class-validator,
      class-transformer, bcrypt, jest, supertest, typescript, Node version) confirmed unchanged;
      ioredis/pg/joi drifted to newer patch versions strictly within their pre-existing, untouched
      package.json ranges (harmless lockfile-regeneration side effect, not a new upgrade).
      apps/api/package.json diff is exactly the 10 approved lines; apps/web/package.json has zero
      diff. A REAL INSTALLATION DEFECT was found and fixed during verification: the first
      (incremental) npm install left @nestjs/config/jwt/throttler resolving a stale nested
      @nestjs/common@10.4.22 copy alongside the new top-level 11.1.28 -- confirmed via
      require.resolve() from within throttler's own resolution context, not merely npm ls output.
      npm dedupe (the standard non-destructive fix) failed with an ERESOLVE error. Root-caused to
      npm's incremental install preserving a lockfile-encoded stale subtree; fixed via a full clean
      reinstall (node_modules + package-lock.json deleted, npm install regenerated both from
      package.json alone) -- verified afterward: a single @nestjs/common@11.1.28 throughout the
      entire tree, all 8 dependent packages deduped to it. apps/api/src/modules/health/indicators/
      redis.health.ts (extends the now-deprecated HealthIndicator/HealthCheckError) was inspected
      post-bump: compiled cleanly (tsc, eslint both exit 0) and functioned correctly at runtime
      (health.controller.spec.ts 2/2, health.e2e-spec.ts 1/1 against real Redis) -- left UNCHANGED
      per the task's explicit decision rule (do not refactor a working, merely-deprecated pattern
      during a framework migration); the deprecation is recorded as a bounded follow-up, not fixed
      here. npm audit --omit=dev: 15 -> 2 total (13 @nestjs/*-rooted findings closed -- every direct
      @nestjs/* package plus its transitive chain of body-parser/express-4.x/qs/uuid/lodash/multer/
      file-type; the 2 remaining, next and postcss, are entirely Next.js-rooted and were never in
      scope). Full regression: unit 251/251 (34 suites) and e2e 59/59 (10 suites), BOTH IDENTICAL to
      the PLACE-031 baseline -- zero test added, skipped, or weakened, proving no observable
      behavior changed anywhere the existing suite has coverage. Clean build 4/4, 0 cached, both
      before and after. Docker-verified live on a freshly-built NestJS 11 image against the real dev
      Postgres/Redis: health 200 db+redis up; full register(201)->login-correct(200)->
      login-wrong(401) round-trip; protected route without token 401; correlation ID explicitly
      confirmed byte-identical between response header and meta.requestId via a separate string
      comparison (not just visual inspection); structured [HTTP] logs present for every request with
      correct correlation IDs; zero ERROR-level log lines for the 401s (quiet-4xx guarantee intact);
      a full log-history grep for the real test password found zero matches; rate limiting unaffected
      (8x401 then 429x3 on repeated bad-password attempts); DB_PASSWORD missing/empty and
      CORS_ALLOWED_ORIGINS missing all three still fail fast with the expected named error (one
      cosmetic-only NestJS-11 stack-trace formatting difference noted, no functional change); web
      image rebuilt (no dependency changed) and confirmed unaffected, GET / -> 200. Full
      N11->N10->N11 rollback rehearsal executed and evidenced: N11 booted+verified; a clearly-
      identifiable temp user created; N11 stopped, N10-baseline (retained from Phase 1) redeployed;
      health re-verified; DATA CONTINUITY explicitly proven -- the user created under N11 logged in
      successfully against N10; auth behavior re-confirmed (wrong password still 401); row counts
      unchanged throughout (migrations=20, places=49); N10 stopped, N11 redeployed once more,
      forward recovery proven (health 200, same user login 200). All temp users/containers/images
      removed afterward; dev stack confirmed healthy and row counts identical to the original
      pre-task baseline; zero migration or schema file touched. Not claimed: no
      HealthIndicatorService rewrite of redis.health.ts (deferred, working as-is); no Next.js
      migration; no apps/web change of any kind; the remaining 2 production dependency findings
      (next, postcss) not closed (out of scope). No PLACE-033 created or started.
    report: docs/delivery/reports/PLACE-032-nestjs-11-migration-report.md
    evidence_index: docs/delivery/evidence/PLACE-032-nestjs-11-migration-evidence-index.md
    migration_assessment: docs/delivery/reports/PLACE-032-NESTJS-11-MIGRATION-ASSESSMENT-2026-07-24.md
  - id: PLACE-031
    completed: "2026-07-24"
    result: >-
      Release & rollback readiness (fresh PLACE-031 candidate assessment, Candidate E, weighted
      score 5.77/10, RECOMMENDED FOR PLACE-031 — the highest-scoring candidate with zero Owner
      dependency; Candidate B, NestJS 10->11, scored higher at 6.06/10 but was reported separately
      as HIGHEST STRATEGIC PRIORITY -- BLOCKED BY OWNER INPUT since its migration timing was never
      separately decided). Closed two gaps named as still-open in the production-readiness
      reassessment and untouched by PLACE-026 through PLACE-030: no release/rollback runbook
      existed (ENVIRONMENT-SETUP-RUNBOOK.md is a dev-machine environment-relocation guide, confirmed
      by full read to have zero overlap), and no rollback had ever been rehearsed, not even
      locally. Wrote docs/delivery/RELEASE-ROLLBACK-RUNBOOK.md, grounded in the rollback mechanism
      already designed in docs/architecture/deployment.md §9 (redeploy the previous immutable image
      tag) -- documented, not invented. Executed a real local rollback rehearsal against the actual
      dev Postgres/Redis: built phuquochub-api:release-N (current HEAD) and booted it (health 200,
      full auth round-trip register->login->wrong-password 201/200/401, structured [HTTP] logs with
      correct correlation IDs); built phuquochub-api:release-N1 (a simulated next release from the
      same reviewed commit) and booted it (health 200); executed the ROLLBACK (stopped N1, redeployed
      N); re-verified health 200; and proved DATA CONTINUITY -- a user created while N1 was running
      remained fully queryable and able to log in after the rollback to N, confirming application
      state lives in Postgres/Redis, not the container. PLACE-029's DB-credential fail-fast
      (DB_PASSWORD unset -> immediate crash) was re-confirmed live after the full rehearsal cycle,
      unaffected. Zero apps/api, apps/web, or packages/* file was touched -- confirmed by full
      regression: unit 251/251 (34 suites) and e2e 59/59 (10 suites), BOTH IDENTICAL to the
      PLACE-030 baseline, plus a clean 4/4 production build. Dev-stack integrity confirmed
      unchanged (migrations=20, places=49, both before and after); the one rehearsal-created test
      user was deleted, both rehearsal images and the container removed. Not claimed: no real
      staging/production rollback was performed (no target host exists in this repository/session);
      no blue-green/zero-downtime cutover; no CI/CD-automated rollback wiring; no
      docker-compose.prod.yml modification (deliberately avoided -- the rehearsal used plain docker
      build/run with distinct tags instead). No PLACE-032 created or started.
    report: docs/delivery/reports/PLACE-031-release-rollback-readiness-report.md
    evidence_index: docs/delivery/evidence/PLACE-031-release-rollback-readiness-evidence-index.md
  - id: PLACE-030
    completed: "2026-07-24"
    result: >-
      Production observability foundation (PLACE-029 Candidate Selection report, Candidate D —
      logging sub-scope only; the monitoring-provider sub-scope remains a separate Owner decision,
      OD2-6, not started here). Preflight (fresh, not memory) confirmed AppLoggerService was fully
      built but never wired (grep useLogger -> zero hits, TD-03 exactly as diagnosed), no correlation-
      ID mechanism existed anywhere, and ApiMeta.requestId already existed in the wire contract
      (shared-types + openapi.yaml) but was never populated. Implemented: a new
      correlation-id.middleware.ts (X-Request-Id header, accepts a valid incoming value or generates
      via crypto.randomUUID() -- Node built-in, zero new dependency; state lives on the per-request
      Express Request object, not module-level global state); app.useLogger(await
      app.resolve(AppLoggerService)) in main.ts with bufferLogs:true, completing TD-03; LoggingInterceptor
      and AllExceptionsFilter given an OPTIONAL LoggerService constructor parameter (default
      new AppLoggerService()) specifically so the 9 pre-existing e2e spec files that construct them
      with zero arguments needed NO changes -- confirmed all 9 pass unmodified; structured
      (not plain-string) request-lifecycle and exception logs, both carrying the same correlation ID
      as the response; meta.requestId populated in every response body; AppLoggerService's
      REDACTED_KEYS expanded to cover this repo's actual snake_case wire format (access_token,
      refresh_token, password_hash -- the prior camelCase-only list would never have matched real
      payloads) plus cookie/DB-credential/Redis-credential keys, with matching made case-insensitive.
      TWO REAL RUNTIME BUGS were found and fixed via live Docker verification (neither could have
      been caught by the unit/e2e suite, since neither exercises main.ts's actual bootstrap()): (1)
      app.get(AppLoggerService) threw at runtime because the provider is Scope.TRANSIENT -- fixed
      with app.resolve() (async); (2) after fixing (1), AppLoggerService's own internal `delegate =
      new Logger()` caused infinite recursion once app.useLogger() made Logger.staticInstanceRef
      point back at the AppLoggerService instance itself -- fixed by changing the delegate to
      ConsoleLogger (the concrete, non-overridable implementation class), preserving identical visual
      log output. Both fixes were verified by re-running the FULL affected verification chain
      (typecheck -> full unit -> full e2e -> clean build -> Docker rebuild -> Docker reboot) from
      scratch, not merely the failing step. Full regression: unit 251/251 (231 baseline + 20 new, 34
      suites), e2e 59/59 (51 baseline + 8 new, 10 suites) -- zero regression, all 9 pre-existing e2e
      files untouched. Docker images (api+web) rebuilt and booted live against the real dev
      Postgres/Redis with NODE_ENV=production: a real register->login(200)->login-wrong-password(401)
      flow proved bcrypt/auth unaffected; the response X-Request-Id header and body meta.requestId
      matched exactly for both a generated and a client-supplied ID; container logs showed structured
      per-request log lines with the correct correlation ID each time; the 401 produced zero
      ERROR-level log lines (quiet-business-error behavior preserved); a full grep of the container's
      log history for the real test password and both issued JWT tokens found zero matches; PLACE-029's
      DB-credential fail-fast (missing and empty-string DB_PASSWORD) re-verified live, unaffected by
      this task's main.ts changes; web image rebuilt per convention, booted, GET / -> 200 (zero
      apps/web file touched). Dev stack confirmed healthy and unaffected throughout (migrations=20,
      places=49 unchanged); all live-verification test users deleted afterward; verification
      containers/images removed. Not claimed: no live Docker proof of the unexpected-500 exception
      path (no debug route exists in the shipped image by design; proven instead at the e2e level via
      a controller that exists only inside the test file's own compiled module, never registered in
      production app.module.ts); no CORS allowedHeaders/exposedHeaders change (deliberate scope
      boundary -- the header is present on the wire regardless); no NestJS/Next migration; no paid
      observability vendor integration. No PLACE-031 created or started.
    report: docs/delivery/reports/PLACE-030-observability-foundation-report.md
    evidence_index: docs/delivery/evidence/PLACE-030-observability-foundation-evidence-index.md
    observability_assessment: docs/delivery/reports/PLACE-030-OBSERVABILITY-ASSESSMENT-2026-07-24.md
  - id: PLACE-029
    completed: "2026-07-24"
    result: >-
      Critical dependency hardening (PLACE-029 Candidate Selection report, Candidate A) — the only
      candidate requiring no further Owner decision. bcrypt 5.1.1 -> 6.0.0: a clean, isolated
      major-version bump (apps/api/package.json, one line; only dependency touched) that removes the
      entire @mapbox/node-pre-gyp/tar install-tooling chain outright, confirmed by `npm ls tar --all`
      and `npm ls @mapbox/node-pre-gyp --all` both returning empty tree-wide after `npm install`
      (added 1 package, removed 31, no --force, no peer bypass). bcrypt's public API (hash/compare)
      has no breaking change between 5.x and 6.x (confirmed via the upstream CHANGELOG); the two call
      sites in auth.service.ts were left untouched. Docker/musl compatibility investigated up front:
      apps/api/Dockerfile already carries a python3/make/g++ compile fallback for musl (added for
      bcrypt 5); in practice this task's full uncached Docker build log shows bcrypt 6's
      prebuildify-shipped binary loading directly with zero compile step. env.validation.ts:
      DB_HOST/DB_USER/DB_PASSWORD/DB_NAME changed from Joi .default() to
      when('NODE_ENV',{is:'production',then:Joi.required()}), the identical pattern already proven
      live by CORS_ALLOWED_ORIGINS (PLACE-028); DB_PASSWORD's .allow('') was removed from the
      production branch, closing the empty-string gap. 8 new focused unit specs
      (env.validation.spec.ts) pin the schema directly. Fresh npm audit --omit=dev: 18 -> 15 total
      (1 critical -> 0, 7 high -> 5, 10 moderate unchanged, 0 low unchanged) — tar/node-pre-gyp/bcrypt
      itself all gone from the audit output, not mitigated. All 15 remaining findings require a
      NestJS 10->11 or Next.js 14->16 migration (neither performed; both remain Owner decisions,
      recorded recommendation "before public launch" in the Candidate Selection report). Full
      regression: unit 231/231 (223 baseline + 8 new, 32 suites), e2e 51/51 (9 suites, identical to
      PLACE-028 baseline) — zero regression. Docker images (api+web) rebuilt and booted live against
      the real dev Postgres/Redis with NODE_ENV=production: a real register -> login (correct
      password, 200) -> login (wrong password, 401) round-trip proved bcrypt.hash/bcrypt.compare
      function correctly at runtime in the new native-binding loading path; two separate boots (
      DB_PASSWORD unset, and DB_PASSWORD='') both crashed immediately with the expected named Joi
      error, before any DB connection attempt. /api/health confirmed database=up, redis=up (PONG)
      throughout. Web image booted, GET / -> 200. Dev stack (phuquoc-postgres/-redis/-minio) confirmed
      healthy and unaffected before/during/after all verification (migrations=20, places=49, both
      unchanged); the one test user created during the live auth round-trip was deleted afterward
      (0 matching rows confirmed). Verification containers and images removed after use. Not claimed:
      no NestJS/Next major-version migration (both remain Owner decisions); the remaining 15
      production dependency findings are unchanged in count or character; no external infrastructure
      touched; no API contract, schema, or product feature change. No PLACE-030 created or started.
    report: docs/delivery/reports/PLACE-029-critical-dependency-hardening-report.md
    evidence_index: docs/delivery/evidence/PLACE-029-critical-dependency-hardening-evidence-index.md
    security_assessment: docs/delivery/reports/PLACE-029-SECURITY-ASSESSMENT-2026-07-24.md
  - id: PLACE-028
    completed: "2026-07-24"
    result: >-
      API bootstrap hardening (Eligible PLACE Candidate 3), implementing OD2-12 (rate limiting) and
      OD2-13 (CORS) — the last of the three Eligible Candidates from the Owner Approval Session.
      Before this task: apps/api/src/main.ts had `enableCors({origin:true, credentials:true})` (fully
      permissive) and NO rate limiting anywhere. Investigation (fresh, not memory) confirmed
      `@nestjs/throttler@6.4.0`'s peerDependencies (`@nestjs/core`/`@nestjs/common: ^7..^11`) are
      compatible with the installed `^10.4.4` — no NestJS major bump required, honoring PLACE-027's
      deferred-migration boundary. A repo-wide grep for `credentials|document.cookie|Set-Cookie|
      res.cookie` found only the CORS config line itself — zero cookie usage anywhere, confirming
      bearer-token-only auth and justifying CORS_CREDENTIALS defaulting to false. Implemented: a new
      RateLimitModule (global APP_GUARD, ThrottlerModule.forRootAsync, env-configurable
      RATE_LIMIT_TTL/RATE_LIMIT_LIMIT, default 100 req/60s); a stricter @Throttle() override on
      /api/auth/login and /api/auth/register (RATE_LIMIT_AUTH_TTL/RATE_LIMIT_AUTH_LIMIT, default 10
      req/60s — an evidenced brute-force/spam mitigation); @SkipThrottle() on /api/health (container
      liveness/readiness probe, must never be limited); an explicit CORS allow-list via
      CORS_ALLOWED_ORIGINS (comma-separated), REQUIRED and fail-fast at boot when NODE_ENV=production
      (Joi conditional schema, mirroring the existing JWT-secret precedent), defaulting to
      http://localhost:3000 in dev; explicit methods/allowedHeaders (GET/POST/PATCH/DELETE/OPTIONS,
      Content-Type/Authorization — grep-verified, no PUT exists); TRUST_PROXY_HOPS (default 0, since
      no real reverse proxy is deployed yet — confirmed via PLACE-026's own findings). Two new focused
      test files: rate-limit.spec.ts (isolated ThrottlerGuard mechanism, deliberately low limit=3,
      2 tests) and security-hardening.e2e-spec.ts (CORS allow/reject/credentials/preflight/no-origin
      + real /api/auth/login 429 at the default RATE_LIMIT_AUTH_LIMIT=10 + /api/health never
      throttled, 7 tests). Full regression: unit 223/223 (221 baseline + 2 new, 31 suites), e2e 51/51
      (44 baseline + 7 new, 9 suites) — zero regression to auth/authz/validation/response-contract
      behaviour. Docker image rebuilt and booted against the REAL dev Postgres/Redis containers with
      NODE_ENV=production (proving the new fail-fast CORS requirement is satisfiable): live curl
      proofs — CORS allowed origin reflected, disallowed origin not reflected, OPTIONS preflight
      returns 204 with the exact configured methods/headers, and (with a RATE_LIMIT_AUTH_LIMIT=5 test
      override) 5 login requests succeed (401, bad credentials) then the 6th returns 429, while
      /api/health stays 200 throughout. CI's docker-build job's API boot step was given
      CORS_ALLOWED_ORIGINS=http://localhost:3000 — without this the job would have started failing
      fast under the new production requirement. docker-compose.prod.yml's api service env block
      extended with all seven new variables, defaulting to the stack's own local web origin (NOT a
      real domain — none has been provisioned, per PLACE-026). Verified `docker compose -p
      phuquochub-place028-verify -f docker-compose.prod.yml config` resolves cleanly under an
      isolated project name; the dev stack (phuquoc-postgres/-redis/-minio) was confirmed healthy and
      completely untouched before and after all Docker verification, with no stray verification
      containers or volumes left behind. Not claimed: no NestJS/Next major-version migration (remains
      deferred per PLACE-027); no distributed/Redis-backed rate limiter (single-instance in-memory,
      limitation documented in docs/architecture/deployment.md); no real production CORS domain
      provisioned. All three Eligible PLACE Candidates are now implemented (Candidate 2 partially, per
      its own explicit narrower scope). No PLACE-029 defined or started.
    report: docs/delivery/reports/PLACE-028-api-bootstrap-hardening-report.md
    evidence_index: docs/delivery/evidence/PLACE-028-api-bootstrap-hardening-evidence-index.md
  - id: PLACE-027
    completed: "2026-07-24"
    result: >-
      Dependency security remediation (Eligible PLACE Candidate 2 / OD2-11), safe-only subset per
      this task's explicit narrower authorization (OD2-11's own recommendation favored a full
      major migration; this task's instruction was to implement ONLY safe patch/minor upgrades and
      stop-and-report on anything major — followed exactly). Fresh evidence (not memory): npm audit
      --omit=dev BEFORE = 17 total (1 critical, 6 high, 10 moderate, 0 low); incl. dev BEFORE = 33
      total (1 critical, 13 high, 16 moderate, 3 low). `npm audit fix` (non-force) applied EXACTLY
      ONE change: fast-uri 3.1.3->3.1.4 (dev-only transitive, zero runtime exposure) — confirmed via
      `git diff --stat` that ZERO package.json files (root or any workspace) were touched, only
      package-lock.json. AFTER: prod totals UNCHANGED at 17 (1/6/10/0); incl.-dev total 32 (one fewer
      than before). Every remaining vulnerability traced to a coordinated major-version requirement:
      `npm outdated` confirms Wanted==Current for every affected NestJS package (10.4.22/3.3.0 are
      already the newest releases in their major line) and for next (14.2.35) — patched versions
      exist only in NestJS 11.x / Next.js 16.x. `tar` (critical, transitive via bcrypt->
      @mapbox/node-pre-gyp) also genuinely needs a 6.x->7.x major bump; npm's own non-force resolver
      declined to apply it, confirming no safe path exists without accepting a breaking change. All
      12 major-gated packages + 20 pass-through transitives classified and explained (5-category
      scheme: 1 safe patch applied, 0 safe minor, 12 major-review, 20 deferred, 0 accepted-risk).
      Full lint/typecheck (api+web) + unit 221/221 + e2e 44/44 IDENTICAL to PLACE-026's baseline —
      zero regression. Docker images rebuilt with the updated lockfile and re-verified for real:
      api /api/health 200 (database=up, redis=up) against the actual dev Postgres/Redis; web GET /
      200. No new library introduced, no package removed, no feature/API/schema/auth change. The
      NestJS 10->11 / Next 14->16 major-version migration remains a PENDING decision, not performed.
    report: docs/delivery/reports/PLACE-027-dependency-security-report.md
    evidence_index: docs/delivery/evidence/PLACE-027-dependency-security-evidence-index.md
  - id: PLACE-026
    completed: "2026-07-24"
    result: >-
      Production deployment pipeline (Eligible PLACE Candidate 1 / Execution Group A) implemented
      for the repository-supported subset of OD2-2,3,4,5,7,8,9. apps/api/Dockerfile and
      apps/web/Dockerfile (multi-stage, Next.js standalone output) BOTH BUILT and BOOTED for real
      against the actual dev Postgres/Redis containers: /api/health returned 200 with database=up,
      redis=up (PONG); web GET / returned 200 including a real static asset. WAL/PITR archiving
      (OD2-4) verified LIVE in an isolated postgres container running the exact -c flags from
      docker-compose.prod.yml: archive_mode=on, wal_level=replica, and a forced pg_switch_wal()
      produced 4 real archived WAL segment files. .github/workflows/ci.yml gained a docker-build job
      reproducing this same build+boot+health-check sequence automatically, plus a GHCR push step
      (OD2-7) using the auto-provided GITHUB_TOKEN — EXPLICITLY documented as unverified by a live
      Actions run, since this repository has no git remote in this session. Map tile provider (OD2-8)
      made env-configurable (NEXT_PUBLIC_MAP_TILE_URL) with the default UNCHANGED from the existing
      OSM URL. No real VPS, Cloudflare R2 bucket, offsite backup target, or MapTiler account was
      provisioned or fabricated — all remain owner-supplied follow-ups. An incident occurred during
      verification: testing docker-compose.prod.yml without an explicit project name caused Compose
      to remove the running dev phuquoc-postgres container (shared default project name with the
      existing docker-compose.yml); the underlying data volume was untouched, the container was
      recreated, and data integrity was verified intact (20 migrations, 49 places, 6 extensions)
      before continuing — recorded in the report so it isn't repeated. Full lint/typecheck (api+web)
      + unit (221/221) + e2e (44/44) IDENTICAL to PLACE-025's baseline, proving zero regression;
      clean build 4/4, artifacts 153==153. Eligible Candidates 2 and 3 remain APPROVED, NOT activated.
    report: docs/delivery/reports/PLACE-026-deployment-pipeline-report.md
    evidence_index: docs/delivery/evidence/PLACE-026-deployment-pipeline-evidence-index.md
  - id: PLACE-025
    completed: "2026-07-24"
    result: >-
      GAP-15 RESOLVED. OD-B7/B7-A documentation/governance reconciliation ONLY — git diff limited to
      docs/99-decisions/ADR-013-prisma-readiness.md, docs/data/database.md, docs/delivery/**; ZERO
      code, schema, migration, entity, repository, service, SQL, or API contract change. Evidence-based
      investigation confirmed: (1) TypeORM entities+migrations+PostgreSQL are the SOLE runtime
      persistence layer (database.module.ts TypeOrmModule.forRootAsync, synchronize:false,
      autoLoadEntities:true; 20 real migrations via a dedicated data-source.ts); (2) Prisma NEVER
      executes anywhere — no prisma/@prisma/client dependency in any package.json, zero CI/script
      invocation, no generated client ever produced or imported (schema self-declares "KHÔNG sinh
      migration, KHÔNG sinh code, KHÔNG sinh API"); (3) most existing documentation (README.md,
      project-registry.yaml, the EEF repository-map, architecture.md) was ALREADY consistent with
      this determination. The one genuinely contradictory line — database.md §11's heading "chuẩn bị
      sinh Prisma" (implied active future Prisma codegen intent) — was reworded, and a durable closing
      addendum was added to ADR-013 (its Superseded status left UNCHANGED) formally recording the
      runtime-persistence-authority determination. prisma/schema.prisma NOT modified, NOT removed, NOT
      regenerated. Full lint + typecheck + unit (221/221) + e2e (44/44) — IDENTICAL totals to PLACE-024,
      proving zero runtime impact; clean build 4/4 (tsbuildinfo purged); artifacts 153==153 (unchanged
      count); API health 200 (database=up, redis=up PONG); port freed.
    report: docs/delivery/reports/PLACE-025-persistence-authority-report.md
    evidence_index: docs/delivery/evidence/PLACE-025-persistence-authority-evidence-index.md
  - id: PLACE-024
    completed: "2026-07-24"
    result: >-
      F-35 RESOLVED. OD-B4/B4-A contract cleanup: `SearchResult.score` (the public leak of Postgres
      ts_rank) REMOVED from search.service.ts's public mapping, docs/api/openapi.yaml SearchResult
      schema, docs/api/api.md §22, and the web SearchResult TypeScript interface. ts_rank computation
      and `ORDER BY score DESC, p.id ASC` RETAINED byte-identical in places.repository.ts — purely
      internal now, proven by places.repository.spec 21/21 UNCHANGED. No renamed equivalent field
      introduced (verified by a recursive scan for score/rank/ts_rank/searchRank at every JSON nesting
      depth). Ordering preservation was EMPIRICALLY PROVEN: id order captured against the pre-change
      production build for two queries ('bien', 'phu quoc') plus a page-2/limit-3 pagination case
      matched EXACTLY post-change, both in a new contract e2e (6 tests) and independently via live
      curl. No frontend component read `.score` (confirmed by a full source grep before the change,
      zero hits beyond the interface declaration); apps/web tsc exit 0 confirms nothing broke. Full
      unit 221/221 (220+1) + e2e 44/44 (38+6) green; clean build 4/4 (tsbuildinfo purged); artifacts
      153==153; health 200 db+redis up; web 200; live /search 0 score occurrences, identical order,
      repeated query byte-identical hash; ports freed. Runtime otherwise UNCHANGED.
    report: docs/delivery/reports/PLACE-024-search-score-removal-report.md
    evidence_index: docs/delivery/evidence/PLACE-024-search-score-removal-evidence-index.md
  - id: PLACE-023
    completed: "2026-07-24"
    result: >-
      F-34 RESOLVED. OD-B3/B3-A correctness hardening: bboxClusters LIMIT-500 truncation made
      DETERMINISTIC by adding `ORDER BY cnt DESC, sample_id ASC` immediately before `LIMIT $6`.
      sample_id = (array_agg(p.id ORDER BY p.id))[1] = the minimum p.id per grid cell — a UNIQUE stable
      cell key (each place is in exactly one cell; p.id is an immutable PK) and an EXISTING returned
      field, reusing the repository's universal `p.id ASC` ordering convention. ORDERING ONLY: SELECT,
      GROUP BY, cell size ($5), aggregation, WHERE and the LIMIT $6 parameter are byte-identical; no
      DTO, HTTP contract, pagination, authorization, DB schema, migration, index or randomness change.
      Proven by 4 SQL-shape unit tests + a new real-DB e2e (5 tests) executed 5 CONSECUTIVE times
      (byte-identical output at zoom 9 and 14; densest-first; equal-cnt cells strictly ascending by
      id) + live HTTP /api/geo/bbox fetched 5x returning a single identical SHA-256. Full unit 220/220
      (216+4) + e2e 38/38 (33+5) green; clean build 4/4; artifacts 153==153; health 200 db+redis up;
      web 200; ports freed. Runtime otherwise UNCHANGED.
    report: docs/delivery/reports/PLACE-023-bbox-deterministic-ordering-report.md
    evidence_index: docs/delivery/evidence/PLACE-023-bbox-deterministic-ordering-evidence-index.md
  - id: PLACE-022
    completed: "2026-07-24"
    result: >-
      F-24 RESOLVED. OD-B2/B2-A security hardening: PlacesRepository.getCardById renamed to
      getCardByIdIncludingInactive — a name that signals it returns inactive/non-published rows and
      that authorization must already be established. PURE RENAME: SQL, parameters, return type,
      null-handling and exceptions byte-identical; NO status predicate added/removed; no controller,
      DTO, guard, pagination or schema change. 5 service call sites + mock + 13 spec references
      updated. Added an enforceable architecture test (places-privileged-access.arch.spec.ts, 6
      tests) that combines runtime @Public metadata with static controller->service->repo
      reachability and an approved-caller allowlist; it fails if any @Public route can reach the
      privileged method (MUTATION-CHECKED: a synthetic @Public leak fails tests #4 and #6 naming the
      path; restore -> 6/6) and is 3x deterministic. Approved privileged callers:
      create/update/archive/approve (all non-@Public, permission-gated); NO @Public route reaches it
      (global APP_GUARD JwtAuthGuard+PermissionsGuard). Full unit 216/216 (210+6) + e2e 33/33 green;
      clean build 4/4 (tsbuildinfo purged); artifacts 153==153; health 200 db+redis up; web 200;
      public list published-only; non-existent detail 404; unauth write 401; ports freed. Runtime
      behaviour UNCHANGED.
    report: docs/delivery/reports/PLACE-022-privileged-card-access-report.md
    evidence_index: docs/delivery/evidence/PLACE-022-privileged-card-access-evidence-index.md
  - id: PLACE-021
    completed: "2026-07-24"
    result: >-
      GAP-05 & GAP-10 RESOLVED. B1-A (OD-B1): the existing OFFSET page/limit pagination is ratified as
      the authoritative public list contract; ADR-010 Accepted as the governing versioning decision; NO
      cursor pagination. Documentation/contract reconciliation to runtime ONLY — no application source,
      DTO, controller, service, repository or schema change (git diff limited to docs/** + one new test).
      openapi listPlaces + api.md corrected: invalid page/limit/price_range documented 422 -> 400 (the
      runtime returns 400 via ValidationPipe default + AllExceptionsFilter statusToCode, NEVER 422 —
      confirmed live), a '400' response added, and status/sort/cursor KEPT deprecated (400) per OD-F-6
      with cursor decided-against for v1. 11 new HTTP contract tests (production-equivalent pipe:
      whitelist+transform+forbidNonWhitelisted) pin default(1/20), explicit(2/5), limit>100 clamp,
      invalid page/limit -> 400, deprecated param -> 400, and meta shape. Verified on the booted
      production build (default total=49/totalPages=3; all cases green). Full lint + typecheck + unit
      210/210 + e2e 33/33 (22+11) green; clean build 4/4; health 200 db+redis up; web 200; ports freed.
      Runtime behaviour UNCHANGED. Sole hard release blocker cleared.
    report: docs/delivery/reports/PLACE-021-list-pagination-contract-report.md
    evidence_index: docs/delivery/evidence/PLACE-021-list-pagination-contract-evidence-index.md
  - id: PLACE-020
    completed: "2026-07-24"
    result: >-
      RECONCILED. Owner-authorized delivery-state reconciliation (governance-only; zero application
      source, test, schema, migration or contract change — git diff limited to docs/delivery/**).
      Recorded the certified D: runtime facts and closed the two in-repo release blockers that
      PLACE-016..019 called "not fixable in-repo": F-2 (Docker) RESOLVED — Docker running,
      phuquoc-postgres/redis/minio healthy on EXISTING volumes (not recreated), API e2e 5 suites /
      22 tests green against real Postgres/PostGIS + Redis, migrations table = 20 rows; and F-3
      (version control) RESOLVED — three commits on master (…0ca958f), clean tree, rollback point,
      no remote added. verification_environment reconciled to the D: NTFS checkout (Node v20.20.2 /
      npm 10.8.2 pinned and used; real workspace symlinks; docker installed_and_running;
      can_run_e2e true; production_startup verified: /api/health 200 db=up/redis=up, web / 200).
      Repository identity block moved from the obsolete F: (FAT32, vcs none) to D: (NTFS, git).
      NO gate VALUE was advanced (PLACE-019 precedent); only comments contradicted by evidence were
      refreshed. NO owner product decision was made: GAP-05/10 remains the sole outstanding release
      blocker and F-24/F-34/score/authoritative-bbox stay OPEN. READY_FOR_BUILD NOT asserted; the
      workstream is NOT marked complete. Full lint + typecheck + unit (210/210) + e2e (22/22)
      re-run green and UNCHANGED; all governance YAML parse.
    report: docs/delivery/reports/PLACE-020-state-reconciliation-report.md
    evidence_index: docs/delivery/evidence/PLACE-020-state-reconciliation-evidence-index.md
  - id: PLACE-019
    completed: "2026-07-23"
    result: >-
      PARTIALLY_REMEDIATED. Owner-authorized pre-BUILD readiness operation. RESOLVED: G-1
      (project-registry.yaml was UNPARSEABLE — env_template was a mapping key inside a sequence;
      repaired to a mapping with an explicit paths list, all values and comments preserved), G-2
      (PLACE-001 given a run_status transcribed from its own 2026-07-22 report, recording
      validation: not_executed — schema normalization, explicitly NOT re-execution), G-3 (three
      gate COMMENTS refreshed where later evidence contradicted them; all three gate VALUES left
      unchanged and still conservative), and F-33 (PlacesRepository.bbox() REMOVED after a
      re-run four-pass sweep found zero consumers; regression spec pins the removal; absent from
      the compiled artifact). `nest build` ran to exit 0 for the first time — 120 files,
      dist/main.js present, matching start:prod. Governance validation went 2 failures -> 0.
      jest 210/210, tsc + eslint exit 0. F-3 is only PARTIALLY CLEARED: the repo is a Git work
      tree but has NO COMMIT, so no rollback point exists, and Git needs a safe.directory
      exception that the authorization forbade setting. F-2 untouched — Docker install was not
      authorized. BOTH still block BUILD; no BUILD task was created and READY_FOR_BUILD is not
      asserted.
    report: docs/delivery/reports/PLACE-019-pre-build-readiness-report.md
    evidence_index: docs/delivery/evidence/PLACE-019-pre-build-readiness-evidence-index.md
  - id: PLACE-018
    completed: "2026-07-23"
    result: >-
      F-17 remediated per OD-F-17 (F17-D). `status` now declared on openapi PlaceCard using the
      PRE-EXISTING PlaceStatus enum — already public via the `Place` detail schema, so zero net-new
      disclosure — and recorded as publication state, NOT business operating state. `score` removed
      from shared-types PlaceCard and from the toPlaceCard branch. Emitted JSON byte-identical BOTH
      ways (runtime already set status; the score branch never executed). IMPORTANT CORRECTION found
      by the mandatory premise re-verification: search.service.ts:24 reads `r.score` off
      PlaceCardRow, so PlaceCardRow.score is LIVE — it was KEPT with a comment naming its consumer
      rather than removed as the decision literally instructed, which would have broken the
      out-of-scope search module. shared-types rebuilt AND re-copied into node_modules (FAT32
      copies, not links) before typechecking. jest places 107/107, search 3/3 UNMODIFIED, api+web
      tsc exit 0, openapi parses.
    report: docs/delivery/reports/PLACE-018-placecard-contract-report.md
    evidence_index: docs/delivery/evidence/PLACE-018-placecard-contract-evidence-index.md
  - id: PLACE-017
    completed: "2026-07-23"
    result: >-
      F-6 remediated per OD-F-6 (F6-A), DOCUMENTATION ONLY — zero runtime files changed, proven by
      jest places 105/105 being identical to the pre-task count. openapi listPlaces now documents
      exactly the five parameters ListPlacesQueryDto accepts, with real defaults (page 1, limit 20),
      the limit CLAMP at 100 (not a rejection), fixed server-side ordering, offset-only pagination,
      and an explicit statement that no geo filter exists here. status/sort/cursor kept with
      `deprecated: true` and notes stating they return HTTP 400 — deprecated rather than deleted
      because external consumers cannot be ruled out (OD-F-6 obligation 7). Meta corrected to the
      emitted envelope (timestamp/page/pageSize/total/totalPages); phantom `limit` removed,
      next_cursor/quota_remaining marked not-emitted. api.md §6/§11 reconciled. Verified by an
      automated contract check that parses the DTO's property names from SOURCE and asserts
      documented-ACTIVE == runtime-ACCEPTED. Also established by evidence of absence: NO Swagger
      decorators exist anywhere, and openapi.yaml is the only contract artifact.
    report: docs/delivery/reports/PLACE-017-openapi-alignment-report.md
    evidence_index: docs/delivery/evidence/PLACE-017-openapi-alignment-evidence-index.md
  - id: PLACE-016
    completed: "2026-07-23"
    result: >-
      F-1 hard enforcement REMOVED per approved owner decision OD-F-1 (F1-C). All six DTO fields
      (places.dto GeoPointDto lat/lng; geo.dto Nearby lat/lng; Bbox minLng/minLat/maxLng/maxLat)
      no longer reject coordinates for falling outside the PROVISIONAL box; the global
      @IsNumber/@Min/@Max guards were kept and are now the only rejection, proven by 7 new specs
      (lat 91, lng 181, NaN, non-numeric). PlacesService.create/update emit a structured
      `place.coordinate.outside_provisional_bounds` warning naming field, value, bounds and
      PROVISIONAL status — 5 specs assert the exact payload. Geo READ paths deliberately do NOT
      signal (an out-of-box read is harmless and would let anonymous callers amplify logs).
      PHU_QUOC_BOUNDS byte-unchanged, PROVISIONAL header intact, NO replacement coordinates
      invented. The two now-unreferenced decorator factories were deleted rather than left as dead
      exports. Mutation check: restoring @Min(9.7)/@Max(10.6) on lat failed exactly the 3
      lat-dependent specs (lng-only and service specs kept passing), then restored. jest places
      105/105, geo 13/13, eslint + tsc exit 0. F-1 release_blocker_status OPEN -> CLEARED on
      conditions pre-committed in findings/F-1.yaml BEFORE execution; the missing authoritative
      boundary is carried forward as NON-BLOCKING future work.
    report: docs/delivery/reports/PLACE-016-bbox-enforcement-report.md
    evidence_index: docs/delivery/evidence/PLACE-016-bbox-enforcement-evidence-index.md
  - id: PLACE-015
    completed: "2026-07-23"
    result: >-
      F-32 RESOLVED. searchFullText's `ORDER BY score DESC` gained the unique final key `p.id ASC`
      — the last query in the repository that combined a non-unique sort key with OFFSET
      pagination, i.e. the GAP-12 defect on the live public /search endpoint. 3 specs added
      (ORDER BY shape parsed from real SQL; two pages over six score-tied rows disjoint+complete
      under rotation; searchCount has no ORDER BY). The mutation check is the substantive evidence:
      with the tie-breaker removed, two pages over six tied rows returned 4 DISTINCT rows instead
      of 6 — duplication and omission together — failing exactly the 2 new determinism specs with
      no collateral. Restored; jest places 97/97, jest search 3/3 with search.service specs
      UNMODIFIED, eslint + tsc exit 0. openapi /search declares no ordering guarantee, so this was
      a quality fix, not a contract fix. searchCount verified unchanged. No index created (F-15).
    report: docs/delivery/reports/PLACE-015-search-ordering-report.md
    evidence_index: docs/delivery/evidence/PLACE-015-search-ordering-evidence-index.md
  - id: PLACE-001
    completed: "2026-07-22"
    result: >-
      Place domain + persistence baseline established (full spec). Vertical modules
      (Hotel/Restaurant/Tour) confirmed as ADR-002 satellites of Place; Business =
      claim/membership overlay on Place. GAP-02/04 RESOLVED; GAP-05/06/07/10/11/12/13/14/15/16
      OPEN against current source. 5 contradictions registered. All checks NOT EXECUTED (no Node; FAT32).
    report: docs/delivery/reports/PLACE-001-place-domain-persistence-baseline.md
    evidence_index: docs/delivery/evidence/PLACE-001-evidence-index.md
    evidence_detail: docs/delivery/evidence/PLACE-001-baseline.md
  - id: PLACE-014
    completed: "2026-07-22"
    result: >-
      F-16 RECLASSIFIED, not simply "fixed". nearby() RESOLVED — `p.id ASC` appended (exact
      ST_Distance ties occur for co-located Places and decided what a user saw at the LIMIT cut);
      2 specs, mutation-checked, jest places 94/94. bbox() closed as not-applicable: it has NO
      CONSUMER (GeoService.bbox calls bboxClusters) → F-33. bboxClusters() reclassified as a
      PRODUCT decision — ordering it would decide which clusters survive truncation at 500 → F-34.
      Biggest discovery: searchFullText paginates with OFFSET over non-unique score — GAP-12's
      defect on a live user-facing endpoint → F-32, selected as PLACE-015. openapi declares no
      ordering guarantee on the geo endpoints, so this was a quality fix, not a contract fix.
    report: docs/delivery/reports/PLACE-014-geo-ordering-analysis.md
    evidence_index: docs/delivery/evidence/PLACE-014-geo-ordering-evidence-index.md
  - id: PLACE-013
    completed: "2026-07-22"
    result: >-
      Build hygiene closed. F-12 was ALREADY FIXED before this task by a user-started background
      task (InitAuditLogs.spec.ts now in migrations/__tests__/ with a corrected import) — this
      task VERIFIED it: migrations/ holds 20 files all real migrations, suite 11/11 unchanged,
      duplicate 1720001500000 prefix gone. F-23 RESOLVED here: apps/api/dist/ (279 stale files,
      still containing getCardBySlug removed by PLACE-007) deleted after regenerability was
      PROVEN via `tsc -p tsconfig.build.json` into a scratchpad — dist/ untouched, `nest build`
      not run. All validations green after removal. No source file changed.
    report: docs/delivery/reports/PLACE-013-build-hygiene-report.md
    evidence_index: docs/delivery/evidence/PLACE-013-build-hygiene-evidence-index.md
  - id: PLACE-012
    completed: "2026-07-22"
    result: >-
      F-19 and F-20 RESOLVED. Shared PlaceDetail.opening_hours is now typed by a structured
      OpeningHours (derived from places.md §4 AND from PLACE-006's validator, all fields optional
      + index signature so it is never stricter than what the write path accepts), and
      toPlaceDetail emits ISO strings matching its own declared wire type. Emitted JSON unchanged
      — JSON.stringify already produced those exact strings via Date.toJSON. jest places 92/92
      with mapper and DTO specs UNMODIFIED; apps/web tsc exit 0, so the read-contract tightening
      broke no consumer. Validator deliberately not coupled to the shared type (F-30).
    report: docs/delivery/reports/PLACE-012-boundary-typing-report.md
    evidence_index: docs/delivery/evidence/PLACE-012-boundary-typing-evidence-index.md
  - id: PLACE-011
    completed: "2026-07-22"
    result: >-
      F-18 RESOLVED. PlaceCardRow.verification_status typed against the shared union instead of
      `string`, so the unchecked `as VerificationStatusValue` cast in places.mapper.ts is gone
      (grep → 0 occurrences). Sound because InitPlaces:47 declares the column ENUM NOT NULL — the
      type now states the invariant the cast merely asserted. jest places 92/92 with specs
      UNMODIFIED, api+web tsc exit 0, first attempt. No SQL, migration or contract change.
    report: docs/delivery/reports/PLACE-011-row-typing-report.md
    evidence_index: docs/delivery/evidence/PLACE-011-row-typing-evidence-index.md
  - id: PLACE-010
    completed: "2026-07-22"
    result: >-
      Release-readiness assessed from repository evidence only. Overall: NOT READY FOR RELEASE.
      13 gates classified, NONE proven — 103 specs pass but every one runs with the database
      mocked or absent. F-1..F-28 consolidated: 5 BLOCKS_RELEASE (PROVISIONAL bbox now actively
      enforced, no Docker, no version control, GAP-05/10 list params, openapi PlaceCard omits
      status/score), 18 NON_BLOCKING, 5 RESOLVED. Three carried claims re-measured rather than
      copied. No gate upgraded; no product code changed; workstream stays INCOMPLETE and closure
      was deliberately not attempted.
    report: docs/delivery/reports/PLACE-010-release-readiness-assessment.md
    evidence_index: docs/delivery/evidence/PLACE-010-readiness-evidence-index.md
  - id: PLACE-009
    completed: "2026-07-22"
    result: >-
      PlacesController route boundary now pinned — 23 specs, first run green; jest places 92/92
      across 7 suites; eslint + tsc exit 0. NO production code changed. Asserts the 3 public
      reads, the 4 permission-gated writes with their exact permission strings (independent
      literals), @HttpCode(201), the ':id/revisions' before ':slug' declaration order, and that
      write routes pass user.sub rather than the whole principal. Scope boundary stated
      explicitly: this asserts what the controller DECLARES, not what the guard DOES.
    report: docs/delivery/reports/PLACE-009-controller-tests-report.md
    evidence_index: docs/delivery/evidence/PLACE-009-controller-tests-evidence-index.md
  - id: PLACE-008
    completed: "2026-07-22"
    result: >-
      PlacesService now has unit coverage — 18 specs, passing on the first run; jest places
      69/69 across 6 suites; eslint + tsc exit 0. NO production code changed. Pins the forced
      PENDING create, the WF-14 revision side effects, the snake_case→camelCase update mapping
      (snake keys asserted ABSENT from the ORM patch), the slug-collision loop, the service-layer
      GAP-04 privilege boundary, and the ADR-016 audit payloads down to permission and {from,to}.
      Disproved the stale place.yaml blocker "service imports @phuquochub/utils" (F-25).
    report: docs/delivery/reports/PLACE-008-service-tests-report.md
    evidence_index: docs/delivery/evidence/PLACE-008-service-tests-evidence-index.md
  - id: PLACE-007
    completed: "2026-07-22"
    result: >-
      GAP-13 RESOLVED. PlacesRepository.getCardBySlug REMOVED after a four-pass repository-wide
      sweep (bare identifier, dynamic bracket access, all *BySlug identifiers, and the exported
      module surface) found zero consumers; tsc exit 0 independently confirms none existed.
      Chose removal over adding a status filter because getCardById deliberately omits that
      filter for moderation, so "fixing" it would have encoded an unsettled intent. jest places
      51/51 with the GAP-02/04 regression specs unmodified. Findings F-23 (stale dist/) and
      F-24 (getCardById's undocumented filter omission) raised.
    report: docs/delivery/reports/PLACE-007-dead-code-report.md
    evidence_index: docs/delivery/evidence/PLACE-007-dead-code-evidence-index.md
  - id: PLACE-006
    completed: "2026-07-22"
    result: >-
      GAP-14 RESOLVED. opening_hours is now validated against places.md §4 by a reusable
      @IsOpeningHours() decorator (apps/api/src/common/opening-hours.ts) applied to BOTH
      CreatePlaceDto and UpdatePlaceDto. 18 new specs, 30/30 green; mutation check (decorator
      removed) fails 9. The SSOT example passes verbatim; unknown keys tolerated (openapi
      additionalProperties:true), open<close deliberately not enforced (overnight ranges), all
      fields optional. No seeded opening_hours exists, so no stored data is invalidated.
    report: docs/delivery/reports/PLACE-006-opening-hours-report.md
    evidence_index: docs/delivery/evidence/PLACE-006-opening-hours-evidence-index.md
  - id: PLACE-005
    completed: "2026-07-22"
    result: >-
      GAP-11 RESOLVED. Place response types now declared ONCE in packages/shared-types/src/
      place.ts and imported by both apps/api (places.mapper.ts) and apps/web (places/types.ts,
      now a pure re-export). Wire payload unchanged — proven by the pre-existing mapper specs
      passing UNMODIFIED (jest places 33/33). apps/web tsc exit 0 (baseline before edits also 0),
      apps/api tsc exit 0, both lints exit 0. Enums represented as string-literal unions so the
      web bundle loads no runtime enum. Findings F-17/F-18/F-19 raised, not absorbed.
    report: docs/delivery/reports/PLACE-005-shared-types-report.md
    evidence_index: docs/delivery/evidence/PLACE-005-shared-types-evidence-index.md
  - id: PLACE-004
    completed: "2026-07-22"
    result: >-
      GAP-12 RESOLVED. PlacesRepository.list() page query now orders by
      `rating_avg DESC NULLS LAST, created_at DESC, id ASC` — a unique final key, so
      LIMIT/OFFSET pagination is deterministic. 4 new specs (11/11 green), eslint + tsc exit 0.
      Mutation-checked: removing the tie-breaker fails exactly the 2 meaningful specs.
      Caller-visible ordering unchanged; count query provably unaffected; no index created
      (recorded as F-15 for DB-backed validation).
    report: docs/delivery/reports/PLACE-004-ordering-report.md
    evidence_index: docs/delivery/evidence/PLACE-004-ordering-evidence-index.md
  - id: PLACE-003
    completed: "2026-07-22"
    result: >-
      GAP-06 RESOLVED. Forward-only migration 1720001900000-AddPlacesStatusPartialIndex creates
      idx_places_status_active = BTREE(status) WHERE deleted_at IS NULL on places, matching
      places.md §3; down() drops only that index. 5 new migration specs; jest migrations 11/11,
      eslint (migrations + places) exit 0, tsc --noEmit exit 0. AC1..AC6 PASS, optional AC7 PASS.
      Migration is implemented_not_executed — no database was touched (Docker not installed) and
      no EXPLAIN evidence is claimed. Entity change was a comment only.
    report: docs/delivery/reports/PLACE-003-migration-report.md
    evidence_index: docs/delivery/evidence/PLACE-003-migration-evidence-index.md
  - id: PLACE-002
    completed: "2026-07-22"
    result: >-
      GAP-07 RESOLVED. Phú Quốc coordinate bounds enforced on GeoPointDto, NearbyQueryDto and
      BboxQueryDto, plus an explicit radius @Max(50000) matching the service cap. All four
      mandatory validation commands EXECUTED and green: jest places.dto 12/12, jest geo.dto
      9/9, eslint (places+geo) exit 0, tsc --noEmit exit 0. AC1..AC5 all PASS. Phú Quốc bbox
      remains PROVISIONAL pending owner confirmation (permitted by AC5; carried as a finding).
    report: docs/delivery/reports/PLACE-002-implementation-report.md
    evidence_index: docs/delivery/evidence/PLACE-002-evidence-index.md

blocked_task_attempts:
  # PLACE-003..PLACE-007 were prompted while state.yaml still authorized PLACE-002 and no
  # task file existed for them. Each returned BLOCKED at preflight. Retained as history.
  # ADR-DELIVERY-001 applies: state, not task numbering, is the execution gate.
  - { tasks: "PLACE-003..PLACE-005", result: blocked_preflight, cause: "no task file; PLACE-002 unvalidated; no Node",
      reports: "docs/delivery/reports/PLACE-00{3,4,5}-execution-report.md" }
  - { tasks: "PLACE-006, PLACE-007", result: blocked_preflight, cause: "same; no block report authored (duplicate of the above)",
      note: "PLACE-006 pivoted to clearing the environment blocker, which unblocked PLACE-002." }
  - { tasks: "PLACE-008", date: "2026-07-22", result: blocked_preflight,
      cause: "no docs/delivery/tasks/PLACE-008.yaml; state.yaml authorizes PLACE-003 (ready). No PLACE-004..PLACE-007 task file exists either, so PLACE-008 has no completed dependency chain.",
      note: "No task file was created and no work was executed; creating PLACE-008 solely because a prompt names it is prohibited. Authorized work remains PLACE-003." }
  - { tasks: "PLACE-009", date: "2026-07-22", result: blocked_preflight,
      cause: "no docs/delivery/tasks/PLACE-009.yaml; state.yaml authorizes PLACE-003 (ready); declared dependency PLACE-008 is itself BLOCKED, never completed (reports/PLACE-008-execution-report.md).",
      note: "Seventh consecutive forward-numbered prompt. The gate is functioning; the loop ends only when PLACE-003 is executed." }
  - { tasks: "PLACE-010", date: "2026-07-22", result: blocked_preflight,
      cause: "no docs/delivery/tasks/PLACE-010.yaml; state.yaml authorizes PLACE-003 (ready); declared dependency PLACE-009 is itself BLOCKED, never completed (reports/PLACE-009-execution-report.md).",
      note: "Eighth consecutive forward-numbered prompt. Per-attempt block reports discontinued after PLACE-009 (finding F-11); this list is now the single record for repeat attempts." }
  - { tasks: "PLACE-011", date: "2026-07-22", result: blocked_preflight,
      cause: "no docs/delivery/tasks/PLACE-011.yaml; state.yaml authorizes PLACE-003 (ready); declared dependency PLACE-010 is itself BLOCKED, never completed.",
      note: "Ninth consecutive forward-numbered prompt. Cause is identical and unchanged since PLACE-003 was derived; no new information." }
  - { tasks: "PLACE-012", date: "2026-07-22", result: blocked_preflight,
      cause: "no docs/delivery/tasks/PLACE-012.yaml; state.yaml authorizes PLACE-003 (ready); declared dependency PLACE-011 is itself BLOCKED, never completed.",
      note: "Tenth consecutive forward-numbered prompt, identical cause. Further repeat attempts need not be logged individually — PLACE-003 execution is the only state change that can alter this outcome." }
